/**
 * Prettier's defaults, on purpose.
 *
 * Four fifths of the code already matched them before this file existed — the
 * hand-over was written in an editor with Prettier's defaults switched on — so
 * adopting anything else would have meant reformatting files that were already
 * consistent, to settle questions nobody in this repo has an opinion about.
 * The one reformat this repo owes its history is the one that came with this
 * file; there is no reason to spend a second.
 *
 * ESLint has no say in formatting here: neither `js.configs.recommended` nor
 * typescript-eslint's `recommended` turns on a stylistic rule, so the two tools
 * cannot disagree and `eslint-config-prettier` has nothing to switch off.
 */
export default {};
