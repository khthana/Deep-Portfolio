# การเปลี่ยนพฤติกรรม (Behaviour changes)

บันทึกทุกจุดที่ระบบ **ตอบไม่เหมือนเดิม** หลังการ refactor — ไม่ใช่ทุกการแก้โค้ด
แต่เฉพาะกรณีที่ผู้เรียก API (โดยเฉพาะ frontend) อาจสังเกตเห็นความต่าง

ทำไมต้องมีไฟล์นี้ (ข้อ D9 ใน [`docs/spec-refactor-redeploy.md`](docs/spec-refactor-redeploy.md)):
งานชุดนี้เขียน test ตาม "พฤติกรรมเดิม" เป็นหลัก ถ้าเจอพฤติกรรมที่ผิดจริงๆ ให้แก้
ทันทีแล้วเขียน test ให้สะท้อนพฤติกรรมที่ถูก — แต่การแก้แบบนั้นทำให้ผู้ที่เคยพึ่ง
พฤติกรรมเดิมพัง จึงต้องจดไว้ว่าเปลี่ยนอะไร เพราะอะไร และไล่แก้ frontend
ที่เรียก endpoint นั้นในการเปลี่ยนแปลงเดียวกัน

รูปแบบของแต่ละรายการ: **endpoint ที่กระทบ → ของเดิม → ของใหม่ → เหตุผล →
frontend ที่ต้องตามแก้**

**สิ่งที่ไม่อยู่ในไฟล์นี้: คำสั่ง CLI** — ตัวนำเข้า master data ไม่มี endpoint และผู้ใช้ของมัน
คือผู้ดูแลที่นั่งอยู่หน้าเครื่อง ไม่ใช่ frontend การเปลี่ยนวิธีเรียกหรือผลลัพธ์ของคำสั่งนั้น
จึงจดไว้ที่ [`docs/importer.md`](docs/importer.md) แทน ที่เดียวกับวิธีใช้ ไม่ต้องมาเปิดสองที่

---

## #10 — รวม auth middleware เป็น `requireRole`

### 1. `GET /auth` เมื่อ token ใช้ได้แต่ผู้ใช้ไม่มีอยู่จริง

| | |
| --- | --- |
| **ของเดิม** | `404` พร้อม `{ "message": "ไม่พบข้อมูลผู้ใช้งาน" }` — middleware ปล่อยผ่าน แล้ว controller ค้นไม่เจอ |
| **ของใหม่** | `401` พร้อม `{ "message": "ไม่พบ Token หรือ Token หมดอายุ" }` — middleware ตรวจตั้งแต่ต้นทาง |
| **เหตุผล** | ลายเซ็นของ token พิสูจน์ได้แค่ว่า server นี้เป็นคนออกให้ ไม่ได้พิสูจน์ว่าบัญชีที่ระบุยังอยู่ ของเดิม token ของผู้ใช้ที่ถูกลบไปแล้วยังนับเป็น session ที่ใช้ได้จนกว่าจะหมดอายุ และ controller ทุกตัวหลัง middleware ต้องรับมือกับ `user_id` ที่ชี้ไปยังแถวที่ไม่มี — `404` ยังสื่อผิดด้วย เพราะแปลว่า "ทรัพยากรที่ขอไม่มี" ไม่ใช่ "session ของคุณใช้ไม่ได้แล้ว" |
| **frontend** | ไม่ต้องแก้ — `src/hooks/use-auth.ts` จับ error ทุกชนิดแล้วตั้ง `isAuthenticated = false` เหมือนกัน และ interceptor ใน `src/lib/axios.ts` จัด `/auth` ไว้ใน `isAuthRoute` อยู่แล้ว จึงไม่วน refresh *(#11 เอา `/auth` ออกจาก `isAuthRoute` แล้ว — ดูข้อ 6 ของ #11 ยังไม่วน เพราะ `_retry` กันการลองซ้ำรอบที่สองอยู่)* |

### 2. `GET /auth` เมื่อไม่มี token / token ปลอม / token หมดอายุ

| | |
| --- | --- |
| **ของเดิม** | `401` พร้อม `{ "message": "Unauthorized" }` |
| **ของใหม่** | `401` พร้อม `{ "message": "ไม่พบ Token หรือ Token หมดอายุ" }` |
| **เหตุผล** | สถานะ (`401`) เท่าเดิม เปลี่ยนเฉพาะข้อความ ของเดิม endpoint นี้ใช้ middleware คนละตัวกับ route อื่น จึงตอบเป็นภาษาอังกฤษอยู่ตัวเดียวในระบบ ทั้งที่ข้อความ error ที่ frontend เอาไปแสดงตรงๆ ต้องเป็นภาษาไทย พอรวม middleware เป็นตัวเดียว ข้อความก็มาจากที่เดียวกัน |
| **frontend** | ไม่ต้องแก้ — ไม่มีที่ไหนอ่าน `message` ของ `401` จาก `/auth` |

### 3. `POST /auth/logout` ลบ cookie ได้จริง

| | |
| --- | --- |
| **ของเดิม** | ตอบ `200` แต่ `access_token` / `refresh_token` ยังอยู่ในเบราว์เซอร์ — ตอน login ตั้ง cookie ที่โดเมน `localhost` (dev) ส่วนตอน logout สั่งลบที่โดเมน `portfolio-api.deep-core.net` ที่ hardcode ไว้ |
| **ของใหม่** | ตอบ `200` และ cookie ถูกลบจริง |
| **เหตุผล** | cookie ถูกระบุตัวด้วย (ชื่อ, โดเมน, path) พร้อมกันทั้งสามอย่าง สั่งลบด้วยโดเมนอื่นจึงไม่ได้ลบอะไรเลย — ผลคือ logout ที่ดูสำเร็จแต่ session ยังใช้ได้ต่อจนกว่า token จะหมดอายุ ตอนนี้ทั้งฝั่ง set และฝั่ง clear อ่านค่าจาก `sessionCookieOptions()` ที่เดียว จึงหลุดจากกันอีกไม่ได้ |
| **frontend** | ไม่ต้องแก้ — `student-info-dropdown.tsx` / `teacher-info-dropdown.tsx` เรียก logout แล้ว redirect ออกอยู่แล้ว ฝั่งที่พังคือ backend ล้วนๆ |

### 4. `POST /auth/refresh` คืน cookie ที่ใช้ได้จริง

| | |
| --- | --- |
| **ของเดิม** | ตอบ `200` และตั้ง `access_token` ใหม่ **โดยไม่ระบุ `path`** — cookie จึงติดอยู่ที่ `/auth/refresh` ตาม path ของ request และไม่ถูกส่งไปกับ request อื่นเลย ส่วนของเก่าที่ path `/` ก็ไม่ได้ถูกลบ (โดเมนไม่ตรง ตามข้อ 3) |
| **ของใหม่** | `access_token` ใหม่ตั้งที่ `path: "/"` โดเมนเดียวกับตอน login จึงทับใบเก่าและถูกส่งไปทุก endpoint |
| **เหตุผล** | interceptor ของ frontend เรียก `/auth/refresh` แล้ว retry request เดิม ของเดิม retry นั้นยังแนบ token ใบที่หมดอายุไปเหมือนเดิม การต่ออายุ session จึงไม่เคยทำงาน |
| **frontend** | ไม่ต้องแก้ — `src/lib/axios.ts` เขียนถูกอยู่แล้ว มันแค่ไม่เคยได้ cookie ที่ใช้ได้กลับมา |

### 5. โดเมนของ cookie มาจาก config แทนการ hardcode

| | |
| --- | --- |
| **ของเดิม** | โดเมนเขียนตายในโค้ด: `portfolio-api.deep-core.net` เมื่อ production, `localhost` เมื่อไม่ใช่ |
| **ของใหม่** | อ่านจาก `COOKIE_DOMAIN` ค่าเริ่มต้นคือเว้นว่าง ซึ่งแปลว่าไม่ใส่ attribute `Domain` เลย (host-only cookie) |
| **เหตุผล** | ย้ายระบบไป server อื่นแล้วต้องแก้โค้ดถึงจะ login ได้ ไม่ใช่การตั้งค่าที่ควรอยู่ในโค้ด และ host-only เป็นค่าที่ถูกต้องกว่าสำหรับ dev อยู่แล้ว ตั้ง `COOKIE_DOMAIN` เฉพาะเมื่อ API กับ web app อยู่คนละ subdomain ของโดเมนเดียวกันจริงๆ |
| **frontend** | ไม่ต้องแก้ — `withCredentials: true` อยู่แล้ว |

### 6. ข้อผิดพลาดที่ไม่คาดคิดใน middleware ไปที่ error handler กลาง

| | |
| --- | --- |
| **ของเดิม** | middleware จับ error เองแล้วตอบ `500` พร้อม `{ "message": "Internal Server Error" }` |
| **ของใหม่** | ส่งต่อด้วย `next(error)` ไปที่ `errorHandler` ซึ่งตอบ `500` พร้อม `{ "success": false, "message": ... }` |
| **เหตุผล** | ของเดิมกลืน error จริง (เช่น ฐานข้อมูลล่ม) ทิ้งไปทั้งก้อน ไม่มีอะไรถูก log และหน้าตาของ `500` จาก middleware ก็ไม่เหมือน `500` จากที่อื่นในระบบ |
| **frontend** | ไม่ต้องแก้ — ไม่มีที่ไหน branch ตามหน้าตาของ body ที่ได้จาก `500` |

---

## #12 — test ของกลุ่ม endpoint ตั้งค่ารายวิชา

### 1. `POST /announcement` ต้องเป็นอาจารย์ที่ล็อกอินแล้ว

| | |
| --- | --- |
| **ของเดิม** | ไม่มีการตรวจสิทธิ์ใดๆ เลย — route นี้ไม่ได้ผ่าน middleware ตัวไหน ใครก็ตามที่ยิงถึง API ได้ โพสต์ประกาศเข้า section ไหนก็ได้ และอัปโหลดไฟล์เข้า bucket ได้ |
| **ของใหม่** | `401` เมื่อไม่มี session, `403` เมื่อผู้ใช้ไม่มีบทบาท `TEACHER` และไม่มีอะไรถูกเขียนลงฐานข้อมูลหรือ bucket ทั้งสองกรณี |
| **เหตุผล** | เป็นช่องโหว่ตรงๆ ไม่ใช่เรื่องความสม่ำเสมอของโค้ด ทุก endpoint ที่เขียนข้อมูลของ section ตัวอื่นในระบบตรวจ `requireRole("TEACHER")` หมด ยกเว้นตัวนี้ตัวเดียว น่าจะตกหล่นตอนเขียน — และเป็น endpoint ที่รับไฟล์ด้วย ผลจึงไม่ใช่แค่ข้อมูลปลอมในฟีดแต่รวมถึงการเอาพื้นที่เก็บไฟล์ไปใช้ฟรี |
| **frontend** | ไม่ต้องแก้ — `features/teacher/announcement/services/announcement-service.service.ts` ยิงผ่าน `axiosInstance` ซึ่งตั้ง `withCredentials: true` อยู่แล้ว และหน้าที่เรียกอยู่หลัง guard ของฝั่งอาจารย์ |

### 2. `POST /course-material` ตรวจสิทธิ์ก่อนรับไฟล์

| | |
| --- | --- |
| **ของเดิม** | ลำดับ middleware เป็น `upload.fields(...)` แล้วค่อย `requireRole("TEACHER")` — คำขอที่กำลังจะโดนปฏิเสธถูกอ่านไฟล์เข้าหน่วยความจำจนครบก่อน แล้วจึงตอบ `401`/`403` |
| **ของใหม่** | `requireRole("TEACHER")` มาก่อน `upload.fields(...)` สถานะและข้อความที่ผู้เรียกเห็นเหมือนเดิมทุกตัวอักษร ต่างกันตรงที่ไฟล์ไม่ถูกอ่านเลย |
| **เหตุผล** | multer ทำงานจนจบก่อนที่ middleware ตัวถัดไปจะถูกเรียก การวางไว้หน้าสุดจึงเท่ากับให้ผู้ที่ไม่มีสิทธิ์ส่งไฟล์ขนาดเท่าไรก็ได้เข้ามาให้ server แบกไว้ก่อนเสมอ — และ handler อยู่ห่างจากการเขียนลง bucket แค่ก้าวเดียว |
| **frontend** | ไม่ต้องแก้ — `features/teacher/material/services/teacher-material.service.ts` ใช้ `axiosInstance` ตัวเดียวกัน คำขอที่ถูกต้องยังผ่านเหมือนเดิม |

### 3. `DELETE /course/clo` เรียงเลข CLO ใหม่ตามลำดับที่แน่นอน

| | |
| --- | --- |
| **ของเดิม** | หลังลบ ระบบไล่ตั้ง `clo_number` ใหม่เป็น 1..n ตามลำดับที่ Postgres คืนแถวมา ซึ่ง `findMany` ที่ไม่มี `orderBy` ไม่รับประกันอะไรเลย — การลบ CLO หนึ่งข้อจึงสลับลำดับข้ออื่นได้ |
| **ของใหม่** | เรียงด้วย `clo_id` จากน้อยไปมากก่อนไล่เลข ผลคือข้อที่เหลือรักษาลำดับเดิมไว้เสมอ |
| **เหตุผล** | `clo_id` จากน้อยไปมากคือลำดับที่ `GET /course/clo` คืนมา ซึ่งก็คือลำดับที่อาจารย์เห็นบนหน้าจอ การลบข้อหนึ่งแล้วอีกสองข้อสลับที่กันเองไม่ใช่สิ่งที่ผู้ใช้สั่ง และไม่มีทางย้อนกลับได้เพราะเลขถูกเขียนทับไปแล้ว |
| **frontend** | ไม่ต้องแก้ — `features/teacher/course/components/clo-table.tsx` เรียก `handleFetchData()` ต่อจากลบทันที จึงได้เลขชุดใหม่มาแสดงอยู่แล้ว |

### 4. `DELETE /lesson-plan` เรียงสัปดาห์ใหม่ทั้ง section ไม่ใช่เฉพาะของผู้เขียนคนเดียว

| | |
| --- | --- |
| **ของเดิม** | หลังลบ ระบบไล่ตั้ง `week_no` ใหม่จากแถวที่ `section_id` **และ** `created_by` ตรงกับของแถวที่เพิ่งลบ และไม่มี `orderBy` |
| **ของใหม่** | ไล่เลขจากทุกแถวของ section นั้น เรียงตาม `week_no` เดิมจากน้อยไปมาก |
| **เหตุผล** | เลขสัปดาห์เป็นของแผนการสอนของ section ไม่ใช่ของอาจารย์คนใดคนหนึ่ง ในวิชาที่สอนร่วมกัน สัปดาห์ของอาจารย์อีกคนถูกข้ามไปจากการไล่เลข ผลคือเลขสัปดาห์ซ้ำกันในตารางเดียว (เช่นเหลือสัปดาห์ 1, 1, 2) ส่วน `orderBy` เป็นเหตุผลเดียวกับข้อ 3 |
| **frontend** | ไม่ต้องแก้ — `features/teacher/lesson-plan/components/lesson-plan-table.tsx` เรียก `handleFetchData()` ต่อจากลบเช่นกัน |

---

## #11 — เข้าสู่ระบบด้วย Google แทน DEEP Core SSO

การเปลี่ยนแปลงชุดนี้เป็นการ **เปลี่ยนวิธี login ทั้งเส้น** ไม่ใช่การแก้ของเสีย
เป็นจุดๆ จึงกระทบ frontend จริงและต้องแก้ตามในการเปลี่ยนแปลงเดียวกัน

### 1. `GET /auth/login` หายไป แทนที่ด้วย `POST /auth/google`

| | |
| --- | --- |
| **ของเดิม** | `GET /auth/login` อ่าน cookie ชื่อ `token` ที่ DEEP Core ตั้งไว้ ตรวจด้วย `DEEP_CORE_SECRET` แล้วออก session ของ Portfolio ให้ — เป็นทางเข้าเดียวที่มี |
| **ของใหม่** | `POST /auth/google` รับ `{ "credential": "<Google ID token>" }` ทาง body ตรวจ token กับ Google แล้วออก session ชุดเดิม (`access_token` 15 นาที + `refresh_token` 7 วัน) route เดิมถูกลบทิ้ง เรียกแล้วได้ `404` |
| **เหตุผล** | Portfolio เป็นระบบ standalone (มีฐานข้อมูลของตัวเองครบ) การ login จึงไม่ควรผูกกับการมีอยู่ของ DEEP Core และ `DEEP_CORE_SECRET` เป็นความลับที่ต้องแชร์ระหว่างสองระบบ — ความลับที่แชร์กันคือความลับที่เพิกถอนฝ่ายเดียวไม่ได้ ฝั่ง Google ไม่มีความลับให้แชร์เลย เบราว์เซอร์ขอ ID token จาก Google ตรงๆ server มีหน้าที่ตรวจอย่างเดียว |
| **frontend** | **ต้องแก้ และแก้แล้ว** — เพิ่มหน้า `/login` (`features/shared/login/pages/login-page.tsx`) ที่โหลด Google Identity Services แล้ว POST credential ไปที่ endpoint ใหม่, `hooks/use-auth.ts` เลิกเรียก `/auth/login` ก่อนทุกครั้ง เหลือ `GET /auth` อย่างเดียว, `configs/endpoints.config.ts` เปลี่ยน `auth.login` เป็น `auth.google` |

### 2. อีเมลที่ไม่มีในตาราง `users` ถูกปฏิเสธ ไม่สร้างผู้ใช้ใหม่

| | |
| --- | --- |
| **ของเดิม** | ไม่มีกรณีนี้ — DEEP Core เป็นคนตัดสินว่าใครเข้าได้ Portfolio เชื่อ `user_id` ที่มากับ token โดยไม่ตรวจว่าแถวนั้นมีจริงไหม |
| **ของใหม่** | `403` พร้อม `{ "message": "ไม่พบบัญชีผู้ใช้ของอีเมลนี้ในระบบ กรุณาติดต่อผู้ดูแลระบบ" }` และไม่มีแถวใดถูกสร้าง |
| **เหตุผล** | `users.user_id` เป็น `VarChar(8)` ที่มีความหมายนอกระบบ (รหัสนักศึกษา/รหัสบุคลากร) ไม่มีอะไรที่ server จะกุขึ้นมาเองได้ บัญชี Google พิสูจน์ได้แค่ว่า "คุณคือใคร" ไม่ได้พิสูจน์ว่า "คุณเป็นคนของที่นี่" — สองคำถามนี้แยกกัน และคำถามที่สองเป็นของเรา |
| **frontend** | ไม่ต้องแก้เพิ่ม — หน้า login แสดง `message` ที่ API ส่งกลับมาตรงๆ ซึ่งเป็นข้อความไทยที่บอกว่าต้องทำอะไรต่อ |

### 3. อีเมลจับคู่แบบไม่สนตัวพิมพ์เล็กใหญ่

| | |
| --- | --- |
| **ของเดิม** | ไม่มีการค้นด้วยอีเมลเลย |
| **ของใหม่** | ค้น `users.email` แบบ case-insensitive |
| **เหตุผล** | Google คืนอีเมลเป็นตัวพิมพ์เล็กเสมอ ส่วนแถวใน `users` เก็บตามที่ import เข้ามา บัญชีที่มีอยู่จริงต้องไม่เข้าไม่ได้เพราะความต่างที่มองไม่เห็น |
| **frontend** | ไม่ต้องแก้ |

### 4. `POST /auth/logout` เลิกยุ่งกับ cookie ของ DEEP Core

| | |
| --- | --- |
| **ของเดิม** | สั่ง `res.clearCookie("token", { domain: ".deep-core.net" })` ก่อนลบ cookie ของตัวเอง — บรรทัดที่ไม่เคยลบอะไรได้จริง (ดูหมายเหตุของ #10) |
| **ของใหม่** | ลบเฉพาะ `access_token` / `refresh_token` ด้วย `sessionCookieOptions()` เท่าเดิม สถานะและ body เหมือนเดิมทุกตัวอักษร |
| **เหตุผล** | ไม่มี SSO cookie ให้ลบแล้ว |
| **frontend** | **ต้องแก้ และแก้แล้ว** — `student-info-dropdown.tsx` / `teacher-info-dropdown.tsx` เดิม redirect ไป `https://deep-core.net/` หลัง logout ตอนนี้ไปที่ `/login` และย้ายการ redirect เข้า `finally` เพราะ logout ที่ล้มเหลวก็ยังแปลว่าผู้ใช้ตั้งใจจะออก |

### 5. ผู้ที่ยังไม่ล็อกอินไปที่ `/login` ไม่ใช่ออกนอกเว็บ

| | |
| --- | --- |
| **ของเดิม** | guard ของนักศึกษาสั่ง `window.location.href = "https://deep-core.net/"` ส่วน guard ของอาจารย์สั่ง `<Navigate to="/unauthorized">` — สองที่ทำคนละอย่างกับสถานการณ์เดียวกัน |
| **ของใหม่** | ทั้งสอง guard ไปที่ `/login` เหมือนกัน และ `/` ที่เดิมตกไปหน้า 404 ตอนนี้พาไปหน้าหลักตามบทบาท (หรือ `/login` ถ้ายังไม่ได้ล็อกอิน) |
| **เหตุผล** | "ยังไม่ได้ล็อกอิน" กับ "ล็อกอินแล้วแต่ไม่มีสิทธิ์" เป็นคนละเรื่อง อย่างแรกแก้ได้ด้วยการล็อกอิน อย่างหลังแก้ไม่ได้ และเมื่อ Portfolio ไม่ได้ถูกเข้าผ่านลิงก์จาก DEEP Core อีกต่อไป โดเมนเปล่าๆ ก็ต้องตอบอะไรสักอย่าง |
| **frontend** | คือการแก้ฝั่ง frontend เอง — `guards/student-auth-guard.tsx`, `guards/teacher-auth-guard.tsx`, `routes/root-redirect.tsx` (ใหม่), `routes/router.tsx` |

### 6. interceptor ต่ออายุ session ให้ `GET /auth` ด้วย

| | |
| --- | --- |
| **ของเดิม** | `src/lib/axios.ts` จัด `/auth`, `/auth/login`, `/auth/ssoLogin`, `/auth/refresh` เป็น route ที่ห้าม refresh — `401` จาก `GET /auth` จึงไม่เคยพยายามต่ออายุ |
| **ของใหม่** | เหลือเฉพาะ `/auth/refresh` กับ `/auth/google` ที่ห้าม refresh |
| **เหตุผล** | ของเดิมไม่เป็นปัญหาเพราะทุกครั้งที่โหลดหน้า `GET /auth/login` ออก session ใหม่จาก SSO cookie ให้อยู่แล้ว เมื่อ session ถูกออกครั้งเดียวตอน login การ refresh จึงกลายเป็นสิ่งเดียวที่ทำให้ session อยู่ได้เกิน 15 นาที และ `GET /auth` คือ request แรกของทุกการโหลดหน้า |
| **frontend** | คือการแก้ฝั่ง frontend เอง |

### 7. ตัวแปรสภาพแวดล้อม

| | |
| --- | --- |
| **ของเดิม** | `DEEP_CORE_SECRET` — บังคับ |
| **ของใหม่** | `GOOGLE_CLIENT_ID` — บังคับ (ฝั่ง API) และ `VITE_GOOGLE_CLIENT_ID` — บังคับ (ฝั่ง web, ต้องเป็นค่าเดียวกัน) |
| **เหตุผล** | `GOOGLE_CLIENT_ID` **ไม่ใช่ความลับ** client id เป็นค่าสาธารณะโดยธรรมชาติ อยู่ใน bundle ของ web ด้วย แต่ยังบังคับ เพราะการตรวจ `audience` คือสิ่งที่กัน token ที่ Google ออกให้เว็บอื่น (token จริง ลายเซ็นถูกต้อง) ไม่ให้เอามาใช้ login ที่นี่ — ตรวจโดยไม่มี audience เท่ากับรับ Google ทุกคนของทุกแอป และ flow นี้ไม่มี client secret อยู่เลยสักที่ |
| **frontend** | ต้องตั้ง `VITE_GOOGLE_CLIENT_ID` ตอน build (Vite ฝังค่าตอน build ไม่ใช่ตอนรัน) `docker-compose.yml` ส่งค่าเดียวกันให้ทั้งสอง service แล้ว |

---

## #13 — test ของกลุ่ม endpoint ออกแบบการประเมิน

### 1. `POST /activity` และ `PUT /activity` ตรวจสิทธิ์ก่อนรับไฟล์

| | |
| --- | --- |
| **ของเดิม** | ลำดับ middleware เป็น `upload.array("files")` แล้วค่อย `requireRole("TEACHER")` — คำขอที่กำลังจะโดนปฏิเสธถูกอ่านไฟล์แนบเข้าหน่วยความจำจนครบก่อน แล้วจึงตอบ `401`/`403` |
| **ของใหม่** | `requireRole("TEACHER")` มาก่อน `upload.array("files")` สถานะและข้อความยังเป็นชุดเดิม ต่างกันตรงที่ไฟล์ไม่ถูกอ่านเลย — ผลข้างเคียงคือคำขอที่โดนปฏิเสธถูกตอบตั้งแต่ยังอัปโหลดไม่จบ เบราว์เซอร์ที่กำลังส่งไฟล์ก้อนใหญ่จึงอาจเห็น connection reset แทนที่จะเห็น `403` |
| **เหตุผล** | เหตุผลเดียวกับ `POST /course-material` ในข้อ 2 ของ #12 — multer ทำงานจนจบก่อน middleware ตัวถัดไปเสมอ การวางไว้หน้าสุดจึงให้ผู้ที่ไม่มีสิทธิ์ส่งไฟล์ขนาดเท่าไรก็ได้มาให้ server แบกไว้ก่อน แก้พร้อมกันทั้งสองที่ให้เหลือรูปแบบเดียวทั้ง codebase |
| **frontend** | ไม่ต้องแก้ — `features/teacher/activity/services/*` ยิงผ่าน `axiosInstance` ซึ่งตั้ง `withCredentials: true` อยู่แล้ว คำขอที่ถูกต้องยังผ่านเหมือนเดิม |

### 2. `POST /learning-activity` และ `PUT /learning-activity` ตรวจสิทธิ์ก่อนรับไฟล์

| | |
| --- | --- |
| **ของเดิม** | ลำดับเดียวกับข้อ 1 คือ `upload.array("files")` มาก่อน `requireRole("TEACHER")` |
| **ของใหม่** | สลับให้ `requireRole("TEACHER")` มาก่อน สถานะและข้อความยังเป็นชุดเดิม พร้อมผลข้างเคียงเรื่อง connection reset แบบเดียวกับข้อ 1 |
| **เหตุผล** | เหมือนข้อ 1 ทุกประการ กิจกรรมการเรียนรู้กับกิจกรรมเก็บคะแนนใช้โครง route ชุดเดียวกัน จึงมีจุดผิดชุดเดียวกัน |
| **frontend** | ไม่ต้องแก้ — เรียกจาก `features/teacher/learning-activity/services/*` ผ่าน `axiosInstance` ตัวเดียวกัน |

### 3. `GET /learning-activity/submitted/list` ต้องเป็นอาจารย์ที่ล็อกอินแล้ว

| | |
| --- | --- |
| **ของเดิม** | ไม่มี middleware ตัวไหนเลย ใครก็ตามที่ยิงถึง API ได้ ขอรายชื่อนักศึกษาทั้ง section พร้อมสถานะการส่งงานของแต่ละคนได้โดยรู้แค่ `learning_activity_id` |
| **ของใหม่** | `401` เมื่อไม่มี session, `403` เมื่อผู้ใช้ไม่มีบทบาท `TEACHER` |
| **เหตุผล** | ข้อมูลที่คืนมาคือรายชื่อชั้นเรียนพร้อมสถานะรายคน เป็นของอาจารย์ผู้สอน ไม่ใช่ข้อมูลสาธารณะ — และ endpoint คู่แฝดของกิจกรรมเก็บคะแนน (`GET /activity/submitted/list`) ตรวจ `requireRole("TEACHER")` อยู่แล้ว ตัวนี้จึงเป็นการตกหล่น ไม่ใช่การออกแบบ |
| **frontend** | ไม่ต้องแก้ — เรียกจากหน้าฝั่งอาจารย์เท่านั้น (`features/teacher/learning-activity/…`) ซึ่งอยู่หลัง guard ของบทบาทอยู่แล้ว |

### 4. `POST /mapping/learning-activity` ต้องเป็นอาจารย์ที่ล็อกอินแล้ว

| | |
| --- | --- |
| **ของเดิม** | ไม่มี middleware ตัวไหนเลย ใครก็ได้ผูกกิจกรรมการเรียนรู้เข้ากับ CLO ข้อไหนก็ได้ |
| **ของใหม่** | `401` เมื่อไม่มี session, `403` เมื่อผู้ใช้ไม่มีบทบาท `TEACHER` และไม่มีแถวใดถูกเขียนลงฐานข้อมูลทั้งสองกรณี |
| **เหตุผล** | เหตุผลเดียวกับข้อ 3 — อีกครึ่งหนึ่งของหน้าจอเดียวกัน (`POST /mapping/activity`) ตรวจสิทธิ์อยู่แล้ว การผูก CLO คือการกำหนดว่ารายวิชาวัดผลลัพธ์การเรียนรู้ข้อใด ซึ่งไหลต่อไปถึงรายงานระดับหลักสูตร |
| **frontend** | ไม่ต้องแก้ — `features/teacher/mapping/services/*` ใช้ `axiosInstance` ตัวเดียวกัน |

### 5. `GET /mapping/activity` คืน `level_no` เป็น `null` เมื่อกิจกรรมไม่มี rubric

| | |
| --- | --- |
| **ของเดิม** | หา rubric ของกิจกรรมด้วย `findFirst` แล้วส่ง `rubric?.id` เข้า `where` ของการหาจำนวนระดับต่อทันที เมื่อกิจกรรมไม่มี rubric ค่าที่ส่งไปเป็น `undefined` ซึ่ง Prisma แปลว่า "ไม่ต้องกรองคอลัมน์นี้" ไม่ใช่ "ไม่ตรงกับอะไรเลย" ผลคือได้ `level_no` สูงสุดของ **ทั้งตาราง** — เลขระดับของ rubric ของกิจกรรมอื่นที่ไม่เกี่ยวกัน |
| **ของใหม่** | ถามจำนวนระดับก็ต่อเมื่อมี rubric จริง ไม่มีก็คืน `level_no: null` |
| **เหตุผล** | ค่าที่คืนไม่ได้แค่ผิด แต่ผิดแบบดูสมเหตุสมผล — เป็นเลขที่มีอยู่จริงในระบบ จึงไม่มีทางสังเกตได้จากหน้าจอ และเปลี่ยนไปเรื่อยๆ ตามข้อมูลของ section อื่น |
| **frontend** | `features/teacher/mapping/components/activity-card.tsx` ตรวจ `if (!props.activity.level_no) return []` อยู่แล้ว ตอนรันจึงไม่ต้องแก้อะไร แต่ type เดิมประกาศไว้ `level_no?: number` ซึ่งรับ `null` ไม่ได้ แก้เป็น `number \| null` ใน `features/teacher/mapping/types/mapping-type.type.ts` ให้ตรงกับสิ่งที่ API คืนจริง |

---

## #14 — test ของกลุ่ม endpoint ส่งงานและทำงานกลุ่ม

### 1. `POST /student/submit/activity` และ `POST /student/submit/learning-activity` ตรวจสิทธิ์ก่อนรับไฟล์

| | |
| --- | --- |
| **ของเดิม** | ลำดับ middleware เป็น `upload.array("files")` แล้วค่อย `requireRole("STUDENT")` — จุดผิดเดียวกับข้อ 1–2 ของ #13 และข้อ 2 ของ #12 แต่อยู่คนละ router จึงตกหล่นมาถึงตอนนี้ |
| **ของใหม่** | `requireRole("STUDENT")` มาก่อน `upload.array("files")` สถานะและข้อความยังเป็นชุดเดิม (`401` / `403` พร้อมข้อความไทยชุดเดียวกัน) ต่างกันตรงที่ไฟล์ไม่ถูกอ่านเลย และมีผลข้างเคียงเรื่อง connection reset แบบเดียวกับ #13 |
| **เหตุผล** | สองตัวนี้เป็น endpoint เดียวในกลุ่มส่งงานที่รับไฟล์จริง และเป็น endpoint ที่ "ใครก็ได้" ยิงถึงง่ายที่สุด เพราะเป็นทางที่นักศึกษาใช้ทุกวัน การปล่อยให้ multer อ่านไฟล์ 10 MB จนครบก่อนถามว่าเป็นใครจึงเป็นภาระที่ไม่มีใครได้อะไรกลับมา แก้ให้เหลือรูปแบบเดียวทั้ง codebase ตามที่เริ่มไว้ใน #12 |
| **frontend** | ไม่ต้องแก้ — `features/student/course/services/course-service.service.ts` ยิงผ่าน `axiosInstance` ซึ่งตั้ง `withCredentials: true` อยู่แล้ว คำขอของนักศึกษาที่ล็อกอินอยู่ยังผ่านเหมือนเดิมทุกประการ |

### 2. ไม่เปิดการเชื่อมต่อ SMTP เมื่อยังไม่ได้ตั้งค่าบัญชีอีเมล

| | |
| --- | --- |
| **ของเดิม** | `GroupService.sendInviteEmail` เรียก `transporter.sendMail` ทุกครั้งที่มีสมาชิกถูกเชิญ แม้ `EMAIL_USER` จะว่างอยู่ ผลคือทุกครั้งที่สร้างหรือแก้ไขกลุ่ม server จะรอ SMTP handshake กับ Gmail จนหมดเวลาต่อสมาชิกหนึ่งคน แล้วค่อยไปเข้า `catch` |
| **ของใหม่** | ถ้าไม่มี `EMAIL_USER` ก็ `return` ตั้งแต่ต้น ไม่เปิดการเชื่อมต่อเลย |
| **เหตุผล** | `config/env.ts` ประกาศ `EMAIL_USER`/`EMAIL_PASS` เป็น optional อยู่แล้ว และผู้เรียกทั้งสองฝั่งก็ห่อการส่งอีเมลไว้ใน `try/catch` เพราะถือว่าส่งไม่สำเร็จก็ไม่เป็นไร การเปิดการเชื่อมต่อเพื่อไปรู้สิ่งที่รู้อยู่แล้วจึงไม่ได้อะไรกลับมานอกจากเวลาที่ผู้ใช้ต้องรอ |
| **ขอบเขต** | ข้อนี้ **ไม่ใช่** การแก้พฤติกรรมตาม D9 — สถานะ ข้อความ และข้อมูลในฐานข้อมูลเหมือนเดิมทุกตัวอักษร (สร้างกลุ่มสำเร็จ, สมาชิกยังเป็น `PENDING` พร้อม token) เปลี่ยนแค่เวลาที่ใช้ ที่แก้ใน #14 เพราะทุกเคสที่สร้างกลุ่มในชุด test ต้องรอ SMTP handshake หมดเวลาทีละสมาชิก ซึ่งทำให้ชุด test ของ ticket นี้ช้าจนใช้งานไม่ได้ บันทึกไว้ตรงนี้เพื่อให้เห็นว่าเป็นการแก้ที่เกิดขึ้นจริงในคอมมิตนี้ ไม่ใช่เพราะถือว่าเป็นการเปลี่ยนพฤติกรรม |
| **frontend** | ไม่ต้องแก้ — response เหมือนเดิมทุกตัวอักษร ต่างกันแค่ตอบเร็วขึ้น |

---

## #15 — test ของกลุ่ม endpoint ให้คะแนน

### 1. `GET /gradebook/per-student` นับสายจากวันที่อย่างเดียว ไม่ใช่จากสถานะการตรวจ

| | |
| --- | --- |
| **ของเดิม** | เงื่อนไขการนับเขียนไว้ว่า "สายก็ต่อเมื่อ `status === "SUBMITTED"` **และ** ส่งหลังกำหนด" งานชิ้นเดียวกันที่ส่งเวลาเดียวกันจึงถูกนับเป็น `late_submissions` ตอนที่ยังไม่ได้ตรวจ แล้วย้ายไปเป็น `on_time_submissions` ทันทีที่อาจารย์กดตรวจ (`GRADED`) — สถิติของทั้งห้องเปลี่ยนไปตามลำดับที่อาจารย์เปิดงาน |
| **ของใหม่** | แยกเป็นสามทางตามความหมายจริง: ไม่ส่ง (`NOT_SUBMITTED`) นับเป็นขาด, ส่งหลังกำหนดนับเป็นสาย, นอกนั้นนับเป็นตรงเวลา — งานที่ไม่มีกำหนดส่งจึงตรงเวลาเสมอ ตรงกับของเดิม |
| **เหตุผล** | การส่งสายหรือไม่เป็นข้อเท็จจริงเรื่องเวลาที่ส่ง การตรวจไม่ควรย้อนไปเปลี่ยนมัน ของเดิมยังใส่ `sa.submitted_at!` ไว้ด้วย ทั้งที่คอลัมน์เป็น nullable — เป็นการยืนยันกับ TypeScript ในสิ่งที่ schema ไม่ได้รับประกัน ของใหม่ตรวจค่าจริงแทน |
| **frontend** | ไม่ต้องแก้ — `features/teacher/gradebook/pages/teacher-gradebook-page.tsx` แค่ส่งตัวเลขทั้งสามต่อเข้าตาราง ชนิดข้อมูลและรูปร่าง response เหมือนเดิมทุกอย่าง ต่างกันแค่ตัวเลขที่ถูกต้องขึ้น |

### 2. คิดคะแนนเป็นจำนวนเต็มหน่วยสตางค์ และ `GET /gradebook/per-activity` ปัดเศษ `mean_score`

| | |
| --- | --- |
| **ของเดิม** | `/per-student` บวก `total_score` เป็น double แล้วปัดทีหลังด้วย `Math.round(v * 100) / 100` ส่วน `/per-activity` ส่งค่าเฉลี่ยดิบออกไปเลย คะแนน 1, 2, 2 จึงได้ `mean_score` เป็น `1.6666666666666667` ทั้งก้อน — คอลัมน์คะแนนชุดเดียวกัน ตอบกลับมาสองตำแหน่งใน endpoint หนึ่ง และสิบเจ็ดตำแหน่งในอีก endpoint หนึ่ง |
| **ของใหม่** | ทั้งสองที่คิดเลขในหน่วยหนึ่งในร้อย (`toHundredths`) ซึ่งคะแนนทุกตัวเป็นจำนวนเต็มและการบวกไม่มีความคลาดเคลื่อนเลย ผลรวมจึงตรงตัวโดยไม่ต้องปัด ส่วนค่าเฉลี่ยซึ่งมีการหารเข้ามาก็ปัดจากจำนวนเต็ม |
| **เหตุผล** | คะแนนเก็บเป็น `Decimal(5,2)` — จำนวนเต็มหน่วยหนึ่งในร้อย — แต่กลายเป็น double ตอนบวกและหาร และ double เก็บ `10.01` ไม่ได้ตรงๆ **ที่สำคัญกว่าคือเรื่องกรณีก้ำกึ่ง**: ผลรวมของค่าทศนิยมสองตำแหน่งเป็นทศนิยมสองตำแหน่งเสมอ จึงไม่มีทางก้ำกึ่ง แต่**ค่าเฉลี่ยมีการหาร** จึงเกิดก้ำกึ่งได้ — คะแนน 1.00 กับ 1.01 เฉลี่ยได้ 1.005 พอดี และการปัดค่านั้นในรูป double ตอบ `1.00` เพราะ double ที่ใกล้ 1.005 ที่สุดอยู่ต่ำกว่า 1.005 นิดเดียว การคิดในหน่วยหนึ่งในร้อยทำให้จุดกึ่งกลางเป็นค่าที่แทนได้ตรงตัว จึงปัดขึ้นเหมือนจุดกึ่งกลางอื่นๆ ค่านี้ไหลต่อไปถึงนักศึกษาด้วย เพราะ `GET /evaluation/list` สร้างต่อจาก endpoint นี้ |
| **frontend** | ไม่ต้องแก้ — ทั้ง `features/teacher/gradebook` และ `features/student/course/components/evaluation/evaluation-column.tsx` render ค่านี้ตรงๆ (`{text ? text : "-"}`) ไม่ได้จัดรูปแบบเอง ที่จริงคือ frontend เป็นเหตุผลว่าทำไมต้องแก้ที่ API — เลขสิบเจ็ดตำแหน่งเคยขึ้นบนหน้าจอนักศึกษาตามที่ส่งไป |

### 3. `GET /evaluation/list` คืน `score` เป็นตัวเลข ไม่ใช่สตริง

| | |
| --- | --- |
| **ของเดิม** | ส่ง `studentActivity.score` ที่ได้จาก Prisma ต่อเข้า `res.json` ตรงๆ ค่า `Decimal` แปลงเป็น JSON เป็น**สตริง** นักศึกษาที่ได้ 12 คะแนนจึงได้ `"12"` ทั้งที่ response model ฝั่ง API และ type ฝั่งเว็บประกาศตรงกันว่าเป็น `number \| null` |
| **ของใหม่** | แปลงด้วย `Number(...)` ก่อนใส่ลง response โดยยังคง `null` ไว้เมื่อยังไม่มีคะแนน เหมือนที่ `gradebook.service` ทำกับทุกคะแนนอยู่แล้ว |
| **เหตุผล** | เป็นที่เดียวในกลุ่มนี้ที่ปล่อย `Decimal` ออกไปถึงผู้เรียก type ทั้งสองฝั่งเขียนตรงกันว่าเป็นตัวเลข มีแต่ข้อมูลจริงบนสายที่ไม่ตรง — ความไม่ตรงแบบนี้ยังไม่พังตอนแสดงผล แต่จะพังทันทีที่มีใครเอาไปเทียบหรือคำนวณต่อ |
| **frontend** | ไม่ต้องแก้ — `evaluation-column.tsx` render ค่าตรงๆ สตริง `"12"` กับเลข `12` จึงขึ้นหน้าจอเหมือนกัน และ `features/student/course/types/course-type.ts` ประกาศ `score: number \| null` ไว้อยู่แล้ว การแก้ครั้งนี้ทำให้ข้อมูลจริงตรงกับสิ่งที่ประกาศไว้ ไม่ได้เปลี่ยนสิ่งที่ประกาศ |

### 4. `GET /gradebook/per-student` และ `/per-activity` ต้องเป็นอาจารย์ที่ล็อกอินแล้ว

| | |
| --- | --- |
| **ของเดิม** | สอง route นี้ไม่มี middleware ตรวจสิทธิ์เลย ไม่ตรวจ session ไม่ตรวจบทบาท ใครก็ได้ที่รู้ `section_id` (เป็นจำนวนเต็ม เดาได้) จึงดึงรายชื่อนักศึกษาทั้ง section พร้อมคะแนนรายกิจกรรมและคะแนนรวมออกมาได้โดยไม่ต้องล็อกอิน |
| **ของใหม่** | ทั้งสอง route ผ่าน `requireRole("TEACHER")` ก่อน — `401` เมื่อไม่มี session, `403` เมื่อผู้เรียกไม่ใช่อาจารย์ ใช้ข้อความไทยชุดเดียวกับที่อื่นทั้งหมด |
| **เหตุผล** | `GET /evaluation/list` ซึ่งแสดง**ข้อมูลชุดเดียวกัน** ให้นักศึกษาดู ตรวจ `requireRole("STUDENT")` และอ่าน `student_id` จาก session อยู่แล้ว ด้านที่นักศึกษาเห็นถูกล็อก ส่วนด้านที่อาจารย์เห็นเปิดโล่ง ความไม่ตรงกันนี้ไม่ใช่การตัดสินใจเชิงนโยบาย แต่เป็นการตกหล่น การใส่ `requireRole("TEACHER")` เป็นรูปแบบเดียวกับทุก route ของอาจารย์ในระบบ |
| **ขอบเขต** | ปิดแค่ "ใครก็ได้" เท่านั้น คำถามว่า**อาจารย์คนไหน**เห็น section ไหนได้ ยังไม่ได้ตอบ — อาจารย์ที่ล็อกอินอยู่ยังเปิดสมุดคะแนนของ section ที่ตัวเองไม่ได้สอนได้ เป็นคำถามความเป็นเจ้าของ section เดียวกับที่ค้างมาตั้งแต่ #12 และ #26 ค้างไว้ที่ #30 |
| **frontend** | ไม่ต้องแก้ — `features/teacher/gradebook/services` ยิงผ่าน `axiosInstance` ซึ่งตั้ง `withCredentials: true` อยู่แล้ว และหน้านี้อยู่ในส่วนของอาจารย์ที่ต้องล็อกอินอยู่แล้ว อาจารย์ที่ใช้งานตามปกติจึงไม่รู้สึกอะไร |

---

## #16 — test ของกลุ่ม endpoint e-Portfolio (ส่วนแรก)

### 1. `GET /portfolio-personal/:user_id` ใช้อีเมลและเบอร์โทรจากบัญชีได้จริง

| | |
| --- | --- |
| **ของเดิม** | service คำนวณค่าสำรองจาก `users.email` / `users.phone` ไว้ในตัวแปร แล้ว**ทิ้ง** — บรรทัดสุดท้าย spread ตัวแปรเดิมก่อนคำนวณกลับไป นักศึกษาที่มีแถว `portfolio_personal` แต่ยังไม่เคยกรอกสองช่องนี้ จึงได้ `null` ทั้งที่ระบบมีข้อมูลอยู่แล้ว ส่วนนักศึกษาที่ยังไม่มีแถวเลย ได้ค่าสำรองถูกต้อง เพราะเดินคนละสาขา นอกจากนี้ response ยังพ่วง object `users` ที่ join มาทั้งก้อนออกไปด้วย |
| **ของใหม่** | ค่าสำรองถูกใช้จริง — `email` และ `phone_number` ที่เป็น `null` ตกไปที่ค่าจากบัญชี เหมือนสาขา "ยังไม่มีแถว" ที่ตอบแบบนี้อยู่แล้ว และ object `users` ไม่ถูกส่งออกไปอีก |
| **เหตุผล** | เป็น bug ตรงๆ ไม่ใช่การตัดสินใจ — โค้ดที่เขียนไว้บอกเจตนาชัดว่าต้องการค่าสำรอง แค่ต่อสายผิด ผลคือหน้าแก้ไขข้อมูลส่วนตัวขึ้นช่องว่างให้นักศึกษากรอกอีเมลที่ตัวเองใช้สมัครซ้ำอีกรอบ ส่วน `users` ที่พ่วงมาไม่เคยถูกประกาศไว้ใน `PortfolioPersonalResp` และไม่มีใครอ่าน — เป็นร่องรอยของการ join เพื่อหาค่าสำรอง ไม่ใช่สิ่งที่ผู้เรียกขอ |
| **frontend** | ไม่ต้องแก้ — `src/types/portfolio-personal-type.type.ts` ไม่มีฟิลด์ `users` และไม่มีที่ไหนใต้ `features/student/portfolio` อ่าน `.users` ส่วนสองช่องที่เคยว่าง ตอนนี้มาพร้อมค่า ซึ่งเป็นสิ่งที่หน้าเว็บคาดหวังอยู่แล้ว |

### 2. `GET /portfolio-education` ปฏิเสธคำขอที่ไม่บอกว่าเป็นของใคร

| | |
| --- | --- |
| **ของเดิม** | ไม่ส่ง `user_id` มาก็ได้ `200` พร้อมประวัติการศึกษาของ**นักศึกษาทุกคน**ในระบบ เพราะ `where: { user_id: undefined }` ใน Prisma ไม่ใช่การกรองด้วยค่าว่าง แต่แปลว่าไม่ต้องกรองเลย |
| **ของใหม่** | `400` พร้อม `{ "success": false, "message": "user_id is required" }` |
| **เหตุผล** | เป็นรูปแบบเดียวกับที่บันทึกไว้แล้วใน #13 และ #14 ต่างกันที่นี่ตรงที่ **endpoint พี่น้องของมันตรวจอยู่แล้ว** — `GET /portfolio-training` และ `GET /portfolio-certificate` ในกลุ่มเดียวกัน โครงเดียวกัน มีการตรวจ `user_id` ครบทั้งคู่ ขาดแค่ education ตัวเดียว จึงเป็นการตกหล่นชัดเจน ไม่ใช่การออกแบบ และการแก้ก็แค่คัดลอกการตรวจจากพี่น้องมาใส่ ไม่ต้องคิดรูปแบบใหม่ |
| **ขอบเขต** | ปิดแค่ "คำขอที่ไม่บอกว่าเป็นของใคร" เท่านั้น คำขอที่ระบุ `user_id` ของนักศึกษาคนอื่นยังผ่านตามเดิม เพราะกลุ่มนี้ทั้งกลุ่มยังไม่มีการตรวจสิทธิ์ — ค้างไว้ที่ #31 |
| **frontend** | ไม่ต้องแก้ — `src/services/portfolio-education.service.ts` ส่ง `params: { user_id }` ทุกครั้งอยู่แล้ว ไม่มีที่ไหนเรียกแบบไม่ระบุ |

### 3. `GET /portfolio-personal/:user_id` ตอบ `404` เมื่อไม่รู้จักผู้ใช้คนนั้น

| | |
| --- | --- |
| **ของเดิม** | `200` พร้อม `data: null` — บล็อก 404 ถูก comment ทิ้งไว้ใน controller endpoint นี้จึงไม่มีคำขอรูปแบบใดที่มันปฏิเสธเลย |
| **ของใหม่** | `404` พร้อม `{ "success": false, "message": "Portfolio personal not found" }` เมื่อ `user_id` ไม่ตรงกับผู้ใช้คนใด |
| **เหตุผล** | service คืน `null` เฉพาะกรณี "ไม่มีผู้ใช้คนนี้" เท่านั้น ส่วนผู้ใช้ที่มีตัวตนแต่ยังไม่เคยกรอกข้อมูล จะได้ค่าตั้งต้นจากบัญชีตัวเองกลับไปเป็น `200` ตามเดิม สองกรณีนี้จึงต่างกันจริง และ `200` ที่ห่อ `null` ทำให้ frontend แยกไม่ออก โค้ดที่ comment ทิ้งไว้บอกเจตนาเดิมอยู่แล้วว่าต้องการ `404` — เป็นโค้ดตายที่ปิดพฤติกรรมที่ตั้งใจไว้ ตรงกับ D9 ที่ว่า "แก้ทันทีที่เจอ ไม่ล็อกพฤติกรรมที่ผิดไว้" |
| **frontend** | ไม่ต้องแก้ — ทั้งสองจุดที่เรียก (`features/student/portfolio/hooks/use-portfolio.ts` และ `pages/personal-details/student-personal-details-page.tsx`) ยิงคู่กับ `getUser(studentId)` ใน `Promise.all` เดียวกัน โดยใช้ `user_id` ของคนที่ล็อกอินอยู่เสมอ ผู้ใช้ที่ไม่มีตัวตนจึงเกิดไม่ได้ในทางปฏิบัติ และถ้าเกิดจริง `getUser` ก็ล้มก่อนอยู่ดี ทั้งคู่มี `catch` ครอบอยู่แล้ว |

### 4. `gpa` ออกจาก API เป็นตัวเลข ไม่ใช่สตริง

| | |
| --- | --- |
| **ของเดิม** | `portfolio_education.gpa` เป็น `Decimal(3,2)` และ `Prisma.Decimal` ที่ผ่าน `res.json` กลายเป็นสตริง `"3.45"` ทั้งที่ type ที่ frontend ถือไว้ (`src/types/portfolio-education-type.type.ts`) เขียนว่า `number \| null` |
| **ของใหม่** | ทุก endpoint ที่ส่ง `gpa` ออกไปคืนเป็น `number` หรือ `null` — ทั้ง list, detail, create, update, delete และผ่านไปถึง `GET /portfolio/:id` กับ `/portfolio/public/:token` ที่ประกอบข้อมูลจาก service ตัวเดียวกัน `PortfolioEducationResp.gpa` เปลี่ยนจาก `Decimal` เป็น `number` ตามไปด้วย |
| **เหตุผล** | รูปแบบเดียวกับ `score` ของ `/evaluation/list` ที่ข้อ 3 ของ #15 แปลงไปแล้ว และใช้วิธีเดียวกัน (`Number(...)` ที่ชั้น service) ไม่ประดิษฐ์วิธีที่สอง เหตุผลที่ไม่ปล่อยไว้ทั้งที่ยังไม่มีอาการที่ผู้ใช้เห็น คือ D9 บอกว่า "ไม่ล็อกพฤติกรรมที่ผิดไว้" — และ type ทั้งสองฝั่งขัดกับสิ่งที่อยู่บน wire อยู่แล้ว TypeScript จึงไม่มีวันเตือนใครที่เขียน `gpa > 3` ในอนาคต |
| **ผลข้างเคียง** | เกรด `3.50` เดิมมาเป็น `"3.50"` ตอนนี้มาเป็น `3.5` — เลขศูนย์ท้ายเป็นรายละเอียดของคอลัมน์ ไม่ใช่คำสั่งจัดรูปแบบ ตรวจแล้วว่าไม่มีที่ไหนในหน้าเว็บพิมพ์ `gpa` ออกจอโดยตรง |
| **frontend** | ไม่ต้องแก้ — `gpa` ถูกอ่านแค่สองที่ ทั้งคู่เป็นการเติมค่าลงฟอร์ม `<Input type="number" step="0.01">` ใน `education-edit-modal.tsx` ซึ่งรับได้ทั้งสตริงและตัวเลข ส่วนขาส่งกลับแปลงด้วย `Number(values.gpa)` อยู่แล้ว การเปลี่ยนนี้ทำให้ type ที่ frontend ประกาศไว้ตรงกับของจริงเป็นครั้งแรก |

---

## #17 — test ของกลุ่ม endpoint e-Portfolio (ส่วนที่สอง)

### 1. `POST /portfolio-skill/assign-work` ตอบ `403` เมื่อผูกทักษะของคนอื่น

| | |
| --- | --- |
| **ของเดิม** | `500` พร้อมข้อความ `One or more skills do not belong to this user` — service โยน `Error` เปล่าๆ ที่ไม่มี `status` ติดมา `errorHandler` จึงตกไปที่ `500` ตามค่าตั้งต้น |
| **ของใหม่** | `403` พร้อมข้อความเดิม โดยติด `status = 403` ไปกับ error ก่อนโยน |
| **เหตุผล** | นี่คือ**การปฏิเสธ** ไม่ใช่ความผิดพลาดของเซิร์ฟเวอร์ — เป็นการตรวจความเป็นเจ้าของที่เขียนไว้อย่างตั้งใจ และเป็นที่เดียวในกลุ่ม `portfolio` ทั้งหมดที่มีการตรวจแบบนี้ `500` บอกผู้เรียกว่าระบบพัง ทั้งที่ระบบทำงานถูกต้องและกำลังบอกว่า "ไม่อนุญาต" วิธีติด `status` ใช้รูปแบบเดิมที่มีอยู่แล้วใน `portfolio.service.ts` (ลิงก์แชร์หมดอายุ → `410`) ไม่ประดิษฐ์วิธีที่สอง |
| **ขอบเขต** | เปลี่ยนแค่สถานะของการปฏิเสธที่มีอยู่แล้ว ไม่ได้เพิ่มการตรวจสิทธิ์ใหม่ — endpoint นี้ยังเชื่อ `user_id` ที่มากับ body อยู่เหมือนเดิม ใครก็ยิงแทนคนอื่นได้ตราบใดที่ทักษะที่อ้างถึงเป็นของคนนั้นจริง เรื่องนั้นค้างอยู่ที่ #31 |
| **frontend** | ไม่ต้องแก้ — ทั้งสองจุดที่เรียก (`create-work-form.tsx` และ `work-edit-modal.tsx`) ใช้ `try`/`catch` ก้อนเดียวแล้วขึ้นข้อความ `เกิดข้อผิดพลาดในการบันทึก` ไม่มีที่ไหนแยกตามสถานะ |

---

## #18 — test ของกลุ่ม endpoint identity (`user` และ `student`)

### 1. `GET /student/calendar` ไม่ส่งค่า `false` ปนมาในรายการอีกต่อไป

| | |
| --- | --- |
| **ของเดิม** | ทุกครั้งที่มีงานที่ยังไม่ถึงวันประกาศ `activities` และ `learning_activities` ใน response จะมีสมาชิกที่เป็นค่า `false` โดด ๆ ปนอยู่ ตามจำนวนงานที่ยังไม่ประกาศ เช่น `[{...}, false]` |
| **ของใหม่** | งานที่ยังไม่ถึงวันประกาศถูกคัดออกจริง เหลือแต่ object ของงานที่ประกาศแล้ว |
| **เหตุผล** | service เขียนเป็น `map` ที่คืน `checkIsOverAnnouncementDate(...) && {...}` ซึ่ง `map` ไม่ได้ทิ้งอะไรทั้งนั้น — เมื่อเงื่อนไขเป็นเท็จ ค่าที่ได้คือ `false` แล้วถูกใส่กลับเข้า array ตามเดิม สิ่งเดียวที่ทำให้ผ่าน typecheck คือ `as` cast ที่ปิดปากคอมไพเลอร์ไว้ แก้เป็น `filter` แล้วค่อย `map` และลบ cast ทิ้ง |
| **ขอบเขต** | พร้อมกันนี้ type `CalendarClassworkEvent` ถูกแก้ให้ตรงกับสิ่งที่ service สร้างจริง ซึ่ง cast เดิมบิดเบือนไว้สามช่อง: `type` เป็น VarChar ไม่มี enum รองรับ, `course` มาจาก `find` ที่พลาดได้จึงเป็น `undefined` ได้, และ `status` เป็นค่าจากคอลัมน์ซึ่งรวม `GRADING` ที่ `ClassworkStatus` ไม่มี ส่วน `ClassworkStatus` เองไม่ได้แตะ เพราะเป็นคำศัพท์ของหน้ารายการงานที่มี `LATE` ซึ่งคำนวณขึ้นเอง ไม่ได้มาจากฐานข้อมูล |
| **frontend** | ไม่ต้องแก้ — `calendar.tsx` กรองด้วย `event.deadline_date` ก่อนใช้ทุกครั้ง ซึ่งบนค่า `false` เป็น `undefined` สมาชิกที่ผิดจึงหลุดออกไปพอดีโดยบังเอิญ ผลที่ผู้ใช้เห็นบนหน้าปฏิทินจึงเหมือนเดิม แต่ response ที่ผู้เรียกอื่นได้รับสะอาดขึ้น |

---

## #19 — test ของตรรกะบริสุทธิ์ฝั่ง frontend (`utils/` และ reducer ของ slice)

ticket นี้ไม่ได้แตะ API เลย ทั้งสองข้อข้างล่างจึงเป็นการเปลี่ยนภายในฝั่ง web
ผู้เรียก endpoint ไม่เห็นความต่าง แต่จดไว้เพราะเป็นการแก้โค้ดที่รันจริง
ไม่ใช่แค่การเพิ่ม test

### 1. `fetchStudentDetail` ไม่พิมพ์รหัสนักศึกษาลง console อีกต่อไป

| | |
| --- | --- |
| **ของเดิม** | `home-slice.ts` มี `console.log("student id : ", ...)` ค้างอยู่ใน `fetchStudentDetail.fulfilled` ทุกครั้งที่หน้าแรกของนักศึกษาโหลด รหัสนักศึกษาจะถูกพิมพ์ลง console ของเบราว์เซอร์ |
| **ของใหม่** | ลบบรรทัดนั้นทิ้ง ส่วนที่เขียน `state.studentId` ยังอยู่เหมือนเดิม |
| **เหตุผล** | เศษของการ debug ที่หลุดมากับการส่งมอบ ไม่ได้ทำอะไรนอกจากพิมพ์ข้อมูลผู้ใช้ออกทางที่ใครเปิด devtools ก็อ่านได้ |
| **frontend** | ไม่ต้องแก้ — ไม่มีใครอ่านค่าที่พิมพ์ออกมา `state.studentId` ซึ่งเป็นสิ่งที่หน้าเว็บใช้จริงยังเหมือนเดิมทุกประการ |

### 2. `use-portfolio.ts` เรียก `cleanNullStr` ตัวกลางแทนสำเนาที่ประกาศไว้ในตัวเอง

| | |
| --- | --- |
| **ของเดิม** | `src/utils/clean-null-str.ts` มีอยู่แต่ไม่มีใครเรียกเลย — `use-portfolio.ts` ประกาศฟังก์ชันชื่อเดียวกันไว้ข้างในอีกชุด เนื้อในเหมือนกันทุกไบต์ |
| **ของใหม่** | ลบสำเนาข้างในทิ้ง แล้ว `import { cleanNullStr }` จาก `utils/` เหมือนที่ไฟล์เดียวกันนี้ import `getFile` อยู่แล้ว |
| **เหตุผล** | ทั้งสองชุดเหมือนกันบรรทัดต่อบรรทัด พฤติกรรมจึงไม่เปลี่ยน แต่ก่อนแก้ ตัวที่ test ครอบไว้กับตัวที่รันจริงเป็นคนละตัวกัน — test ที่ไม่ได้ทดสอบโค้ดที่รันจริงไม่มีค่าอะไร |
| **frontend** | คือฝั่ง frontend เอง หน้า e-Portfolio แสดงผลเหมือนเดิม |

### 3. `user_id` ตั้งต้นของ `teacher-home-slice` ไม่ใช่รหัสของอาจารย์คนอื่นอีกต่อไป

| | |
| --- | --- |
| **ของเดิม** | `initialState.user_id` เป็นค่าคงที่ `"2771a2eb"` — รหัสของอาจารย์จริงคนหนึ่งที่ค้างมาจากเครื่อง dev ตอนส่งมอบ และติดไปกับ bundle ที่ส่งให้ทุกคน |
| **ของใหม่** | เริ่มเป็นสตริงว่าง แล้วถูกเขียนทับด้วย `teacher_id` ที่มากับ response ของ `fetchAllCourse` เหมือนเดิม |
| **เหตุผล** | ไล่ดูแล้วมีที่อ่าน field นี้อยู่ที่เดียวคือ `teacher-home-page.tsx` ซึ่งส่งต่อเป็น query `teacher_id` และฝั่ง API `getAllCourse` อ่านรหัสอาจารย์จาก session ไม่ใช่จาก query (บรรทัดที่อ่านจาก query ถูก comment ทิ้งไว้) ค่านี้จึงไม่เคยมีผลต่อคำตอบที่ได้ — เป็นแค่รหัสของคนอื่นที่ค้างอยู่ในโค้ดที่เปิดสาธารณะ |
| **frontend** | ไม่ต้องแก้ — คำขอแรกยังส่ง `teacher_id` ไปเหมือนเดิม เพียงแต่เป็นค่าว่าง และ server ก็ไม่ได้อ่านอยู่แล้ว |

### สิ่งที่พบแล้ว **pin ไว้ตามที่เป็น** (ไม่ได้แก้ใน ticket นี้)

- ~~**`isProduction` ใน `get-file.ts` เป็นเท็จเสมอ**~~ — เงื่อนไขอ่าน
  `import.meta.env.NODE_ENV` ซึ่ง Vite ไม่เคยกำหนดให้ (Vite ให้ `MODE` / `DEV` /
  `PROD`) สาขาที่ตั้งใจจะเสิร์ฟไฟล์จาก origin เดียวกันบน production จึงเป็นโค้ดตาย
  ทุก URL ถูกประกอบจาก `VITE_BACKEND_URL` เสมอ **ลบสาขาที่ตายแล้วทิ้งใน #36** ทุก
  URL ยังประกอบจาก `VITE_BACKEND_URL` เหมือนเดิม คำถามเรื่อง origin บน production
  ยังเปิดอยู่ ดูหัวข้อ #36
- ~~**`getFile` ต่อ path เข้ากับ query โดยไม่ escape**~~ — ชื่อไฟล์ที่มี `&` หรือ `#`
  จะทำให้ URL ขาดกลางคัน ทางแก้ตรง ๆ คือ `encodeURIComponent` แต่จะทำให้ `/`
  กลายเป็น `%2F` ด้วย ซึ่งเปลี่ยนสิ่งที่ endpoint `GET /files` ได้รับจริง
  ต้องแก้พร้อมกันทั้งสองฝั่ง ไม่ใช่ฝั่ง frontend ฝ่ายเดียว — **ปิดใน #36** ซึ่งแก้
  ทั้งสองฝั่งพร้อมกันพอดี: query ถูกประกอบด้วย `URLSearchParams` ฝั่ง API และ
  frontend ไม่ได้ประกอบ query เองอีกแล้ว
- **ไฟล์แนบที่ไม่มี `originFileObj` ถูกส่งเป็นสตริง `"undefined"`** —
  `appendAttachments` ใส่ `String(undefined)` ลงใน `FormData` แทนที่จะข้ามไป
  backend จึงได้ field `files` ที่มีค่าเป็นข้อความ ยังไม่แก้เพราะต้องตกลงก่อนว่า
  ควรข้าม ควรฟ้อง หรือควรกันตั้งแต่หน้าฟอร์ม
- **dialog ของ activity และ learning activity เขียนทับ field เดียวกัน** —
  `studentGroup`, `studentGroupInSec` และ `studentList` ใน `course-slice` ถูกใช้
  ร่วมกันโดยงานทั้งสองชนิด เปิดสองอันไล่กันแล้วค่าของอันแรกจะถูกแทนที่ ไม่ได้แยก
  เพราะการแยก field ต้องแก้หน้าที่อ่านมันด้วย ซึ่งเกินขอบเขตของ ticket ที่เขียน test
- **flag ที่ประกาศไว้แต่ไม่มีใครตั้งค่า** — `postURLLoading` / `postFileLoading`
  (announcement), `fetchAllStudentInSectionLoading` (gradebook — คัดลอกมาจาก slice
  ของ teacher-student), `editCourseMaterialLoading` /
  `fetchCourseMaterialOptionsLoading` (material) และ `isShowInstructionSubMenu` /
  `isShowAssessmentSubMenu` / `selectedSubMenu` (teacher-home) ทั้งหมดค้างเป็นค่า
  ตั้งต้นตลอดอายุการใช้งาน test ระบุไว้เป็นข้อ ๆ เพื่อให้ตอนลบทิ้งรู้ว่าลบอะไรได้บ้าง

---

## #20 — ตรวจ input ทุก endpoint

ticket นี้แตะ router ทุกตัวในระบบ การจดทีละ endpoint จะได้ตารางยาวนับร้อยแถว
ที่ไม่มีใครอ่านจนจบ จึงจัดกลุ่มตาม **ชนิดของการเปลี่ยน** แล้วบอกไว้ในแต่ละข้อว่า
กระทบ endpoint ไหนบ้าง

กฎเดียวที่ใช้ทั้ง ticket: คำขอที่ข้อมูลไม่ครบหรือผิดชนิดถูกปฏิเสธที่ขอบ HTTP ด้วย
`400` **ก่อน** ถึง controller ไม่ใช่ปล่อยให้ไปตายที่ Postgres แล้วโผล่ออกมาเป็น
`500` และข้อความที่ผู้ใช้เห็นเป็นภาษาไทยเสมอ เพราะ frontend เอา `message`
ไปแสดงตรงๆ (ตรวจแล้วมี 13 จุดที่เขียนว่า `error.response?.data?.message || "..."`)

### 1. error ของทุก endpoint อยู่ในซองเดียวกัน

| | |
| --- | --- |
| **ของเดิม** | มีสามหน้าตาปนกัน — `{ "message": ... }` เปล่าๆ จาก auth middleware, `{ "success": false, "message": ... }` จาก `errorHandler`, และ `{ "success": false, "message": ..., "stack": ... }` เมื่อ `NODE_ENV` เป็น `development` ผู้เรียกจึงอ่าน response ไม่ได้ถ้าไม่รู้ก่อนว่าชั้นไหนเป็นคนปฏิเสธ |
| **ของใหม่** | ทุกการปฏิเสธเป็น `{ "success": false, "message": "<ข้อความไทย>" }` และเพิ่ม `errors` เข้ามาเฉพาะเมื่อเป็นการปฏิเสธรายฟิลด์: `[{ "field": "section_id", "location": "query", "message": "ต้องระบุ" }]` ส่วน `message` ของกรณีนั้นคือประโยคสรุปที่รวมทุกฟิลด์ไว้แล้ว เช่น `ข้อมูลที่ส่งมาไม่ถูกต้อง: section_id ต้องระบุ, semester ต้องเป็นตัวเลข` |
| **เหตุผล** | `message` เป็นสิ่งเดียวที่ frontend อ่าน จึงต้องอ่านได้โดยไม่ต้องเดา และต้องสมบูรณ์ในตัวเอง — `errors` มีไว้ให้ผู้เรียกที่อยากไฮไลต์ช่องที่ผิดในฟอร์ม ไม่ใช่ข้อมูลที่ต้องอ่านถึงจะรู้เรื่อง `location` ต้องมีเพราะชื่อฟิลด์ซ้ำกันได้ระหว่าง query กับ body (`user_id` ซ้ำอยู่หลาย endpoint) |
| **frontend** | ไม่ต้องแก้ — ทุกจุดอ่าน `message` อย่างเดียว ไม่มีที่ไหนอ่าน `errors` หรือ `stack` และไม่มีที่ไหน branch ตามหน้าตาของ body (ที่ branch ตามสถานะมีสองแห่ง: `410` ของลิงก์แชร์หมดอายุใน `public-portfolio-page.tsx` และ `401` ใน `lib/axios.ts` ทั้งคู่ยังเป็นสถานะเดิม) |

### 2. คำขอที่ไม่ครบ ตอบ `400` แทน `500` หรือ `200`

| | |
| --- | --- |
| **ของเดิม** | ขึ้นกับว่า endpoint นั้นบังเอิญใช้ query แบบไหน — `findUnique` ที่ได้ `NaN`/`undefined` ตอบ `500`, `findMany` ที่ได้ `undefined` ใน `where` แปลว่า "ไม่ต้องกรอง" จึงตอบ `200` พร้อม **ข้อมูลของทุกคน** ส่วน `create` ที่ขาดคอลัมน์ NOT NULL ตอบ `500` จาก Postgres |
| **ของใหม่** | ทั้งสามแบบเป็น `400` พร้อมชื่อฟิลด์ที่ขาด กระทบ `/course/*`, `/announcement`, `/score-weight`, `/lesson-plan`, `/course-material`, `/rubric/shared-rubric`, `/activity/*`, `/learning-activity/*`, `/mapping/*`, `/student-activity*`, `/student-learning-activity*`, `/gradebook/*`, `/evaluation/list`, `/user`, `/student/*`, `/group/*`, `/files`, และทั้ง 14 router ของกลุ่ม `portfolio` |
| **เหตุผล** | เป็นเรื่องที่ค้างอยู่ในไฟล์นี้มาตั้งแต่ #12 ในชื่อ "รอ #20" — ผู้เรียกที่ลืมส่ง parameter ได้คำตอบคนละแบบขึ้นกับ endpoint และแบบที่อันตรายที่สุดคือ `200` ที่คืนข้อมูลของทุกคน ซึ่งไม่ใช่แค่ผิด แต่เป็นการรั่วของข้อมูล (`GET /portfolio-education`, `GET /student/enrolled/subjects`, `GET /rubric/shared-rubric`, `GET /student-activity-group/all`) |
| **frontend** | ไม่ต้องแก้ — คำขอที่ frontend ยิงจริงส่ง parameter ครบทุกครั้ง สิ่งที่เปลี่ยนคือคำตอบของคำขอที่เขียนผิด ซึ่งไม่เคยมีใครตั้งใจยิง |

### 3. `errorHandler` ไม่ส่ง stack trace และไม่ส่งข้อความที่ไม่ได้ตั้งใจให้ผู้ใช้เห็น

| | |
| --- | --- |
| **ของเดิม** | แนบ `stack` ไปกับทุก response เมื่อ `NODE_ENV === "development"` และส่ง `err.message` ของ error ทุกชนิดออกไปเสมอ — รวมถึงข้อความของ Prisma ที่อ้างชื่อตาราง ชื่อคอลัมน์ และ query ที่ล้มเหลว |
| **ของใหม่** | `500` ตอบเพียง `เกิดข้อผิดพลาดภายในระบบ` ส่วนข้อความจริงและ stack ไปที่ `console.error` ฝั่ง server ข้อความจะถูกส่งต่อให้ผู้เรียกก็ต่อเมื่อ error นั้นเป็น `HttpError` ซึ่งแปลว่ามีคนเลือกถ้อยคำนั้นไว้ให้ผู้ใช้อ่านโดยตั้งใจ |
| **ของใหม่ (ต่อ)** | คำขอที่ body ไม่ใช่ JSON ที่ parse ได้ ตอบ `400` พร้อม `ข้อมูลที่ส่งมาไม่ถูกต้อง` — `express.json()` ปฏิเสธก่อนถึง route ไหนเลย และติด `status` 400 มาเอง ระบบเก็บ**สถานะ**นั้นไว้เพราะมันบอกความจริง (คำขอผิด ไม่ใช่เซิร์ฟเวอร์พัง) แต่ไม่เก็บ**ถ้อยคำ** เพราะข้อความของ body-parser เป็นภาษาอังกฤษและยกไบต์ที่ parse ไม่ผ่านของผู้เรียกกลับมาด้วย |
| **เหตุผล** | สองอย่างพร้อมกัน — หน้าตาของ error ไม่ควรขึ้นกับว่า deploy ที่ไหน ไม่งั้นไม่มีใครเขียนโค้ดฝั่งผู้เรียกให้รองรับได้ทั้งสองแบบ และรายละเอียดของความล้มเหลวเป็นของคนที่แก้ได้ ซึ่งอยู่ที่ server ไม่ใช่ของคนที่บังเอิญยิงคำขอเข้ามา ส่วนเกณฑ์ "มี `status` ต่ำกว่า 500" เพียงอย่างเดียวไม่พอ เพราะ middleware ของ Express เองก็ติด `status` มาได้โดยที่ถ้อยคำไม่ได้เขียนไว้ให้ผู้ใช้อ่าน |
| **frontend** | ไม่ต้องแก้ — ไม่มีที่ไหนอ่าน `stack` |

### 4. ค่าที่อยู่นอกชุดที่กำหนดไว้ ถูกปฏิเสธ

| | |
| --- | --- |
| **ของเดิม** | คอลัมน์สถานะทั้งหมดเป็น `VarChar` ไม่ใช่ enum ของฐานข้อมูล ค่าที่ส่งมาจึงถูกเขียนลงไปตามนั้นทั้งหมด — `activity_type: "banana"` เขียนสำเร็จ และ `POST /group/accept-invite` เขียน `action` อะไรก็ได้ลงคอลัมน์สถานะของสมาชิก |
| **ของใหม่** | `400` พร้อม `ต้องเป็นค่าใดค่าหนึ่งใน: ...` สำหรับ `activity_type` / `learning_activity_type` (`INDIVIDUAL`, `GROUP`), `role` ของสมาชิกกลุ่ม (`LEADER`, `MEMBER`), `action` ของคำเชิญ (`ACCEPT`, `REJECTED`), `type` ของคำเชิญและของการส่งงาน (`activity`, `learning-activity`), `day_of_week` (`MON`..`SUN`) และรูปแบบเวลา `HH:MM` |
| **เหตุผล** | ค่าที่ระบบทั้งระบบ `if` เทียบกับสตริงคงที่ ต้องถูกจำกัดที่ทางเข้า ไม่งั้นแถวที่มีค่าที่ไม่มีใครรู้จักจะไหลไปโผล่ตรงกลางของ logic ที่อื่น — และเป็นเรื่องที่ #14 บันทึกไว้ว่า "เป็นเรื่องของ #20" ตอนพบว่า `action` ถูกเขียนลงคอลัมน์โดยไม่ตรวจ |
| **frontend** | ไม่ต้องแก้ — ทุกจุดส่งค่าจาก dropdown หรือค่าคงที่ในโค้ด ไม่มีที่ไหนให้ผู้ใช้พิมพ์ค่าพวกนี้เอง |

### 5. คอลัมน์ที่ว่างได้ ถูกล้างค่าได้จริง

| | |
| --- | --- |
| **ของเดิม** | service ของกลุ่ม e-Portfolio แปลงทุกค่าที่ falsy เป็น `undefined` ก่อนถึง Prisma ซึ่งแปลว่า "ไม่ต้องแก้คอลัมน์นี้" นักศึกษาที่กรอกวันที่ผิดแล้วอยากลบออกจึงลบไม่ได้ ส่งค่าว่างมากี่ครั้งค่าเดิมก็ยังอยู่ |
| **ของใหม่** | `""` และ `null` แปลว่า "ล้างค่านี้" และกลายเป็น `NULL` ส่วนฟิลด์ที่ **ไม่ได้ส่งมาเลย** ยังแปลว่า "ไม่ต้องแก้" เหมือนเดิม กระทบ `date` ของ training / certificate / activity, `year` ของ award และ `date_of_birth` ของ portfolio-personal |
| **เหตุผล** | เป็นข้อที่ pin ไว้ใน #16 และ #17 ว่า "รอ #20" การแยก "ไม่ได้ส่งมา" ออกจาก "ส่งค่าว่างมา" ทำที่ชั้น schema ไม่ได้ทำที่ service เพราะ service เห็นแต่ค่าที่ถูกแปลงมาแล้ว — ตัวที่รู้ความต่างคือตัวที่อ่าน request ดิบ |
| **frontend** | ไม่ต้องแก้ — ฟอร์มส่งทุกช่องที่มีอยู่เสมอ ช่องที่ผู้ใช้ลบข้อความออกจึงมาเป็น `""` ซึ่งตอนนี้ทำสิ่งที่ผู้ใช้สั่ง ส่วน `portfolio-personal` ที่ frontend แปลง `null` เป็นสตริง `"null"` ก่อนใส่ FormData (`services/portfolio-personal.service.ts`) schema รับทั้งสองแบบไว้แล้ว ไม่ต้องตามแก้ |

### 6. `PUT /activity` และ `PUT /learning-activity` ไม่ล้าง `is_average_score` / `is_self_assessment` ทิ้ง

| | |
| --- | --- |
| **ของเดิม** | สองช่องนี้ผ่าน `parseBool` ซึ่งอ่านทุกอย่างที่ไม่ใช่ `"true"` ว่าเป็นเท็จ การแก้ไขกิจกรรมโดยไม่ส่งสองช่องนี้มาด้วยจึงเขียนทับเป็น `false` เงียบๆ |
| **ของใหม่** | ไม่ส่งมา = ไม่แก้ ส่ง `"true"`/`"false"` มา = ตั้งค่าตามนั้น ส่งค่าอื่นมา = `400` พร้อม `ต้องเป็นค่า true หรือ false` |
| **เหตุผล** | `parseBool` แยกคนที่ตั้งใจส่ง `false` ออกจากคนที่พิมพ์ผิดหรือไม่ได้ส่งไม่ได้เลย ซึ่งเป็นสิ่งเดียวที่การตรวจ input มีไว้ทำ ส่วนตอน **สร้าง** ไม่มีอะไรเปลี่ยน เพราะคอลัมน์มี `@default(false)` อยู่แล้ว ค่าที่ได้จึงเท่ากับที่ `parseBool(undefined)` เคยให้ |
| **frontend** | ไม่ต้องแก้ — ฟอร์มแก้ไขกิจกรรมส่งสองช่องนี้มาทุกครั้งอยู่แล้ว |

### 7. ข้อความภาษาอังกฤษที่ผู้ใช้เห็น เปลี่ยนเป็นไทย

| | |
| --- | --- |
| **ของเดิม** | กลุ่ม `portfolio` ตอบเป็นภาษาอังกฤษเกือบทั้งกลุ่ม — `Invalid ID`, `user_id is required`, `Portfolio training not found` และอีกเก้าประโยคในรูปแบบเดียวกัน, `This link has expired`, `One or more skills do not belong to this user`, `student_activity_id is required`, `skill_ids must be a non-empty array` ส่วนที่อื่นในระบบมี `File not found`, `Group has no accepted members`, `Student activity not found`, `student_learning_activity not found` และ `ต้องระบุ Token` ที่เป็นไทยครึ่งเดียว |
| **ของใหม่** | ทั้งหมดเป็นไทย — `Invalid ID` / `user_id is required` ถูกแทนที่ด้วยซองของข้อ 1 (`ข้อมูลที่ส่งมาไม่ถูกต้อง: id ต้องเป็นตัวเลข`) ส่วนที่เหลือเป็นข้อความเฉพาะเรื่อง: `ไม่พบการอบรมที่ต้องการ`, `ไม่พบเกียรติบัตรที่ต้องการ`, `ไม่พบประวัติการศึกษาที่ต้องการ`, `ไม่พบการฝึกงานที่ต้องการ`, `ไม่พบรางวัลที่ต้องการ`, `ไม่พบโครงงานที่ต้องการ`, `ไม่พบกิจกรรมที่ต้องการ`, `ไม่พบทักษะที่ต้องการ`, `ไม่พบการเชื่อมโยงชิ้นงานกับทักษะที่ต้องการ`, `ไม่พบข้อมูลส่วนตัวของผู้ใช้รายนี้`, `ไม่พบแฟ้มสะสมผลงานที่ต้องการ`, `ลิงก์นี้หมดอายุแล้ว`, `มีทักษะบางรายการที่ไม่ใช่ของผู้ใช้รายนี้`, `ไม่พบไฟล์ที่ต้องการ`, `ยังไม่มีสมาชิกที่ตอบรับคำเชิญในกลุ่มนี้`, `ไม่พบงานที่ต้องการส่ง` (ตอนส่งงาน) และ `ไม่พบงานที่ต้องการ` (ตอนเปิดดูรายละเอียดงาน ซึ่งเป็นที่สุดท้ายที่ยัง `res.status(404).json(...)` เองอยู่นอก `errorHandler`), `ไม่พบงานของสมาชิกในกลุ่ม`, `โทเค็นคำเชิญไม่ถูกต้องหรือหมดอายุแล้ว` และ `/auth/refresh` ที่เคยตอบอังกฤษสองประโยคใช้ `ไม่พบ Token หรือ Token หมดอายุ` ชุดเดียวกับ middleware |
| **เหตุผล** | frontend แสดง `message` ตรงๆ ข้อความอังกฤษจึงไปโผล่บนหน้าจอนักศึกษาจริง และเป็นข้อบังคับของโครงการอยู่แล้วว่า error ที่ผู้ใช้เห็นต้องเป็นไทย |
| **frontend** | ไม่ต้องแก้ — ไม่มีที่ไหนเทียบ `message` กับสตริง มีแต่เอาไปแสดง |

### 8. `GET /uploads/:filename` ปฏิเสธชื่อไฟล์ที่เป็นเส้นทาง

| | |
| --- | --- |
| **ของเดิม** | ชื่อไฟล์ถูกส่งเข้า `path.resolve` ตรงๆ Express แมตช์ parameter กับ path ที่ยัง encode อยู่ แต่ decode ให้ก่อนถึง handler — `%2e%2e%2f` จึงกลายเป็น `../` **หลัง** จากแมตช์ผ่านไปแล้ว และอ่านไฟล์นอกโฟลเดอร์ uploads ได้ |
| **ของใหม่** | `400` พร้อม `filename ต้องเป็นชื่อไฟล์ ไม่ใช่เส้นทาง` เมื่อค่าที่ decode แล้วมี `/`, `\` หรือเป็น `.` / `..` |
| **เหตุผล** | เป็นช่องโหว่ตรงๆ ไม่ใช่เรื่องความสม่ำเสมอ และเป็นเหตุผลว่าทำไมการตรวจต้องอยู่หลังการ decode — การตรวจที่ path ดิบไม่เห็นอะไรเลย |
| **frontend** | ไม่ต้องแก้ — `getFile` ประกอบ URL จากชื่อไฟล์ที่ API เป็นคนตั้งเอง |

### 9. จำนวนเต็มที่เกินขนาดของคอลัมน์ ตอบ `400`

| | |
| --- | --- |
| **ของเดิม** | ทุก id ในระบบเป็น `integer` ของ Postgres ค่าที่เกิน 2147483647 ถูกส่งลงไปถึงฐานข้อมูลแล้วตีกลับมาเป็น `500` เรื่องความกว้างของชนิดข้อมูล |
| **ของใหม่** | `400` พร้อม `ต้องไม่เกิน 2147483647` และ id ที่เป็นศูนย์หรือติดลบก็ถูกปฏิเสธด้วย เพราะกุญแจของทุกตารางเริ่มที่ 1 — ของเดิมค่าพวกนี้ไปจบที่ "ไม่พบแถว" ซึ่งแยกไม่ออกจากการค้นที่ไม่เจอจริงๆ |
| **เหตุผล** | ขอบเขตของชนิดข้อมูลเป็นความรู้ที่มีอยู่แล้วตั้งแต่ตอนอ่านคำขอ ไม่มีเหตุผลให้ต้องรอฐานข้อมูลตอบ |
| **frontend** | ไม่ต้องแก้ — id ทุกตัวที่ frontend ส่งมาจาก response ของ API เอง |

### 10. `PUT /portfolio-education` ไม่ย้ายเจ้าของแถวตามที่ body สั่งอีกต่อไป

| | |
| --- | --- |
| **ของเดิม** | body ถูกส่งต่อให้ Prisma ทั้งก้อน คำขอที่ใส่ `user_id` มาด้วยจึงเขียนทับคอลัมน์นั้นได้ — ย้ายประวัติการศึกษาของคนอื่นมาเป็นของตัวเองด้วย request เดียว (pin ไว้ที่ `portfolio-education.test.ts` ตอน #16) |
| **ของใหม่** | schema ของการแก้ไขไม่มี `user_id` และคีย์ที่ไม่รู้จักถูกตัดทิ้งก่อนถึง service คอลัมน์เจ้าของจึงไม่ถูกแตะ |
| **เหตุผล** | รูปแบบเดียวกันนี้ปิดไปทั้งกลุ่ม — ทุก schema ของการแก้ไขประกาศเฉพาะคอลัมน์ที่แก้ได้จริง สิ่งที่ไม่ได้ประกาศจะไม่ถึง Prisma |
| **ขอบเขต** | ปิดแค่ **กลไก** นี้ ยังไม่มีอะไรตรวจว่าคนที่ยิงคำขอเป็นเจ้าของแถวนั้นจริง — ยังเป็น #31 เหมือนเดิม |
| **frontend** | ไม่ต้องแก้ — ไม่มีที่ไหนส่ง `user_id` มาใน body ของ `PUT` |

### 11. `POST /portfolio-skill` ต้องบอกว่าแต่ละ mapping อ้างถึงงานชิ้นไหน

| | |
| --- | --- |
| **ของเดิม** | `student_activity_id` เป็น optional ในชนิดที่ประกาศไว้ mapping ที่ไม่มีงานจึงถูกเขียนเป็นแถวที่คอลัมน์นั้นเป็น `NULL` — หลักฐานของทักษะที่ไม่ได้ชี้ไปที่อะไรเลย (คอลัมน์นี้ไม่มี foreign key ตามที่ #17 บันทึกไว้) |
| **ของใหม่** | `400` พร้อม `mappings[0].student_activity_id ต้องระบุ` |
| **เหตุผล** | แถวใน `portfolio_skill_activity_mapping` มีอยู่เพื่อผูกทักษะเข้ากับงาน แถวที่ไม่มีงานจึงไม่ได้แปลว่าอะไรเลย พร้อมกันนี้ `UpdateSkillMappingReqBody` ที่ประกาศ `id` ของแถวไว้ก็ถูกลบทิ้ง เพราะ handler ลบ mapping ทั้งชุดแล้วเขียนใหม่ทุกครั้ง ไม่เคยแก้รายแถว — `id` ที่ส่งมาไม่เคยถูกอ่าน |
| **frontend** | ไม่ต้องแก้ — `create-work-form.tsx` และ `work-edit-modal.tsx` ส่ง `student_activity_id` มาทุก mapping อยู่แล้ว |

### 12. `GET /portfolio/:id` และ `/portfolio/public/:token` ที่ไม่ใช่ UUID ตอบ `400`

| | |
| --- | --- |
| **ของเดิม** | `500` — `404` ที่ controller ตรวจไว้เอื้อมถึงเฉพาะ id ที่มีรูปแบบถูกต้องเท่านั้น ค่าอื่นล้มที่ Postgres ก่อนที่ controller จะได้ตัดสินใจ |
| **ของใหม่** | `400` พร้อม `id ต้องเป็นรหัส UUID` ส่วน id ที่รูปแบบถูกแต่ไม่มีแถว ยังเป็น `404` `ไม่พบแฟ้มสะสมผลงานที่ต้องการ` เหมือนเดิม |
| **เหตุผล** | "รูปแบบผิด" กับ "ไม่มีของชิ้นนี้" เป็นคนละคำตอบ และของเดิมตอบข้อแรกว่าเซิร์ฟเวอร์พัง |
| **frontend** | ไม่ต้องแก้ — token ในลิงก์แชร์มาจาก API และหน้า `public-portfolio-page.tsx` แยกเฉพาะ `410` ซึ่งไม่เปลี่ยน |

### 13. การส่งงานแบบกลุ่มต้องมี `group_id` และ "ไม่พบงาน" เป็น `404`

| | |
| --- | --- |
| **ของเดิม** | `POST /student/submit/activity` และคู่แฝดฝั่ง learning-activity ที่ส่ง `type: "GROUP"` มาโดยไม่มี `group_id` ตอบ `500` ส่วนงานที่ไม่มีอยู่จริงตอบ `500` พร้อมข้อความอังกฤษ (`Student activity not found`) และกลุ่มที่ยังไม่มีใครตอบรับคำเชิญตอบ `500` พร้อม `Group has no accepted members` |
| **ของใหม่** | ตามลำดับ: `400` พร้อม `group_id ต้องระบุเมื่อส่งงานแบบกลุ่ม`, `404` พร้อม `ไม่พบงานที่ต้องการส่ง`, และ `400` พร้อม `ยังไม่มีสมาชิกที่ตอบรับคำเชิญในกลุ่มนี้` |
| **เหตุผล** | ทั้งสามเป็นการปฏิเสธที่เขียนไว้อย่างตั้งใจแล้ว ขาดแค่สถานะที่บอกความจริง — `500` แปลว่าเซิร์ฟเวอร์พัง ซึ่งบอกนักศึกษาที่กดส่งงานผิดว่าไม่มีอะไรให้ทำได้ ทั้งที่แก้ได้ด้วยตัวเอง |
| **frontend** | ไม่ต้องแก้ — หน้าส่งงานแสดง `message` ตรงๆ และตอนนี้เป็นไทยที่บอกสาเหตุจริง |

### 14. `POST /auth/google` ที่ไม่ส่ง credential มา

| | |
| --- | --- |
| **ของเดิม** | `400` พร้อม `ไม่พบข้อมูลการเข้าสู่ระบบจาก Google` |
| **ของใหม่** | `400` พร้อม `ข้อมูลที่ส่งมาไม่ถูกต้อง: credential ต้องระบุ` — สถานะเท่าเดิม เปลี่ยนเฉพาะข้อความให้เข้าซองเดียวกับที่อื่น |
| **เหตุผล** | เป็นการตรวจ input แบบเดียวกับทุก endpoint จึงไม่มีเหตุผลให้มีถ้อยคำของตัวเอง ส่วนกรณีที่ credential มาแต่ Google ปฏิเสธ ยังเป็นข้อความเดิมของมัน เพราะนั่นไม่ใช่เรื่องรูปแบบของข้อมูล |
| **frontend** | ไม่ต้องแก้ — `login-page.tsx` แสดง `message` ตรงๆ และกรณีนี้เกิดจากโค้ดที่เขียนผิดเท่านั้น หน้า login ไม่มีทางยิงโดยไม่มี credential |

### 15. `POST /student-activity/attachments` ตอบพร้อม `message` เหมือน endpoint อื่น

| | |
| --- | --- |
| **ของเดิม** | ตอบ `{ "success": true, "data": ... }` โดยไม่มี `message` เป็น endpoint เดียวในระบบที่ซองไม่ครบ |
| **ของใหม่** | มี `message` เหมือนทุกตัว |
| **เหตุผล** | ซองของ response ที่ผู้เรียกต้องรู้ว่า endpoint ไหนขาดฟิลด์ไหน ก็ไม่ใช่ซอง |
| **frontend** | ไม่ต้องแก้ — ไม่มีที่ไหนอ่าน `message` ของ response ที่สำเร็จ |

### รายการที่เคยเขียนไว้ว่า "รอ #20" และปิดแล้วในรอบนี้

- **query parameter ที่หายไปแล้วกลายเป็น `NaN`** (#12, #13) — เป็น `400` ทั้งหมดแล้ว ตามข้อ 2
- **`GET /rubric/shared-rubric` ที่ไม่ส่ง `program_id` คืน rubric ของทุกหลักสูตร** (#13) — `400`
- **`POST /mapping/activity` ที่ไม่ส่ง `weight` ตอบ `500` จาก `NaN`** (#13) — `400`
- **`action` ของ `/group/accept-invite` ถูกเขียนลงคอลัมน์โดยไม่ตรวจค่า** (#14) — เป็น enum ปิดแล้ว ตามข้อ 4
- **`GET /gradebook/per-activity` ที่ไม่ส่ง `section_id` ตอบ `200`** (#15) — `400`
- **`PUT` ของ training / certificate / award / portfolio-activity ล้างค่าวันที่/ปีไม่ได้** (#16, #17) — ล้างได้แล้ว ตามข้อ 5
- **`GET /student/enrolled/subjects` ที่ไม่ส่ง `student_id` คืนการลงทะเบียนของทุกคน** (#18) — `400`
- **`GET /user` ที่ไม่ส่ง `id` ตอบ `500` ส่วน `GET /student/activities/list` ตอบ `200` พร้อม `[]`** (#18) — ทั้งคู่เป็น `400`
- **`PUT /portfolio-education` เขียนทับคอลัมน์ `user_id` จาก body ได้** (#16) — ปิดแล้ว ตามข้อ 10 (ส่วนเรื่องความเป็นเจ้าของยังเป็น #31)

### สิ่งที่พบแล้ว **pin ไว้ตามที่เป็น** (ไม่ได้แก้ใน ticket นี้)

- **`PUT` และ `DELETE` ที่อ้าง id ที่ไม่มีอยู่ ยังตอบ `500` ไม่ใช่ `404`** — #16/#17
  เขียนไว้ว่า "ควรทำรวดเดียวพร้อม #20" แต่ไม่ได้ทำ เพราะเป็นคนละแกนกัน: id ที่ส่งมา
  **ถูกต้องตามรูปแบบทุกประการ** แถวที่มันชี้ถึงต่างหากที่ไม่มี การแปลง `P2025` ของ
  Prisma เป็น `404` ที่ `errorHandler` เปลี่ยนคำตอบของทุก endpoint ในระบบพร้อมกัน
  รวมถึง endpoint ที่ไม่มีใครแตะใน ticket นี้ จึงควรเป็น ticket ของตัวเอง — ซึ่งก็คือ
  #42 **ปิดแล้ว** ดูหัวข้อ #42 ข้างบน
- **`POST /portfolio-award` ที่ไม่ส่ง `is_show` ยังสร้างรางวัลแบบ "ซ่อน"** — #17
  บันทึกไว้ว่ารอ ticket นี้ แต่เมื่อดูจริงแล้วไม่ใช่เรื่องรูปแบบของข้อมูล: ค่าที่หายไป
  จะแปลว่า "ซ่อน" หรือ "แสดง" เป็นการตัดสินใจเรื่องความหมายของฟอร์ม ไม่ใช่การตรวจ
  input schema จึงประกาศให้เป็น optional ตามที่ควรเป็น แล้วปล่อยพฤติกรรมเดิมของ
  service ไว้ test ยังบันทึกไว้ตามที่เป็นทั้งสองฝั่ง — การตัดสินใจนั้นเกิดขึ้นแล้วใน
  #46 **ปิดแล้ว** ดูหัวข้อ #46 ข้างบน ค่าที่ไม่ได้ส่งมาแปลว่า "แสดง" ตาม
  `@default(true)` ของคอลัมน์
- **`fileType` ของไฟล์แนบใน `GET /portfolio/public/:token` เป็น `"file"` เสมอ** — โค้ด
  อ่าน `a.file_type` จากผลลัพธ์ของ query ที่ไม่ได้ `select` คอลัมน์นั้นมา ค่าจึงเป็น
  `undefined` ตั้งแต่วันแรกที่เขียน พอใส่ type ให้ตัวแปรที่เคยเป็น `any` ความจริงข้อนี้
  ก็โผล่ออกมา ไม่ได้แก้เพราะสองเหตุผล: คอลัมน์เก็บนามสกุลไฟล์เป็นตัวพิมพ์ใหญ่ (`"PDF"`)
  ซึ่งไม่ใช่คำใดในห้าคำที่ template ใช้เทียบ และ query เดียวกันนี้เสิร์ฟ
  `/student-activity/attachments` ด้วย การเพิ่มคอลัมน์เข้าไปจึงเปลี่ยน response ของ
  endpoint อื่นด้วย หน้าเว็บไม่กระทบเลย เพราะ `use-public-portfolio.ts` คำนวณชนิดไฟล์
  จากชื่อไฟล์เองอยู่แล้ว
- **`POST /mapping/activity` ยังตอบ `500` เมื่อกิจกรรมไม่มีหมวดคะแนน** — #13 บันทึกไว้
  ว่ารอ ticket นี้ แต่เป็นกรณีเดียวกับข้อแรก: `activity_id` ที่ส่งมาถูกต้องและมี
  กิจกรรมนั้นอยู่จริง สิ่งที่ขาดคือแถวใน `subject_score_ratio` ที่กิจกรรมนั้นอ้างถึง
  ซึ่งเป็นข้อเท็จจริงในฐานข้อมูล ไม่ใช่รูปแบบของคำขอ — `score_ratio_id ?? 0` ยังส่ง
  `0` ที่ไม่มีอยู่จริงลงไปติด foreign key เหมือนเดิม test ยังบันทึกไว้ตามที่เป็น
- **การตรวจความเป็นเจ้าของยังเป็น #31** — ticket นี้ตรวจว่าข้อมูล **มีรูปแบบถูกต้อง**
  ไม่ได้ตรวจว่า **คนที่ส่งมามีสิทธิ์กับข้อมูลนั้น** คำขอที่ระบุ `user_id` ของคนอื่น
  ยังผ่านทุกจุดที่เคยผ่าน

---

## #21 — สุขอนามัยของโค้ดฝั่ง frontend

ticket นี้ไม่ได้แตะ API เลย เหมือน #19 — ผู้เรียก endpoint ไม่เห็นความต่าง
ที่**ผู้ใช้**เห็นความต่างจริงมีสองข้อคือข้อ 1 และข้อ 2 ที่เหลือเป็นการเปลี่ยนภายใน

### 1. คำขอที่ได้ `401` พร้อมกันรอคิว refresh เดียวกัน ไม่ถูกโยนทิ้ง

| | |
| --- | --- |
| **ของเดิม** | `src/lib/axios.ts` ใช้ตัวแปร global `isRefreshing` เป็น boolean คำขอที่ได้ `401` ตอนที่ flag ถูกตั้งอยู่แล้วจะถูก `Promise.reject` ทันทีโดยไม่รอผล การโหลดหน้าหนึ่งครั้งยิงหลายคำขอพร้อมกัน token ที่หมดอายุจึงกลับมาเป็น `401` ทีละหลายใบ ใบแรกต่ออายุ session สำเร็จ ส่วนที่เหลือล้มทั้งหมด ผลที่ผู้ใช้เห็นคือหน้าที่โหลดอยู่ดีๆ เด้งกลับไปหน้า login ทั้งที่ session ถูกต่ออายุเรียบร้อยแล้ว |
| **ของใหม่** | `src/utils/single-flight.ts` (ใหม่) ห่อการ refresh ไว้ คำขอทุกใบในชุดนั้นรอ promise เดียวกัน แล้วค่อย retry คำขอเดิมของตัวเองต่อ ไม่มีใบไหนถูกปฏิเสธเพราะ "มีคนอื่นกำลัง refresh อยู่" |
| **เหตุผล** | API หมุน refresh token ใหม่ทุกครั้งที่ใช้ การปล่อยให้ refresh สองรอบวิ่งพร้อมกันจึงไม่ใช่แค่เปลืองคำขอ แต่รอบที่สองจะใช้ token ที่รอบแรกเปลี่ยนไปแล้ว และจบ session ด้วยคำขอที่ควรจะต่ออายุมันเสียเอง — การเข้าคิวจึงเป็นทั้งการแก้อาการเด้งออกและการกันการแข่งกันใช้ token พร้อมกัน ส่วน `single-flight` ไม่จำผลของครั้งก่อน (ทั้งสำเร็จและล้มเหลว) เพราะ refresh ที่ล้มเพราะเน็ตสะดุดไม่ควรกลายเป็นการ logout ของทุกคำขอที่ตามมา |
| **frontend** | คือการแก้ฝั่ง frontend เอง — `src/lib/axios.ts` และ `src/utils/single-flight.ts` (พร้อม test ของตรรกะบริสุทธิ์ตามเส้นที่ #19 วางไว้) |

### 2. ปุ่มแก้ไข (ไอคอนดินสอ) ของ "เป้าหมายทางวิชาชีพ" หายไป

| | |
| --- | --- |
| **ของเดิม** | `goal-section.tsx` ส่ง `onEdit={() => console.log("click")}` ให้ `TitleWithEditIcon` ไอคอนดินสอจึงถูกวาดข้างหัวข้อ "เป้าหมายทางวิชาชีพ" กดแล้วไม่มีอะไรเกิดขึ้นนอกจาก log ลง console |
| **ของใหม่** | `onEdit` เป็น optional และ `goal-section.tsx` ไม่ส่งมาแล้ว ไอคอนจึงไม่ถูกวาด หัวข้ออื่นที่ส่ง `onEdit` จริงยังมีไอคอนเหมือนเดิมทุกอัน |
| **เหตุผล** | เป็นผลข้างเคียงของการลบ `console.log` (ข้อ 5) ที่ผู้ใช้เห็น จึงจดแยกไว้ ทางเลือกคือคงไอคอนไว้โดยไม่มี handler แต่ปุ่มที่กดแล้วไม่ทำอะไรบอกผู้ใช้ผิดว่าแก้ไขได้ ส่วนนี้ยังเป็นส่วนที่ใช้ข้อความ mock ทั้งก้อนอยู่ ยังไม่มีขาบันทึกให้ต่อ |
| **frontend** | คือการแก้ฝั่ง frontend เอง — ถ้าจะทำหน้าแก้ไขเป้าหมายจริงเมื่อไหร่ ให้ส่ง `onEdit` กลับเข้าไป ไอคอนจะกลับมาเอง |

### 3. `useParams()` ถูกย้ายออกจาก `render` ของตาราง 5 ไฟล์

| | |
| --- | --- |
| **ของเดิม** | `activity-column.tsx`, `student-work-column.tsx` (ทั้งของ activity และของ learning-activity), `learning-activity-column.tsx` และ `evaluation-column.tsx` เรียก `useParams()` อยู่ **ข้างใน** `render` ของคอลัมน์ antd เรียก `render` หนึ่งครั้งต่อหนึ่งแถว จำนวน hook ที่ถูกเรียกจึงผูกกับจำนวนแถวในตาราง |
| **ของใหม่** | เรียก `useParams()` ครั้งเดียวที่ตัว column factory แล้วให้ `render` ปิดทับค่าที่ได้ |
| **เหตุผล** | เป็นการผิดกฎ hooks ตรง ๆ (D9 — เจอระหว่างทำก็แก้ตรงนั้น) ตารางที่จำนวนแถวเปลี่ยนระหว่าง render ทำให้ลำดับ hook ของ React เลื่อน ซึ่งเป็นคลาสของบั๊กที่อาการไม่ตรงกับต้นเหตุ ยังไม่เคยระเบิดเพราะ column factory ถูกเรียกในตำแหน่งเดิมทุกครั้งและ `useParams` ไม่ถือ state ของตัวเอง แต่ไม่ใช่เหตุผลที่จะทิ้งไว้ |
| **frontend** | ไม่กระทบสิ่งที่ผู้ใช้เห็น — `useParams()` คืนค่าเดิมทุกแถวอยู่แล้ว ลิงก์ที่ `generatePath` สร้างจึงเหมือนเดิมทุกเส้น |

### 4. `catch` ที่เคยว่างเปล่า 17 จุดพิมพ์ error ลง console

| | |
| --- | --- |
| **ของเดิม** | `} catch (error) {}` — error ถูกกลืนหายไปเงียบๆ ทั้งก้อน |
| **ของใหม่** | `} catch (error) { console.error(error); }` เหมือนกับอีก ~54 จุดในโค้ดเดิมที่ทำแบบนี้อยู่แล้ว |
| **เหตุผล** | eslint กฎ `no-empty` ผ่านได้ด้วยการใส่ comment เฉยๆ แต่นั่นทำให้กฎผ่านโดยที่ปัญหายังอยู่ การเลือก `console.error` เพราะเป็นรูปแบบที่ codebase นี้ใช้อยู่แล้ว ไม่ประดิษฐ์วิธีที่สอง |
| **frontend** | ไม่กระทบสิ่งที่ผู้ใช้เห็น — เปลี่ยนเฉพาะสิ่งที่ปรากฏใน console ของนักพัฒนา |

### 5. ตัวแปรสภาพแวดล้อมฝั่ง web ถูกอ่านที่เดียวและล้มทันทีเมื่อขาด

| | |
| --- | --- |
| **ของเดิม** | `import.meta.env.VITE_BACKEND_URL` / `VITE_GOOGLE_CLIENT_ID` ถูกอ่านกระจายตามไฟล์ ไม่มี type ไม่มีการตรวจ ค่าที่ไม่ได้ตั้งตอน build จะเป็น `undefined` แล้วไหลไปโผล่เป็นคำขอไปที่ `"undefined/student/…"` |
| **ของใหม่** | `src/configs/env.ts` (ใหม่) อ่านตัวแปร `VITE_*` ทั้งสองตัวที่เดียว ตรวจว่าครบ แล้ว `throw` พร้อมชื่อตัวแปรที่ขาดและที่อยู่ของ `.env.example` — และ `src/vite-env.d.ts` ประกาศ type ของทั้งสองตัวไว้ |
| **เหตุผล** | Vite ฝังค่าตอน build ไม่ใช่ตอนรัน ความผิดพลาดจึงเกิดตอน build เสมอ แต่ไปแสดงอาการตอนผู้ใช้เปิดหน้าเว็บ การล้มพร้อมข้อความที่บอกชื่อตัวแปรจึงย้ายอาการกลับไปอยู่ใกล้ต้นเหตุ |
| **frontend** | ไม่ต้องแก้เพิ่ม — ทั้งสองตัวเป็นตัวแปรเดิมที่ `docker-compose.yml` ส่งให้อยู่แล้ว (ดูข้อ 7 ของ #11) build ที่ตั้งค่าครบทำงานเหมือนเดิมทุกประการ แต่ **รัศมีของ build ที่ตั้งค่าไม่ครบกว้างขึ้น**: `throw` เกิดตอนโหลด module ไม่ใช่ตอนใช้ค่า build ที่ขาดเฉพาะ `VITE_GOOGLE_CLIENT_ID` เดิมพังแค่หน้า login ตอนนี้จอขาวทุกหน้า เลือกทางนี้เพราะทั้งสองตัวเป็นของที่แอปไม่ทำงานถ้าขาด และจอขาวที่มี error บอกชื่อตัวแปรใน console หาต้นเหตุได้เร็วกว่าหน้าที่ดูปกติแต่ยิงคำขอไปที่ `"undefined/…"` |

### 6. `console.log` ที่ค้างอยู่ถูกลบ 38 จุด

issue #21 เขียนไว้ว่า 39 จุด นับจริงในโค้ดได้ 38 — ตัวเลขในนี้คือจำนวนที่ลบจริง
ในจำนวนนี้มี 7 จุดที่เป็น `onClick={() => console.log("click")}` ผูกอยู่กับ
`SectionLayout` และ 3 จุดที่เป็น handler ปลอมของ `UploadButton` / `UploadSection`
ซึ่งเป็นการรับ event ไว้เฉยๆ ทั้งหมด — prop เหล่านั้นถูกเปลี่ยนเป็น optional
พร้อม comment กำกับว่ายังไม่มีใครรอฟัง แทนที่จะปล่อยให้ callback ที่ไม่ทำอะไร
ดูเหมือนมีคนต่อสายไว้แล้ว อีกหนึ่งจุดใน `e-portfolio-table.tsx` เปลี่ยนเป็น
`console.error` เพราะเป็นการรายงาน validation ที่ล้ม ไม่ใช่เศษ debug

### สิ่งที่พบแล้ว **pin ไว้ตามที่เป็น** (ไม่ได้แก้ใน ticket นี้)

- **ตาราง "ไฟล์แนบปริญญานิพนธ์" ยังบันทึกอะไรไม่ได้** — `thesis-file-table.tsx`
  เรียก `message.useMessage()` แล้วใช้ `messageApi.error(...)` แต่ไม่เคย render
  `contextHolder` ลงใน JSX ซึ่ง antd ต้องมีถึงจะวาดข้อความได้ ticket นี้ใส่
  `{contextHolder}` กลับเข้าไปเพราะเป็นสายที่ขาดชัด ๆ แต่**ผู้ใช้ยังไม่เห็นความต่าง**:
  ตัว `handleSave` มีแต่ comment ทั้งก้อน ไม่มีอะไรที่ throw ได้ `catch` จึงยังไม่เคย
  ทำงาน ข้อความ `"กรุณาลองใหม่อีกครั้ง"` ยังไม่ขึ้นจอ การจะทำให้ตารางนี้บันทึกได้จริง
  ต้องต่อทั้งเส้น (ยังไม่มี endpoint สำหรับไฟล์แนบปริญญานิพนธ์) ไม่ใช่งานของ ticket
  สุขอนามัย
- ~~**`utils/get-file.ts` ยังอ่าน `import.meta.env` เอง**~~ — อ่าน `NODE_ENV` ซึ่ง Vite
  ไม่เคยกำหนด (มีแต่ `MODE`/`DEV`/`PROD`) ค่าจึงเป็น `undefined` เสมอและสาขา
  same-origin ไม่เคยทำงาน pin ไว้ตั้งแต่ #19 — **ลบทิ้งใน #36** ไฟล์นี้ไม่อ่าน
  `import.meta.env` เองอีกแล้ว `configs/env.ts` จึงเป็นที่เดียวจริง ๆ ตามที่ comment
  ของมันบอกไว้ การตัดสินว่า production ควรยิงไป origin ไหนยังไม่ได้เกิดขึ้น —
  ที่หายไปคือโค้ดที่ทำเหมือนว่าตัดสินไปแล้ว
- **`vite-env.d.ts` ยังกันชื่อที่พิมพ์ผิดไม่ได้** — `ImportMetaEnv` ที่ประกาศไว้ถูก
  merge เข้ากับของ `vite/client` ที่มี index signature `[key: string]: any` อยู่แล้ว
  `import.meta.env.VITE_ชื่อที่ไม่มีจริง` จึงยัง typecheck ผ่าน ตัวที่จับได้จริงคือ
  การตรวจตอนรันใน `env.ts` การจะให้ type จับได้ต้องเลิก `/// <reference
  types="vite/client" />` แล้วประกาศทุกอย่างที่ Vite ให้มาเอง ซึ่งแลกไม่คุ้ม

### สิ่งที่ **ไม่ได้** ทำใน ticket นี้

- **`any` 142 จุด** — issue กันไว้นอกขอบเขตตั้งแต่ต้น (D8: frontend ไม่มี test คุ้ม
  การไล่แก้ type จึงเป็นการแก้ที่ไม่มีใครตรวจได้) กฎ
  `@typescript-eslint/no-explicit-any` จึงตั้งเป็น `warn` ไม่ใช่ `error` —
  ยังเห็นได้ทุกครั้งที่ lint แต่ไม่ทำให้ `npm run lint` ล้ม การปรับกลับเป็น
  `error` คือขั้นตอนสุดท้ายของคนที่รับงานนั้นไปทำ
- **`react-hooks/exhaustive-deps` 74 จุด** — เป็น warning ของ preset ตั้งแต่ต้น
  ไม่ได้ปรับ การใส่ dependency ให้ครบเปลี่ยนจำนวนครั้งที่ effect ทำงานจริง
  ซึ่งเป็นการเปลี่ยนพฤติกรรมที่ต้องดูทีละจุด ไม่ใช่งานเก็บกวาด
- **ไฟล์ขนาด 500–600 บรรทัด** — issue กันไว้นอกขอบเขตด้วยเหตุผลเดียวกับ `any`
- **`ids_to_delete` ที่ `work-edit-modal.tsx` ไม่เคยส่ง** — ตัวแปรและ `handleRemove`
  ที่เก็บ id ไว้ถูกลบทิ้งเพราะไม่มีใครอ่าน ไม่ใช่การตัดฟีเจอร์: `Upload` ตัวนั้นตั้ง
  `showUploadList={{ showRemoveIcon: false }}` ไว้ ปุ่มลบจึงไม่เคยถูกวาด และ
  `assignWorkToSkills` ที่เป็นขาบันทึกก็ไม่มี field สำหรับไฟล์แนบเลย การจะลบไฟล์แนบ
  จากหน้านี้ได้จริงต้องออกแบบทั้งเส้น ไม่ใช่ต่อสายที่ค้างอยู่

---

## #31 — กลุ่ม e-Portfolio อยู่หลังการตรวจสิทธิ์

ticket นี้ไม่ได้เขียน test ตามพฤติกรรมเดิมอย่าง #16–#18 แต่**เปลี่ยนพฤติกรรม
ทั้งกลุ่มโดยตั้งใจ** — 10 router 56 endpoint ที่ไม่เคยมี middleware แม้แต่ตัวเดียว
กติกาที่ใช้ร่วมกันทั้งกลุ่ม ทางเลือกที่ไม่ได้เลือก และเหตุผลที่อาจารย์ยังเข้าไม่ได้
อยู่ที่ [ADR-0001](docs/adr/0001-portfolio-access.md) ที่นี่บันทึกเฉพาะสิ่งที่
**ผู้เรียกเห็นต่างไป** ทุกข้อข้างล่างมีผลกับทั้งกลุ่ม ไม่ใช่ endpoint เดียว

### 1. ทุก endpoint ในกลุ่มต้องมี session — `401` เมื่อไม่มี

| | |
| --- | --- |
| **ของเดิม** | ทั้ง 56 endpoint ตอบคำขอที่ไม่ส่ง cookie มาเลยอย่างครบถ้วน สิ่งเดียวที่บอกว่าคำขอเป็นของใครคือ `user_id` ที่ผู้เรียกเขียนมาเองใน query string หรือ body และค่านั้นคือรหัสนักศึกษา 8 หลักที่เดาได้ ใครยิง HTTP ถึงก็อ่านวันเกิด เบอร์โทรและอีเมลของนักศึกษาคนไหนก็ได้ แก้หรือลบประวัติของคนอื่นได้ และออก share link สาธารณะให้แฟ้มของคนอื่นได้ |
| **ของใหม่** | ทุกตัวอยู่หลัง `requireUser` — ไม่มี session ได้ `401` พร้อม `{ "success": false, "message": "ไม่พบ Token หรือ Token หมดอายุ" }` ก่อนถึงการตรวจอย่างอื่นทั้งหมด ข้อยกเว้นเดียวคือ `GET /portfolio/public/:token` ที่**ยังเปิดสาธารณะตามเดิม** |
| **เหตุผล** | ตัว token ของลิงก์แชร์คือหลักฐานการเข้าถึงในตัวมันเอง ถ้าบังคับให้ล็อกอินก่อน ปุ่มแชร์ก็ไม่เหลืออะไรให้ทำ ส่วนที่เหลือของกลุ่มเป็นข้อมูลของนักศึกษารายคนล้วนๆ ไม่มีเหตุผลใดให้เปิด — ดู ADR-0001 |
| **ขอบเขต** | `GET /portfolio/templates` อยู่หลัง `requireUser` เฉยๆ ไม่มีการตรวจเจ้าของ เพราะ template เป็นชุดเดียวกันของทุกคนและไม่เป็นของใคร และทั้งกลุ่มใช้ `requireUser` ไม่ใช่ `requireRole("STUDENT")` — แฟ้มเป็นของ**ผู้ใช้** ไม่ใช่ของบทบาท ผู้ใช้ที่ยังไม่มีแถวใน `user_roles` จึงยังใช้แฟ้มของตัวเองได้ |
| **ผลข้างเคียง** | session ที่บัญชีถูกลบไปแล้วได้ `401` แทนที่จะเดินต่อไปจนล้ม — `POST /portfolio` ที่เคยตอบ `500` เพราะติด foreign key ตอนนี้ตอบ `401` เป็นผลจาก `requireUser` ที่ตรวจการมีอยู่จริงทุก request มาตั้งแต่ข้อ 1 ของ #10 |
| **frontend** | ไม่ต้องแก้ — ทุกไฟล์ใต้ `features/student/portfolio` ยิงผ่าน `axiosInstance` ซึ่งตั้ง `withCredentials: true` และมีคิว refresh ของ #21 อยู่แล้ว ทั้งหมดนี้อยู่ในส่วนของนักศึกษาที่ต้องล็อกอินอยู่ก่อนแล้ว |

### 2. คำขอที่ระบุ `user_id` ของคนอื่น ตอบ `403`

| | |
| --- | --- |
| **ของเดิม** | รายการทั้งเก้าของกลุ่ม (`GET /portfolio`, `/portfolio-education`, `/portfolio-training`, `/portfolio-certificate`, `/portfolio-internship`, `/portfolio-award`, `/portfolio-thesis`, `/portfolio-activity`, `/portfolio-skill`) รวมทั้ง `/portfolio-skill/works` รับ `?user_id=` ของใครก็ได้ เช่นเดียวกับสี่ endpoint ของ `/portfolio-personal/:user_id` ที่รับรหัสจาก path |
| **ของใหม่** | `403` พร้อม `{ "success": false, "message": "คุณไม่มีสิทธิ์เข้าถึงข้อมูลของผู้ใช้อื่น" }` เมื่อรหัสที่ระบุไม่ใช่เจ้าของ session |
| **เหตุผล** | กติกาข้อเดียวของทั้งกลุ่มคือ "ผู้กระทำคือเจ้าของ session เสมอ ไม่ใช่คนที่คำขอบอกว่าเป็น" ข้อความปฏิเสธเป็นประโยคเดียวกันทั้ง "ไม่ใช่รายการของคุณ" และ "ไม่ใช่แถวของคุณ" โดยตั้งใจ — การแยกสองอย่างนี้คือการบอกผู้เรียกว่าอะไรมีอยู่จริง |
| **ขอบเขต** | พารามิเตอร์ `user_id` **ยังอยู่** ไม่ได้ถอดทิ้งทั้งที่ค่าต้องเท่ากับ session เสมอ เพราะนั่นคือรอยต่อที่ #30 จะมาขยาย ถ้าวันหนึ่งอาจารย์อ่านแฟ้มของนักศึกษาที่ตัวเองสอนได้ กฎจะเปลี่ยนที่ middleware จุดเดียว ไม่ต้องคืนพารามิเตอร์กลับมาทั้งกลุ่ม |
| **ผลข้างเคียง** | `404` ของ `GET /portfolio-personal/:user_id` (ข้อ 3 ของ #16) เอื้อมไม่ถึงอีกต่อไป — path ต้องเป็นรหัสของเจ้าของ session ซึ่ง `requireUser` ยืนยันแล้วว่ามีอยู่จริง test ยังคงเคสนั้นไว้แต่เปลี่ยนไปยืนยัน `403` พร้อม comment ว่าทำไม |
| **frontend** | ไม่ต้องแก้ — ทุกจุดที่เรียกส่ง `studentId` ของคนที่ล็อกอินอยู่จาก `home-slice` อยู่แล้ว ไม่มีหน้าไหนขอของคนอื่น |

### 3. คำขอที่ระบุแถวของคนอื่น (`/:id`) ตอบ `403`

| | |
| --- | --- |
| **ของเดิม** | `GET`, `PUT`, `DELETE` ที่อ้างแถวด้วย id ไม่เคยดูเลยว่าแถวนั้นเป็นของใคร รหัสของทั้งเก้าส่วนเป็น autoincrement ที่เดาได้ ใครที่เดาเลขถูกจึงอ่าน แก้ หรือลบประวัติของคนอื่นได้ รวมถึง `POST /portfolio/:id/generate-share-link` ที่เปิดแฟ้มของคนอื่นสู่สาธารณะได้ |
| **ของใหม่** | `requireOwnEntry` อ่านเจ้าของของแถวนั้นก่อน แล้วตอบ `403` ด้วยข้อความเดียวกับข้อ 2 เมื่อไม่ตรงกับ session |
| **เหตุผล** | เป็นการตรวจชุดเดียวกับข้อ 2 คนละรูปของคำขอ — ที่นั่นคำขอบอกว่าเป็นของใคร ที่นี่แถวเป็นคนบอก มี lookup หนึ่งชุดต่อหนึ่งตาราง (`entryOwner`) แทนที่จะให้ทุก service เติม `user_id` ลงใน `where` ของตัวเอง ซึ่งเป็น query เท่ากันแต่กระจายไปหกสิบจุดที่ลืมได้ทีละจุด |
| **ขอบเขต** | mapping ของทักษะไม่มี `user_id` ของตัวเอง เจ้าของอ่านผ่าน `portfolio_skill` ที่มันห้อยอยู่ ส่วน**แถวที่ไม่มีอยู่จริงไม่ใช่เรื่องของ middleware ตัวนี้** — มันปล่อยผ่านไปให้ controller ตอบ `404`/`500` เหมือนเดิม การรวมสองคำถามเข้าด้วยกันจะทำให้ `404` ทุกตัวในกลุ่มกลายเป็น `403` และปิดความต่างนั้นจากผู้เรียก |
| **frontend** | ไม่ต้องแก้ — ทุกหน้าเดินมาจากรายการของตัวเอง id ที่ส่งต่อจึงเป็นของเจ้าของ session เสมอ |

### 4. `POST` ทุกตัวในกลุ่มไม่รับ `user_id` จาก body อีกต่อไป

| | |
| --- | --- |
| **ของเดิม** | สิบเอ็ด endpoint ที่เขียนแถวใหม่ (แฟ้ม, เจ็ดส่วนของประวัติ, ข้อมูลส่วนตัว, ทักษะ และ `/assign-work`) ประกาศ `user_id` เป็นฟิลด์**บังคับ**ใน body ส่งรหัสของใครไป แถวก็ถูกเขียนเป็นของคนนั้น |
| **ของใหม่** | ฟิลด์นี้หายจาก schema ของทั้งสิบเอ็ดตัว เจ้าของแถวมาจาก session ผู้เรียกที่ยังส่งมา**ไม่พัง** — zod ตัดคีย์ที่ไม่รู้จักทิ้ง ค่านั้นจึงถูกเมินเฉยๆ |
| **เหตุผล** | ฟิลด์ที่หายไปจาก schema ปฏิเสธได้ 100% โดยไม่บอกอะไรผู้โจมตีกลับไปเลย ส่วนการเทียบแล้วตอบ `403` จะกลายเป็นเครื่องมือให้ลองว่ารหัสไหนมีอยู่จริง และยังทำให้รูปของคำขอโกหกต่อไปว่า "ระบุเจ้าของได้" ทั้งที่ระบุไม่ได้แล้ว — `PUT` ของทั้งกลุ่มเป็นแบบนี้อยู่ก่อนแล้วตั้งแต่ข้อ 10 ของ #20 ข้อนี้แค่ทำให้ `POST` เหมือนกัน |
| **frontend** | **ต้องแก้ และแก้แล้วในการเปลี่ยนแปลงเดียวกัน** — `user_id` ถูกถอดออกจาก type ของคำขอสร้างทั้งเก้าไฟล์ใน `src/types/` และ `features/student/portfolio/types/`, ออกจาก `formData.append(...)` ของ service ทั้งเจ็ดตัวที่ส่งแบบ multipart และออกจาก payload ของฟอร์ม 10 ไฟล์กับหน้าเพิ่ม e-Portfolio อีกหนึ่ง ทั้งหมดเป็นการ**ลบฟิลด์ที่ไม่มีใครอ่านแล้ว** ไม่ได้เปลี่ยนสิ่งที่ผู้ใช้เห็น ส่วน `params: { user_id }` ของขารายการยังอยู่ครบตามข้อ 2 |

### 5. `POST /portfolio` และ `PUT`/`PATCH /portfolio/:id` ตอบ `403` เมื่อ `selectedSkillIds` มีทักษะของคนอื่น

| | |
| --- | --- |
| **ของเดิม** | `201`/`200` พร้อม mapping ที่ชี้ไปทักษะของนักศึกษาคนอื่น ชื่อทักษะของคนนั้นจึงขึ้นบนหน้าแฟ้มของผู้เรียก และไหลต่อไปถึง `GET /portfolio/public/:token` ที่ใครก็เปิดได้ |
| **ของใหม่** | `403` พร้อม `{ "success": false, "message": "มีทักษะบางรายการที่ไม่ใช่ของผู้ใช้รายนี้" }` และเพราะทั้งคำขอเป็น transaction เดียว ทักษะที่เป็นของผู้เรียกจริงในรายการเดียวกันก็ไม่ถูกผูกด้วย |
| **เหตุผล** | เป็นช่องที่ middleware มองไม่เห็น: แถวที่กำลังเขียนเป็นของผู้เรียกเอง สิ่งที่ไม่ใช่ของเขาคือ id ที่อยู่ในรายการ ซึ่ง id ของทักษะไม่ได้บอกว่าเป็นของใคร ใช้ถ้อยคำและสถานะเดียวกับที่ `/assign-work` ปฏิเสธด้วยเหตุผลเดียวกันอยู่แล้ว ไม่ประดิษฐ์คำที่สอง |
| **frontend** | ไม่ต้องแก้ — หน้าเพิ่มและหน้าแก้ไข e-Portfolio เลือกทักษะจากรายการที่ได้มาจาก `GET /portfolio-skill?user_id=` ของตัวเอง จึงเกิดขึ้นไม่ได้ในทางปฏิบัติ และทั้งสองหน้ามี `catch` ที่ขึ้นข้อความรวมอยู่แล้ว |

### 6. `POST /portfolio-skill/assign-work` วัดความเป็นเจ้าของจาก session

| | |
| --- | --- |
| **ของเดิม** | #17 ทำให้การปฏิเสธของ endpoint นี้เป็น `403` แทน `500` ไปแล้ว แต่ยัง**วัดจาก `user_id` ที่มากับ body** ซึ่งผู้เรียกเป็นคนเขียนเอง ใครที่ระบุรหัสของเจ้าของทักษะให้ตรงจึงผ่านการตรวจนั้นได้ทุกครั้ง |
| **ของใหม่** | `user_id` หายจาก body ตามข้อ 4 การตรวจวัดกับ session แทน — ผูกงานเข้ากับทักษะของคนอื่นได้ `403` เสมอ ไม่ว่าจะระบุรหัสของใครมา |
| **เหตุผล** | นี่คือสิ่งที่ **ขอบเขต** ของข้อ 1 ใน #17 เขียนค้างไว้ว่า "endpoint นี้ยังเชื่อ `user_id` ที่มากับ body อยู่เหมือนเดิม เรื่องนั้นค้างอยู่ที่ #31" การตรวจที่ผู้ถูกตรวจกรอกเงื่อนไขเองไม่ใช่การตรวจ |
| **frontend** | แก้แล้ว — `create-work-form.tsx` และ `work-edit-modal.tsx` เลิกส่ง `user_id` ทั้งใน `assignWorkToSkills` และ `createPortfolioSkill` |

### 7. คำขอที่ถูกปฏิเสธไม่มีไฟล์ไหลเข้า bucket

| | |
| --- | --- |
| **ของเดิม** | `upload` ของ multer เป็น middleware ตัวแรกของทุก `POST`/`PUT` ที่รับไฟล์ในกลุ่ม ไฟล์ของคำขอที่ไม่มีสิทธิ์จึงถูกเขียนลง MinIO จนครบก่อน แล้วค่อยไม่มีใครปฏิเสธมันเลย เพราะกลุ่มนี้ไม่มีการตรวจสิทธิ์อยู่แล้ว |
| **ของใหม่** | ลำดับของทุก router คือ `requireUser` → การตรวจเจ้าของ → `upload` → `validate` คำขอที่ไม่มี session หรือไม่ใช่เจ้าของจึงจบที่ `401`/`403` ก่อน multer อ่านไบต์แรก |
| **เหตุผล** | รูปแบบเดียวกับข้อ 2 ของ #12 และข้อ 1–2 ของ #13 ที่ย้าย `requireRole` ขึ้นหน้า `upload` ด้วยเหตุผลเดียวกัน ส่วน `validate` ยังอยู่หลัง `upload` เพราะฟิลด์ของ multipart body ไม่มีอยู่จนกว่า multer จะอ่านจาก stream — และ `requireSelf` เป็นข้อยกเว้นที่ต้องอยู่**หลัง** `validate` เพราะมันเทียบฟิลด์ที่ schema ประกาศไว้ คำขอที่ไม่ระบุผู้ใช้เลยคือคำขอที่ผิดรูป `400` จึงมีประโยชน์กับผู้เรียกมากกว่า `403` |
| **frontend** | ไม่ต้องแก้ |

### 8. รายการ id ทักษะที่ซ้ำกัน นับเป็นหนึ่งครั้ง ไม่ใช่ `500` หรือ `403`

| | |
| --- | --- |
| **ของเดิม** | `selectedSkillIds: [7, 7]` ของ `POST`/`PUT`/`PATCH /portfolio` ตอบ `500` เพราะ `portfolio_skill_mapping` มีคีย์หลักเป็นคู่ `(portfolio_id, skill_id)` การเขียนคู่เดิมสองครั้งจึงชนกันเอง ส่วน `skill_ids: [7, 7]` ของ `POST /portfolio-skill/assign-work` ตอบ `403` "มีทักษะบางรายการที่ไม่ใช่ของผู้ใช้รายนี้" เพราะการตรวจนับจำนวนแถวที่หาเจอเทียบกับความยาวของรายการที่ส่งมา — ทักษะเป็นของผู้เรียกทั้งคู่ แต่ 1 ไม่เท่ากับ 2 |
| **ของใหม่** | ทั้งสองตอบสำเร็จและผูกทักษะนั้นครั้งเดียว การตรวจความเป็นเจ้าของตัดรายการที่ซ้ำออกก่อน แล้วคืนรายการที่ไม่ซ้ำให้ผู้เรียกเขียน |
| **เหตุผล** | ระบุทักษะเดิมสองครั้งคือการผูกหนึ่งครั้ง ไม่ใช่สอง คำตอบเดิมทั้งสองแบบไม่ตรงกับสิ่งที่เกิดขึ้น — `500` บอกว่าเซิร์ฟเวอร์พัง และ `403` บอกว่าทักษะเป็นของคนอื่น ทั้งที่เป็นของผู้เรียกเอง พบตอนรวมการตรวจของสองที่ให้เหลือฟังก์ชันเดียว (D9) |
| **frontend** | ไม่ต้องแก้ — ทั้งสองหน้าเลือกทักษะจาก `Select` แบบหลายค่า ซึ่งไม่ให้เลือกค่าเดิมซ้ำอยู่แล้ว |

### รายการที่เคยเขียนไว้ว่า "ค้างไว้ที่ #31" และปิดแล้วในรอบนี้

- **กลุ่ม `portfolio` ทั้งกลุ่มไม่มีการตรวจสิทธิ์เลย** (#16) — ปิดครบทั้ง 56 endpoint ตามข้อ 1–3
- **ขอบเขตของข้อ 2 ใน #16** — `GET /portfolio-education` ที่ระบุ `user_id` ของคนอื่นเป็น `403` แล้ว ตามข้อ 2
- **ขอบเขตของข้อ 1 ใน #17** — `/assign-work` เลิกเชื่อ `user_id` จาก body ตามข้อ 6
- **`PUT /portfolio-education` เขียนทับคอลัมน์ `user_id` จาก body ได้** (#16, #20 ข้อ 10) — ส่วนที่ #20 เขียนไว้ว่า "เรื่องความเป็นเจ้าของยังเป็น #31" ปิดแล้ว
- **"การตรวจความเป็นเจ้าของยังเป็น #31"** ในรายการที่ pin ไว้ของ #20 — สำหรับกลุ่มนี้ปิดแล้ว

### สิ่งที่ **ไม่ได้** ทำใน ticket นี้

- **อาจารย์ยังเปิดแฟ้มของนักศึกษาไม่ได้** — ADR-0001 ตัดสินไว้ว่า "เจ้าของกับผู้ถือ
  share link เท่านั้น" เพราะกฎ "อาจารย์ที่สอนนักศึกษาคนนี้" ยังไม่มีในระบบเลยและ
  เป็นกฎเดียวกับที่ #30 ค้างอยู่ การเดากฎนั้นล่วงหน้าแล้ว #30 ตัดสินเป็นอย่างอื่น
  จะได้ระบบที่มีสองกฎ เมื่อ #30 เสร็จ ให้เอากฎนั้นมาเป็นสาขาที่สองใน `requireSelf`
  และ `requireOwnEntry` — จุดเดียว ไม่ต้องไล่แก้ router
- **`GET /user` และ `POST /student/submit/activity`** (#18) — เป็นคำถามความเป็น
  เจ้าของแบบเดียวกัน แต่อยู่นอก 10 router ที่ #31 รับผิดชอบ ยังไม่ได้แก้และยังไม่มี
  issue รองรับ สิ่งที่เปลี่ยนไปคือตอนนี้มี middleware ให้ใช้ซ้ำแล้ว — คนที่รับงานนั้น
  ไปทำไม่ต้องออกแบบอะไรใหม่ test ของทั้งสองยัง pin ไว้ตามที่เป็น
  *(ภายหลัง: `POST /student/submit/activity` ปิดใน #38 และ `GET /user` ปิดใน #40
  ทั้งคู่ใช้ middleware ที่ ticket นี้ทิ้งไว้ตามที่คาดไว้)*
- **`PUT` และ `DELETE` ที่อ้าง id ที่ไม่มีอยู่ยังตอบ `500`** — middleware ปล่อยแถว
  ที่ไม่มีอยู่จริงผ่านไปโดยตั้งใจ (ข้อ 3) การไปแตะเรื่องนี้ด้วยจะลาก pin ของ #16/#17
  เข้ามาใน ticket นี้โดยไม่จำเป็น

---

## #30 — อาจารย์เห็นเฉพาะกลุ่มเรียนที่ตัวเองสอน

คำถาม "อาจารย์คนไหนเห็น section ไหนได้" ค้างมาตั้งแต่ #12 และถูกอ้างถึงใน #14, #15
และ #31 ว่า "ระบบยังไม่มีการตรวจความเป็นเจ้าของ section ที่ไหนเลย" ticket นี้ตัดสิน
กติกานั้นและบังคับใช้ที่สามจุดแรก กติกา ทางเลือกที่ไม่ได้เลือก และรายการที่ยังไม่ได้
ทำ อยู่ที่ [ADR-0002](docs/adr/0002-section-access.md) ที่นี่บันทึกเฉพาะสิ่งที่
**ผู้เรียกเห็นต่างไป**

### 1. `/gradebook` ทั้งสอง endpoint ตอบ `403` เมื่อผู้เรียกไม่ได้สอนกลุ่มเรียนนั้น

| | |
| --- | --- |
| **ของเดิม** | #15 ทำให้ `GET /gradebook/per-student` และ `GET /gradebook/per-activity` ต้องเป็นอาจารย์ที่ล็อกอินแล้ว แต่**อาจารย์คนใดก็ได้ในระบบ** ก็ยังใส่ `section_id` ของกลุ่มเรียนที่ตัวเองไม่ได้สอนแล้วได้ `200` พร้อมรายชื่อนักศึกษาและคะแนนรายกิจกรรมทั้งกลุ่ม |
| **ของใหม่** | `403` พร้อม `{ "success": false, "message": "คุณไม่มีสิทธิ์เข้าถึงข้อมูลของกลุ่มเรียนนี้" }` เมื่อไม่มีแถวใน `course_sections_teacher` ที่จับคู่ `section_id` นั้นกับเจ้าของ session |
| **เหตุผล** | นี่คือสิ่งที่ **ขอบเขต** ของข้อ 4 ใน #15 เขียนค้างไว้ว่า "ปิดแค่ใครก็ได้เท่านั้น คำถามว่าอาจารย์คนไหนเห็น section ไหนได้ ยังไม่ได้ตอบ" — ดู ADR-0002 |
| **ขอบเขต** | `section_id` ที่**ไม่มีอยู่จริง**ก็ได้ `403` ประโยคเดียวกัน ไม่ใช่ `404` และไม่ใช่ `200` ที่ว่างเปล่า เพราะ `section_id` เป็นจำนวนเต็มเรียงกัน ถ้าคำตอบแยกได้ว่า "ไม่มี" กับ "ไม่ใช่ของคุณ" ผู้เรียกก็ไล่หมายเลขแล้ววาดแผนที่การสอนของทั้งสถาบันได้ |
| **ผลข้างเคียง** | คำขอที่ไม่ส่ง `section_id` หรือส่งมาผิดรูปยังได้ `400` เหมือนเดิม เพราะการตรวจอยู่**หลัง** `validate` |
| **frontend** | ไม่ต้องแก้ — หน้าสมุดคะแนนเดินมาจากรายวิชาที่อาจารย์คนนั้นสอน `section_id` ที่ส่งจึงเป็นของตัวเองเสมอ |

### 2. `POST /announcement` ตอบ `403` เมื่อผู้เรียกไม่ได้สอนกลุ่มเรียนนั้น

| | |
| --- | --- |
| **ของเดิม** | อาจารย์คนใดก็ได้โพสต์ประกาศลงกระดานข่าวของกลุ่มเรียนที่ตัวเองไม่ได้สอนได้ พร้อมไฟล์แนบ |
| **ของใหม่** | `403` ประโยคเดียวกับข้อ 1 และ **ไม่มีไฟล์ตกค้างใน bucket** เพราะ multer ของเส้นทางนี้เก็บไฟล์ไว้ในหน่วยความจำ ตัวที่เขียนลง MinIO คือ service ซึ่งอยู่หลังการปฏิเสธ |
| **เหตุผล** | กติกาเดียวกับข้อ 1 คนละ endpoint ลำดับ middleware เป็น `requireRole` → `upload` → `validate` → การตรวจเจ้าของ ด้วยเหตุผลเดียวกับข้อ 7 ของ #31 |
| **frontend** | ไม่ต้องแก้ — หน้าเพิ่มประกาศอ่าน `section_id` จากรายวิชาที่เลือกอยู่ใน `home-slice` |

### 3. `POST /announcement` ไม่รับ `created_by` จาก body อีกต่อไป

| | |
| --- | --- |
| **ของเดิม** | `created_by` เป็นฟิลด์**บังคับ**ใน body ส่งรหัสของอาจารย์คนไหนไป ประกาศก็ถูกบันทึกว่าเป็นของคนนั้น |
| **ของใหม่** | ฟิลด์นี้หายจาก schema ผู้เขียนมาจาก session เสมอ ผู้เรียกที่ยังส่งมา**ไม่พัง** — zod ตัดคีย์ที่ไม่รู้จักทิ้ง ค่านั้นจึงถูกเมินเฉยๆ |
| **เหตุผล** | กติกาเดียวกับข้อ 4 ของ #31 — "ผู้กระทำคือเจ้าของ session เสมอ ไม่ใช่คนที่คำขอบอกว่าเป็น" และเป็นสิ่งที่ pin ไว้ตั้งแต่ #12 ว่า "การแก้เฉพาะจุดนี้จุดเดียวไม่ได้ปิดอะไรจริง" ซึ่งไม่จริงอีกต่อไปเมื่อข้อ 2 ปิดทางเข้าไปแล้ว |
| **frontend** | **ต้องแก้ และแก้แล้วในการเปลี่ยนแปลงเดียวกัน** — `create-announcement-section.tsx` เลิก `formData.append("created_by", ...)` ค่าที่เคยส่งคือ `teacher_id` ของกลุ่มเรียน ซึ่งเป็นคนละคนกับผู้โพสต์ทันทีที่รายวิชามีอาจารย์มากกว่าหนึ่งคน |

### 4. `all_section` แผ่ประกาศไปเฉพาะกลุ่มเรียนที่ผู้โพสต์สอน

| | |
| --- | --- |
| **ของเดิม** | `all_section: true` โพสต์ลง**ทุกกลุ่มเรียนของรายวิชานั้น** โดยไม่สนว่าใครสอนกลุ่มไหน อาจารย์ที่สอนกลุ่มเดียวจึงเขียนลงกระดานข่าวของเพื่อนร่วมงานได้ด้วยคำขอเดียว |
| **ของใหม่** | ปลายทางคือกลุ่มเรียนของรายวิชานั้น **ที่ผู้โพสต์สอน** เท่านั้น กลุ่มเรียนของเพื่อนร่วมงานในรายวิชาเดียวกันไม่ได้รับประกาศ |
| **เหตุผล** | ถ้าไม่แก้ กติกาของข้อ 2 ก็ถูกข้ามในบรรทัดถัดมา — ตรวจ `section_id` แล้วแผ่ออกไปที่อื่นเอง ความหมายที่ถูกของ "ทุกกลุ่มเรียน" บนหน้าจอของอาจารย์คนหนึ่ง คือทุกกลุ่มเรียนที่เขาสอน |
| **ขอบเขต** | อาจารย์ที่สอนหลายกลุ่มในรายวิชาเดียวกันยังได้ครบทุกกลุ่มเหมือนเดิม สิ่งที่หายไปคือกลุ่มที่ไม่ใช่ของเขา |
| **frontend** | ไม่ต้องแก้ — checkbox "เพิ่มประกาศในทุกกลุ่มเรียน" ยังส่งค่าเดิม เปลี่ยนแค่ความหมายของคำว่าทุก |

### รายการที่เคยเขียนไว้ว่า "ค้างไว้ที่ #30" และปิดแล้วในรอบนี้

- **`/gradebook` ยังไม่ตรวจว่าอาจารย์คนนั้นสอน section นั้นจริง** (#15) — ปิดตามข้อ 1
- **`created_by` ของ `POST /announcement` ยังมาจาก body ไม่ใช่ session** (#12) — ปิดตามข้อ 3
- **ขอบเขตของข้อ 4 ใน #15** — คำถาม "อาจารย์คนไหนเห็น section ไหนได้" ตัดสินแล้วใน ADR-0002

### สิ่งที่ **ไม่ได้** ทำใน ticket นี้

- **สาม `GET` ของกลุ่ม (#26) ยังไม่ใช้กติกานี้** (ปิดแล้วในหัวข้อ #26 ข้างล่าง) —
  เกณฑ์ยอมรับของ #30 เปิดทางไว้ว่า
  "ใช้กติกาเดียวกัน หรือมีเหตุผลบันทึกไว้ว่าทำไมต่าง" เหตุผลอยู่ใน ADR-0002 โดยย่อคือ
  ผู้เรียกที่ควรได้คำตอบมีสองแบบ — อาจารย์ของกลุ่มเรียน กับนักศึกษาที่ลงทะเบียนอยู่ —
  กติกานี้ครอบคลุมแค่ครึ่งแรก และ #26 ยังต้องตัดสินก่อนว่า `student_id` มาจาก session
  หรือจาก query ซึ่งเป็นการเปลี่ยนสัญญากับ frontend ที่ควรทำรวดเดียวทั้งสามตัว
- **endpoint ของอาจารย์อีก 9 ตัวที่ระบุ `section_id` ยังไม่ได้ตรวจ** —
  `POST`/`PUT /activity`, `POST`/`PUT /learning-activity`, `POST /lesson-plan`,
  `POST /score-weight`, `POST /course-material`, `POST /course/schedule` และ
  `POST /course/clo` ทั้งหมดใช้ middleware ตัวเดียวกันนี้ได้ทันที รายการอยู่ใน
  ADR-0002
- **`GET` ของฝั่งรายวิชายังไม่มีแม้แต่ session** — `GET /announcement`,
  `GET /course`, `GET /activity/list`, `GET /lesson-plan`, `GET /score-weight`,
  `GET /course-material` เรียกได้โดยไม่ต้องล็อกอิน เป็นช่องที่กว้างกว่าเรื่องความ
  เป็นเจ้าของ และอยู่นอกเกณฑ์ยอมรับของ #30
- **อาจารย์ยังเปิดแฟ้ม e-Portfolio ของนักศึกษาไม่ได้** — #31 เขียนไว้ว่ารอกติกาของ
  #30 ตอนนี้มีกติกาแล้ว แต่การเชื่อมสองเรื่องเข้าด้วยกันต้องเดินจากนักศึกษาไปหา
  กลุ่มเรียนผ่าน `student_course` ซึ่งเป็นการตัดสินว่า "อาจารย์ที่สอนคนนี้อยู่"
  หมายถึงอะไร — คนละคำถามกับที่ ADR-0002 ตอบ และไม่มีใครขอ

---

## #26 — การอ่านกลุ่มเป็นเรื่องของนักศึกษาที่ล็อกอินอยู่

#30 ตัดสินว่าอาจารย์คนไหนเข้าถึงกลุ่มเรียนไหนได้ และเขียนค้างไว้ว่าฝั่งนักศึกษา
ยังไม่มีที่ไหนตรวจเลย ticket นี้ปิดครึ่งหลังนั้น กติกา ทางเลือกที่ไม่ได้เลือก และ
รายการที่ยังไม่ได้ทำ อยู่ที่
[ADR-0003](docs/adr/0003-enrolment-access.md) ที่นี่บันทึกเฉพาะสิ่งที่
**ผู้เรียกเห็นต่างไป**

การเปลี่ยนแปลงทุกข้อข้างล่างเกิดกับ **ทั้งสอง router พร้อมกัน** —
`/student-activity-group` และ `/student-learning-activity-group` — เพราะทั้งคู่มี
สาม `GET` รูปเดียวกันเป๊ะ ต่างกันแค่ชื่อ id ของชิ้นงาน

### 1. `student_id` หายไปจาก query ของ `GET /` และ `GET /all`

| | |
| --- | --- |
| **ของเดิม** | `student_id` เป็นฟิลด์**บังคับ**ใน query (ตั้งแต่ #20) ใครก็ได้ที่รู้รหัสนักศึกษาแปดหลักของเพื่อน — ซึ่งเพื่อนร่วมชั้นรู้กันหมด — เรียกแล้วได้กลุ่มของคนนั้นพร้อมรายชื่อสมาชิกทั้งกลุ่ม |
| **ของใหม่** | ฟิลด์นี้หายจาก schema คำตอบเป็นของเจ้าของ session เสมอ ผู้เรียกที่ยังส่งมา**ไม่พัง** — zod ตัดคีย์ที่ไม่รู้จักทิ้ง คำขอที่ระบุรหัสของเพื่อนจึงได้กลุ่ม**ของตัวเอง**กลับไป ไม่ใช่ `403` เพราะไม่มีพารามิเตอร์ให้ปฏิเสธแล้ว |
| **เหตุผล** | กติกาเดียวกับข้อ 4 ของ #31 และข้อ 3 ของ #30 — "ผู้กระทำคือเจ้าของ session เสมอ ไม่ใช่คนที่คำขอบอกว่าเป็น" ดู ADR-0003 ว่าทำไมจึงตัดฟิลด์ทิ้ง แทนที่จะเก็บไว้แล้วตรวจว่าตรงกับ session |
| **ขอบเขต** | `activity_id` / `learning_activity_id` ของ `GET /` และ `section_id` ของ `GET /all` ยังบังคับเหมือนเดิม ไม่ส่งมายังได้ `400` เหมือนเดิม |
| **frontend** | **ต้องแก้ และแก้แล้วในการเปลี่ยนแปลงเดียวกัน** — `student_id` ออกจาก `GetStudentActivityGroupParams`, `GetStudentActivityGroupInSecParams` และ `GetStudentLearningActivityGroupParams` ใน `course-type.ts` ผู้เรียกทั้งสามที่ (`submit-classwork-section.tsx`, `create-groupwork-section.tsx` สองครั้ง) เลิกเติมค่านั้น ทุกที่เคยส่งรหัสของคนที่ล็อกอินอยู่แล้ว หน้าจอจึงไม่เปลี่ยน |

### 2. `GET /without-group` ตอบ `403` เมื่อผู้เรียกไม่ได้ลงทะเบียนกลุ่มเรียนนั้น

| | |
| --- | --- |
| **ของเดิม** | เรียกได้**โดยไม่ต้องล็อกอิน** ใส่ `section_id` กับ `activity_id` แล้วได้รายชื่อพร้อมชื่อ-นามสกุลจริงของนักศึกษาทุกคนในกลุ่มเรียนที่ยังไม่มีกลุ่ม |
| **ของใหม่** | `403` พร้อม `{ "success": false, "message": "คุณไม่มีสิทธิ์เข้าถึงข้อมูลของกลุ่มเรียนนี้" }` เมื่อไม่มีแถวใน `student_course` ที่จับคู่ `section_id` นั้นกับเจ้าของ session |
| **เหตุผล** | endpoint นี้ระบุกลุ่มเรียนแต่**ไม่ระบุนักศึกษา** session จึงไม่ได้แคบคำตอบเลย ต่างจากข้อ 1 ที่ session แคบพอแล้ว — ดู ADR-0003 |
| **ขอบเขต** | `section_id` ที่**ไม่มีอยู่จริง**ก็ได้ `403` ประโยคเดียวกัน ด้วยเหตุผลเดียวกับข้อ 1 ของ #30 ทุกประการ และคำขอที่ไม่ส่ง `section_id` ยังได้ `400` เพราะการตรวจอยู่**หลัง** `validate` |
| **frontend** | ไม่ต้องแก้ — หน้าสร้างกลุ่มเดินมาจากงานในรายวิชาที่นักศึกษาคนนั้นเรียนอยู่ `section_id` ที่ส่งจึงเป็นของตัวเองเสมอ |

### 3. ทั้งหก `GET` ต้องล็อกอินเป็นนักศึกษา

| | |
| --- | --- |
| **ของเดิม** | ไม่มี middleware อะไรเลยนอกจาก `validate` เรียกได้โดยไม่ต้องมี cookie |
| **ของใหม่** | ไม่มี session ได้ `401` พร้อม `"ไม่พบ Token หรือ Token หมดอายุ"` เป็นอาจารย์ได้ `403` พร้อม `"สิทธิ์การเข้าถึงเฉพาะนักศึกษาเท่านั้น"` — ชุดเดียวกับ `POST`/`PATCH` ของ router เดียวกันที่มีอยู่แล้ว |
| **เหตุผล** | ข้อ 1 อ่าน `student_id` จาก session จึงต้องมี session ให้อ่าน และไม่มีหน้าไหนของอาจารย์ใน `apps/web` เรียกหกตัวนี้ — ผู้เรียกทุกตัวอยู่ใต้ `features/student/` |
| **frontend** | ไม่ต้องแก้ — cookie ถูกแนบไปกับทุกคำขออยู่แล้ว |

### รายการที่เคยเขียนไว้ว่า "ค้างไว้ที่ #26" และปิดแล้วในรอบนี้

- **สาม `GET` ของกลุ่มรับ `student_id` จาก query โดยไม่ตรวจความเป็นเจ้าของ** (#14) —
  ปิดตามข้อ 1 และข้อ 3
- **สาม `GET` ของกลุ่มยังไม่ใช้กติกาของ #30** — ตัดสินแล้วใน ADR-0003 ว่าใช้กติกา
  คู่ขนานที่เดินผ่าน `student_course` แทน `course_sections_teacher`

### หมายเหตุเรื่องเกณฑ์ยอมรับข้อแรกของ #26

เกณฑ์ข้อแรกเขียนไว้ว่า "เรียกโดยไม่ส่ง `student_id` แล้วได้ `400` ไม่ใช่รายการกลุ่ม
ของทั้ง section" ซึ่ง **#20 ทำไปแล้ว** และตอนนี้ข้อ 1 ข้างบนทำให้ไม่จริงอีกต่อไปโดย
ตั้งใจ — คำขอที่ไม่ส่ง `student_id` ได้ `200` พร้อมกลุ่มของตัวเอง เพราะฟิลด์นั้นไม่ใช่
พารามิเตอร์แล้ว สิ่งที่เกณฑ์ข้อนี้ต้องการจริง ๆ คือ "ไม่คายรายการกลุ่มของทั้ง section"
ซึ่งยังจริงอยู่และแน่นกว่าเดิม

### สิ่งที่ **ไม่ได้** ทำใน ticket นี้

- **`POST`/`PATCH` ของกลุ่มยังไม่ตรวจอะไรเลย** — ทั้งคู่รับ `members` มาทั้งก้อนโดย
  ไม่ตรวจว่าผู้เรียกอยู่ในนั้น ไม่ตรวจว่าเป็น `LEADER` และไม่ตรวจว่าสมาชิกที่ระบุ
  ลงทะเบียนกลุ่มเรียนเดียวกัน นักศึกษาจึงยังจับเพื่อนเข้ากลุ่มหรือรื้อกลุ่มของคนอื่น
  ได้ อยู่ที่ [#27](https://github.com/khthana/Deep-Portfolio/issues/27) ซึ่งใช้
  `student_course` ตัวเดียวกันนี้ตอบได้ — **ปิดไปครึ่งหนึ่งแล้ว** ดูหัวข้อ #27
  ข้างล่าง `PATCH` ต้องเป็นหัวหน้ากลุ่ม ส่วน `POST` และการตรวจว่าสมาชิกลงทะเบียน
  กลุ่มเรียนเดียวกัน ค้างไว้ที่ #37
- **อาจารย์ยังดูกลุ่มของทั้งกลุ่มเรียนไม่ได้** — ไม่ใช่การผ่อนเงื่อนไขของหกตัวนี้
  แต่เป็น endpoint คนละตัวที่คำตอบคนละรูป (ทุกกลุ่ม ไม่ใช่กลุ่มของคนถาม) และไม่มี
  หน้าไหนขอ

---

## #27 — การแก้รายชื่อกลุ่มเป็นสิทธิ์ของหัวหน้ากลุ่ม

#26 ปิดฝั่ง **อ่าน** ของกลุ่มไปแล้ว และเขียนค้างไว้ว่าฝั่ง **เขียน** ยังไม่ตรวจ
อะไรเลย ticket นี้ปิดสองข้อของฝั่งเขียน คือใครแก้รายชื่อได้ และกลุ่มถูกยุบอย่างไร
กติกา ทางเลือกที่ไม่ได้เลือก และรายการที่ยังไม่ได้ทำ อยู่ที่
[ADR-0004](docs/adr/0004-group-leader.md) ที่นี่บันทึกเฉพาะสิ่งที่
**ผู้เรียกเห็นต่างไป**

ทุกข้อข้างล่างเกิดกับ **ทั้งสอง router พร้อมกัน** — `/student-activity-group` และ
`/student-learning-activity-group` — เพราะรูปเหมือนกันทุกประการ

### 1. `PATCH` ตอบ `403` เมื่อผู้เรียกไม่ใช่หัวหน้ากลุ่ม

| | |
| --- | --- |
| **ของเดิม** | มีแค่ `requireRole("STUDENT")` นักศึกษาคนไหนก็ได้ที่รู้ `group_id` — ซึ่งเป็นจำนวนเต็มเรียงกัน — เขียนรายชื่อทับได้ทั้งกลุ่ม จะเตะหัวหน้าออก ตั้งตัวเองเป็นหัวหน้า หรือรื้อกลุ่มของคนที่ไม่รู้จักก็ได้ |
| **ของใหม่** | `403` พร้อม `{ "success": false, "message": "เฉพาะหัวหน้ากลุ่มเท่านั้นที่แก้ไขกลุ่มได้" }` เมื่อเจ้าของ session ไม่ใช่แถวที่ `role = LEADER` ของกลุ่มนั้น |
| **เหตุผล** | `members` มาทั้งก้อนและถูกเขียนทับทั้งกลุ่ม ใครเขียนได้ก็ยึดกลุ่มได้ — ดู ADR-0004 ว่าทำไมจึงเป็น `LEADER` ไม่ใช่สมาชิกที่ตอบรับแล้วทุกคน |
| **ขอบเขต** | `group_id` ที่**ไม่มีอยู่จริง**ได้ `403` ประโยคเดียวกัน **จากเดิมที่เป็น `500`** ด้วยเหตุผลของ #30 ทุกข้อ และคำขอที่ไม่ส่ง `group_id` ยังได้ `400` เพราะการตรวจอยู่**หลัง** `validate` |
| **frontend** | **ต้องแก้ และแก้แล้วในการเปลี่ยนแปลงเดียวกัน** — ปุ่ม "แก้ไข" ใน `show-groupwork-modal.tsx` เคยขึ้นให้สมาชิกทุกคน ตอนนี้ขึ้นเฉพาะหัวหน้า เทียบจาก `state.home.studentId` กับรายชื่อที่ modal ถืออยู่ ถ้าไม่แก้ สมาชิกจะเปิดฟอร์มได้แล้วไปเจอ error ตอนกดบันทึก |

### 2. `members: []` ถูกปฏิเสธ กลุ่มยังอยู่ครบ

| | |
| --- | --- |
| **ของเดิม** | `200` และกลุ่มเหลือแถวลอยที่ไม่มีสมาชิกเลย เพราะ service ลบสมาชิกทั้งหมดก่อนแล้วค่อยเขียนรายการที่ได้รับกลับลงไป ไม่มี endpoint ไหนพากลุ่มนั้นกลับมาได้อีก เพราะทุกตัวหาจากสมาชิก |
| **ของใหม่** | `400` พร้อม `errors: [{ field: "members", location: "body", message: "ต้องมีอย่างน้อย 1 รายการ" }]` สมาชิกเดิมอยู่ครบ |
| **เหตุผล** | การยุบกลุ่มมี endpoint ของตัวเองแล้ว (ข้อ 4) คำขอที่ส่งรายชื่อว่างมาโดยไม่ตั้งใจไม่ควรลบกลุ่มทิ้ง |
| **ขอบเขต** | ใช้กับ `POST` ด้วย — สร้างกลุ่มเปล่าไม่ได้แล้วเช่นกัน |
| **frontend** | ไม่ต้องแก้ — modal ไม่ยอมให้ถอดหัวหน้าออกจากรายชื่อ รายการที่ส่งจึงมีอย่างน้อยหนึ่งคนเสมอ |

### 3. รายชื่อต้องมีหัวหน้าหนึ่งคนพอดี

| | |
| --- | --- |
| **ของเดิม** | ไม่ตรวจ รายชื่อที่ไม่มี `LEADER` เลยสร้างกลุ่มได้ และ `created_by` ของกลุ่มนั้นถูกเขียนเป็นสตริงว่าง ส่วนรายชื่อที่มีสองหัวหน้าก็ผ่านเหมือนกัน |
| **ของใหม่** | `400` พร้อม `errors: [{ field: "members", location: "body", message: "ต้องมีหัวหน้ากลุ่มหนึ่งคน" }]` ทั้ง `POST` และ `PATCH` |
| **เหตุผล** | กลุ่มที่ไม่มีหัวหน้าคือกลุ่มที่ไม่มีใครแก้ได้อีกภายใต้ข้อ 1 ส่วนสองหัวหน้าคือสองคนที่เขียนทับกันได้ |
| **ขอบเขต** | ตรวจที่ schema ไม่ใช่ middleware จึงมาก่อนการตรวจสิทธิ์ คำขอที่ผิดทั้งรูปและสิทธิ์ได้ `400` ไม่ใช่ `403` |
| **frontend** | ไม่ต้องแก้ — `create-groupwork-modal.tsx` ใส่คนที่ล็อกอินเป็น `LEADER` ให้เสมอ และ modal แก้ไขก็ส่งรายชื่อเดิมที่มีหัวหน้าอยู่แล้วกลับไป |

### 4. เพิ่ม `DELETE /student-activity-group/:group_id` และคู่แฝด

| | |
| --- | --- |
| **ของเดิม** | ไม่มี endpoint ยุบกลุ่ม ทางเดียวคือส่ง `members: []` ซึ่งไม่ได้ลบกลุ่มจริง แค่ทิ้งแถวว่างไว้ |
| **ของใหม่** | `200` พร้อม `{ "success": true, "data": null, "message": "Delete group successfully" }` แถวกลุ่มถูกลบ สมาชิกหายไปตาม cascade **แถวการส่งงานของแต่ละคนไม่ถูกลบ** |
| **เหตุผล** | ยุบกลุ่มเป็นคนละเจตนากับแก้รายชื่อ จึงควรบอกให้ชัดในตัว endpoint เอง ส่วนงานที่ส่งไปแล้วเป็นของนักศึกษาแต่ละคน กติกาเดียวกับตอนหัวหน้าถอดสมาชิกออก |
| **ขอบเขต** | ตรวจสิทธิ์ด้วยกติกาข้อ 1 ตัวเดียวกัน — ไม่ใช่หัวหน้าหรือไม่มีกลุ่มนั้นได้ `403` ประโยคเดียวกัน ไม่มี session ได้ `401` เป็นอาจารย์ได้ `403` ของบทบาท และ `group_id` ที่ไม่ใช่ตัวเลขได้ `400` |
| **frontend** | ไม่ต้องแก้ — ยังไม่มีปุ่มยุบกลุ่มในหน้าไหน และของเดิมก็ยุบจากหน้าจอไม่ได้อยู่แล้ว |

### รายการที่เคยเขียนไว้ว่า "ค้างไว้ที่ #27" และปิดแล้วในรอบนี้

- **`PATCH` กลุ่มที่ส่ง `members: []` ทำให้กลุ่มว่างเปล่าแบบกู้คืนไม่ได้** (#14) —
  ปิดตามข้อ 2 ส่วนการตรวจ `LEADER` ที่บันทึกไว้ในย่อหน้าเดียวกัน ปิดตามข้อ 1
- **`POST`/`PATCH` ของกลุ่มยังไม่ตรวจอะไรเลย** (#26) — ปิดเฉพาะส่วน `PATCH` ที่เหลือ
  อยู่ในรายการข้างล่าง

### สิ่งที่ **ไม่ได้** ทำใน ticket นี้

- **`POST` ยังไม่ตรวจว่าผู้เรียกอยู่ในรายชื่อที่ตัวเองส่ง** — นักศึกษายังสร้างกลุ่ม
  โดยตั้งคนอื่นเป็นหัวหน้าแล้วไม่ใส่ตัวเองลงไปได้ ซึ่งเป็นการสร้างกลุ่มในนามคนอื่น
  ตอนสร้างยังไม่มีกลุ่มให้ตรวจว่าใครเป็นหัวหน้า จึงเป็นคำถามคนละข้อกับข้อ 1 และต้อง
  ตัดสินเพิ่มว่าผู้สร้างต้องเป็นหัวหน้าเสมอหรือแค่ต้องอยู่ในรายชื่อ อยู่ที่
  [#37](https://github.com/khthana/Deep-Portfolio/issues/37)
- **ยังไม่ตรวจว่าสมาชิกที่ระบุลงทะเบียนกลุ่มเรียนเดียวกับชิ้นงาน** — หัวหน้ายังดึง
  คนนอกชั้นเรียนเข้ากลุ่มได้ ทั้ง `POST` และ `PATCH` และระบบจะสร้างแถวส่งงานให้คน
  นั้นด้วย อยู่ที่ #37 เหมือนกัน
- **ยังไม่ดูสถานะการส่งงานของกลุ่ม** — หัวหน้ายุบกลุ่มที่ส่งงานหรือถูกให้คะแนนไปแล้ว
  ได้ เช่นเดียวกับที่ `PATCH` แก้รายชื่อของกลุ่มแบบนั้นได้อยู่ก่อนแล้ว หน้าจอซ่อนปุ่ม
  "แก้ไข" เมื่อ `status === "GRADED"` แต่ API ไม่ได้บังคับ เป็นช่องเดิมที่ `DELETE`
  รับมาด้วย ไม่ใช่ของใหม่ และการตัดสินว่างานที่ส่งแล้วยังแก้กลุ่มได้แค่ไหนเป็นคำถาม
  ของตัวเอง จึงไม่รวมในเกณฑ์ยอมรับของ #27
- **การเปลี่ยนหัวหน้ายังทำได้ทางเดียว** — หัวหน้าคนเดิมส่งรายชื่อที่ยก `LEADER` ให้
  คนอื่น ถ้าหัวหน้าหายไปเฉย ๆ กลุ่มนั้นก็ไม่มีใครแก้ได้ ADR-0004 บอกไว้ว่าทางออก
  คือ endpoint ของอาจารย์ ไม่ใช่การผ่อนกติกาข้อ 1

---

## #28 — สถิติของกิจกรรมที่ยังไม่มีใครถูกตรวจเป็น `null` ไม่ใช่ `0`

#15 บันทึกไว้ว่า max/min/mean ตกไปที่ `0` เมื่อยังไม่มีใครถูกตรวจ ทำให้กิจกรรมที่
ทั้งห้องได้ 0 จริง ๆ กับกิจกรรมที่อาจารย์ยังไม่ได้เปิดตรวจ ส่งค่าเดียวกันออกมา
ticket นี้แยกสองอย่างนั้นออกจากกัน

### 1. `GET /gradebook/per-activity` ส่ง `null` เมื่อไม่มีคะแนนให้คำนวณ

| | |
| --- | --- |
| **ของเดิม** | `max_score` `min_score` `mean_score` เป็น `0` เมื่อไม่มีใครในกลุ่มเรียนถูกตรวจ ซึ่งเป็นค่าเดียวกับที่ส่งเมื่อทุกคนได้ 0 จริง |
| **ของใหม่** | ทั้งสามค่าเป็น `null` เมื่อยังไม่มีคะแนนสักคน และเป็นตัวเลขตามเดิมเมื่อมีคะแนนแล้ว — รวมถึง `0` เมื่อคะแนนที่ตรวจแล้วเป็น 0 |
| **เหตุผล** | ค่าสูงสุด ต่ำสุด และเฉลี่ยของ "ไม่มีคะแนน" ไม่ใช่ 0 แต่คือไม่มี ส่วน 0 เป็นคะแนนที่ได้จริงได้ ผู้เรียกเคยแยกสองกรณีนี้ได้ทางเดียวคือดู `graded_count` ประกอบ ซึ่งหน้าเว็บไม่ได้ดู |
| **ขอบเขต** | `submitted_count` `not_submitted_count` `graded_count` `full_score` ไม่เปลี่ยน — งานที่ส่งมารอตรวจเป็นข้อเท็จจริงอยู่แล้วไม่ว่าจะตรวจหรือยัง |
| **frontend** | type `ActivityData` ใน `gradebook-type.type.ts` และแถวตาราง `GradebookPerActivityDataType` เป็น `number \| null` แล้ว และคอลัมน์ Max/Min/Mean ของสมุดคะแนนอาจารย์แสดง `-` |

### 2. `GET /evaluation/list` ส่งต่อค่าชุดเดียวกัน

| | |
| --- | --- |
| **ของเดิม** | นักศึกษาเห็น max/min/mean เป็น `0` ก่อนที่งานจะถูกตรวจ เพราะ endpoint นี้สร้างต่อจาก `/gradebook/per-activity` |
| **ของใหม่** | เห็น `null` และตารางแสดง `-` จนกว่าจะมีคนถูกตรวจ |
| **เหตุผล** | เป็นค่าชุดเดียวกัน ไม่ได้แก้ที่ endpoint นี้เลย |
| **ขอบเขต** | `StudentEvaluationData` เขียนว่าทุกแถวมีคะแนนและสถิติครบ แต่แถวของงานในชั้นเรียนไม่เคยมี field เหล่านั้นเลย (`as StudentEvaluationData` ปิดไว้) field ที่แถวงานในชั้นเรียนไม่มีจึงเปลี่ยนเป็น optional และตัด cast ทิ้ง ตัว response ไม่เปลี่ยน — เปลี่ยนแต่ type ให้ตรงกับที่ส่งจริง ตามเกณฑ์ข้อ 2 ของ ticket |
| **frontend** | `course-type.ts` เปลี่ยน optional ตามให้ตรงกัน และ `evaluation-table.tsx` ยุบ `undefined` (งานในชั้นเรียน) กับ `null` (ยังไม่มีใครถูกตรวจ) ให้เป็น `null` ก่อนลงตาราง เพราะตารางวาด `-` เหมือนกันทั้งคู่ ส่วนการ render ดูข้อ 3 |

### 3. ตารางคะแนนแสดง `0` เป็น `0` ไม่ใช่ `-`

| | |
| --- | --- |
| **ของเดิม** | ทุกคอลัมน์คะแนนใน `evaluation-column.tsx` เขียน `text ? text : "-"` คะแนน 0 จึงแสดงเป็น `-` เหมือนกับไม่มีคะแนน ส่วนคอลัมน์ Max/Min/Mean ของอาจารย์ไม่มี `render` เลย ค่า `null` จะกลายเป็นช่องว่าง |
| **ของใหม่** | ทั้งสามตารางเรียก `formatScore` ตัวเดียวกัน — `null`/`undefined` แสดง `-` ตัวเลขแสดงตามจริงรวมทั้ง `0` |
| **เหตุผล** | ถ้ายังใช้ `text ? text : "-"` การแยก `null` ออกจาก `0` ที่เพิ่งทำมาถึง wire ก็หายไปตอน render พอดี |
| **ขอบเขต** | คอลัมน์คะแนนของสามตาราง — เพิ่มคอลัมน์ "คะแนน" ในตารางงานของนักศึกษาฝั่งอาจารย์ (`student-work-column.tsx`) ที่มีอาการเดียวกันคือคะแนน 0 แสดงเป็น `-` ส่วนคอลัมน์นับจำนวน (ส่งแล้ว/ประเมินแล้ว/ยังไม่ส่ง) ยังแสดง `-` เมื่อเป็น 0 ตามเดิม ค่าเหล่านั้นไม่เคยเป็น `null` จึงไม่มีสองความหมายให้สับสนบน wire การจะแสดงเป็น `0` หรือ `-` เป็นเรื่องของหน้าจอล้วน ๆ และอยู่นอกเกณฑ์ยอมรับของ ticket นี้ |
| **frontend** | `apps/web/src/utils/format-score.ts` เป็น pure function มี test ข้าง ๆ ตาม seam ของ web |

### รายการที่เคยเขียนไว้ว่า "แยกไปเป็น #28" และปิดแล้วในรอบนี้

- **สถิติของกิจกรรมที่ยังไม่มีใครถูกตรวจเป็น `0` ไม่ใช่ `null`** (#15) — ปิดตามข้อ 1
  และข้อ 2

---

## #29 — งานที่ไม่มีวันประกาศ ถือว่าประกาศแล้ว

การตัดสินใจอยู่ใน [ADR-0005](docs/adr/0005-announcement-date.md) โค้ดที่เปลี่ยนมี
บรรทัดเดียว — ฟังก์ชันคืน `true` แทน `false` เมื่อ `announcement_date` เป็น
`null` — แต่ฟังก์ชันนี้เป็นประตูของทุกหน้าจอฝั่งนักศึกษา ผลจึงกระจายไป 5 route

### 1. งานที่ไม่มีวันประกาศปรากฏให้นักศึกษาเห็นทุกหน้าจอ

| | |
| --- | --- |
| **ของเดิม** | `announcement_date` ที่เป็น `null` อ่านว่า "ยังไม่ถึงวันประกาศ" งานชิ้นนั้นจึงไม่ปรากฏใน `GET /student/calendar`, `GET /lesson-plan/student`, `GET /student/classwork/list`, `GET /student/all/classwork/list` และ `GET /evaluation/list` เลย ไม่ว่าเวลาจะผ่านไปนานแค่ไหนหรือตรวจเสร็จแล้วหรือยัง |
| **ของใหม่** | `null` อ่านว่า "ประกาศแล้ว" งานชิ้นนั้นปรากฏในทั้งห้า route ตั้งแต่แรก พร้อมคะแนนของตัวเองถ้าตรวจแล้ว |
| **เหตุผล** | ช่องนี้เป็น optional ทุกที่ที่เขียนลงไป (`optionalDate` ทั้งสอง schema และคอลัมน์ nullable) การไม่กรอกจึงไม่ใช่คำสั่งให้ซ่อน แต่คือการไม่ได้สั่งอะไร และผลของการไม่สั่งอะไรไม่ควรเป็นการซ่อนถาวรที่กู้คืนเองไม่ได้ เหตุผลเต็มอยู่ใน ADR |
| **ขอบเขต** | ใช้ความหมายเดียวกันทั้งฝั่ง activity และ learning activity เพราะทั้งคู่เรียกฟังก์ชันตัวเดียวกัน การซ่อนงานด้วยการตั้งวันประกาศไว้ในอนาคตยังทำได้เหมือนเดิมทุกประการ — ที่หายไปคือการซ่อนโดยไม่ได้ตั้งใจ ส่วน `optionalDate` ใน schema ไม่เปลี่ยน `POST`/`PATCH` ที่ไม่ส่ง field นี้ยังได้ `200` ตามเดิม ฟังก์ชันเปลี่ยนชื่อจาก `checkIsOverAnnouncementDate` เป็น `isAnnounced` (`apps/api/src/utils/is-announced.ts`) เป็นเรื่องภายใน ผู้เรียกไม่เห็นอะไร |
| **frontend** | ไม่ต้องแก้ — form ทั้งสองฝั่ง (`activity-detail-form.tsx:226`, `learning-activity-detail-form.tsx:192`) ตั้ง `required: true` ที่ช่อง "วันประกาศ" อยู่แล้ว งานที่สร้างผ่านหน้าจอจึงมีวันเสมอ และหน้าจอฝั่งอาจารย์ไม่เคยผ่านประตูนี้อยู่แล้ว ที่นักศึกษาจะเห็นต่างไปคือรายการยาวขึ้นเมื่อฐานข้อมูลมีแถวที่ค่าว่าง |

### รายการที่เคยเขียนไว้ว่า "แยกไปเป็น #29" และปิดแล้วในรอบนี้

- **งานที่ไม่เคยกรอกวันประกาศผล ไม่ถูกแสดงให้นักศึกษาเลย** (#15) — ปิดตามข้อ 1
  ข้างบน ตอนบันทึกไว้ครั้งแรกเขียนถึงแต่ `GET /evaluation/list` แต่ประตูเดียวกัน
  กั้นอีกสามหน้าจอด้วย

---

## #25 — `PUT /activity` เก็บคะแนนที่ตรวจไปแล้วไว้

เป็นการเปลี่ยน**สัญญาระหว่าง frontend กับ API** ไม่ใช่แค่การแก้ service — เพราะ
API ไม่มีทางรู้ว่าเกณฑ์ไหนคือเกณฑ์เดิม ถ้า frontend ไม่ส่ง `id` กลับมา

### 1. เกณฑ์ที่ส่ง `id` กลับมาถูกเขียนทับที่เดิม ไม่ใช่ลบแล้วสร้างใหม่

| | |
| --- | --- |
| **ของเดิม** | ทุกครั้งที่บันทึก route นี้ลบ `rubric_activity_mapping` ของกิจกรรมทั้งชุดแล้วสร้างใหม่จากรายการที่ส่งมา `student_activity_rubric_score` มี `ON DELETE CASCADE` อยู่ข้างหลัง คะแนนรายเกณฑ์ของนักศึกษาทุกคนจึงหายทุกครั้ง แม้จะแก้แค่กำหนดส่งหรือชื่อกิจกรรม |
| **ของใหม่** | เกณฑ์แต่ละรายการใน `rubric` รับ `id` เพิ่มมาได้ มี `id` = เกณฑ์เดิม เขียนทับที่เดิม (คะแนนอยู่ครบ) ไม่มี `id` = เกณฑ์ใหม่ สร้างเพิ่ม `id` เดิมที่ไม่ถูกส่งกลับมา = เกณฑ์ที่ถูกลบ ลบทิ้งพร้อมคะแนนของเกณฑ์นั้น ส่วนช่วงคะแนน (`rubric_levels`) จับคู่ด้วย `level_no` ซึ่ง unique อยู่แล้วในแต่ละเกณฑ์ แถวที่ยังอยู่จึงคง `id` เดิมไว้ |
| **เหตุผล** | การให้คะแนนเขียน `rubric_activity_mapping_id` และ `rubric_level_id` ลงไปตรงๆ (`student-activity.service.ts`) การสร้างเกณฑ์ใหม่ทุกครั้งจึงทำให้คะแนนเก่าไม่มีอะไรให้ชี้ถึง และไม่มีคำเตือนหรือทางกู้คืนเลย — ข้อบกพร่องที่หนักที่สุดที่พบระหว่างทำ #13 |
| **ขอบเขต** | `POST /activity` ไม่เปลี่ยน — ตอนสร้างไม่มี `id` ให้ส่งอยู่แล้ว และ schema ของ `POST` ไม่มีคีย์ `id` zod จึงตัดทิ้ง (ตอบ `200` ตามปกติ ไม่ใช่ปฏิเสธ) สิ่งที่ยัง**ไม่ได้**ทำคือคะแนนรวม: `student_activity.score` ถูกคำนวณจาก rubric ชุดเดิมและไม่มีอะไรคำนวณใหม่ให้ ลบเกณฑ์ทิ้งแล้วคะแนนรวมของนักศึกษายังเป็นค่าเดิม อาจารย์ต้องตรวจซ้ำเอง (ของเดิมก็เป็นแบบนี้) และการกด "ลบช่วงคะแนน" ในหน้าจอยังทำให้คะแนนที่ให้ไว้เพี้ยนได้ ดูรายการที่ปักหมุดไว้ในหัวข้อ "สิ่งที่**ไม่ได้**เปลี่ยน" ท้ายไฟล์ |
| **frontend** | **ต้องแก้ และแก้แล้ว** — `rubric-form.tsx` ส่ง `id` ของเกณฑ์ผ่าน form ต่อไป (`handleInitEditForm`) และ `create-activity-section.tsx` ส่งกลับไปกับ `rubric` ผ่าน `toRubricPayload` ใน `features/teacher/activity/utils/rubric-payload.ts` ตัวเดียวกันทั้งตอนสร้างและตอนแก้ ผู้เรียกที่ไม่ส่ง `id` มายังได้ `200` เหมือนเดิม เพียงแต่ได้พฤติกรรมเดิมคือเขียน rubric ใหม่ทั้งชุด |

### 2. `id` ของเกณฑ์ที่ไม่ใช่ของกิจกรรมนี้ หรือส่งซ้ำ ตอบ `400`

| | |
| --- | --- |
| **ของเดิม** | ไม่มีกรณีนี้ — ของเดิมไม่รับ `id` เลย |
| **ของใหม่** | `400` สองแบบ — `id` ที่ไม่ใช่เกณฑ์ของกิจกรรมนี้ได้ `{ "message": "มีเกณฑ์บางรายการที่ไม่ใช่ของกิจกรรมนี้" }` ส่วน `id` เดียวกันที่ส่งมาสองรายการได้ `{ "message": "มีเกณฑ์เดียวกันถูกส่งมาซ้ำ" }` ทั้งคู่ตรวจก่อนเขียนอะไรทั้งสิ้น ไม่มีอะไรถูกเขียนเลย ทั้งตัวกิจกรรม rubric ของทั้งสองฝั่ง และไฟล์แนบที่แนบมาด้วย |
| **เหตุผล** | `id` ไม่ได้บอกว่ามันเป็นของกิจกรรมไหน ถ้าเขียนตามที่ส่งมาโดยไม่ตรวจ การบันทึกกิจกรรมหนึ่งจะแก้ rubric ของอีกกิจกรรมหนึ่งได้ — `PUT /activity` เป็นหนึ่งใน route ที่ [ADR-0002](docs/adr/0002-section-access.md) ระบุว่ายังไม่มีการตรวจความเป็นเจ้าของเลย ส่วน `id` ซ้ำ ทั้งสองรายการจะถูกเขียนลงแถวเดียวกัน รายการหลังทับรายการแรก อาจารย์จึงเหลือเกณฑ์น้อยกว่าที่หน้าจอแสดงไว้โดยไม่มีอะไรบอก ต่างจาก `assertOwnSkills` (#31) ที่ตัดรายการซ้ำทิ้งได้ เพราะทักษะซ้ำคือความสัมพันธ์เดียวกัน แต่เกณฑ์ซ้ำมีเนื้อหาคนละอย่าง เลือกแทนอาจารย์ไม่ได้ว่าอันไหนคือของจริง |
| **ขอบเขต** | เป็น `400` ไม่ใช่ `403` แบบ `assertOwnSkills` — ที่นั่นคำถามคือแถวนี้เป็นของใคร คำตอบจึงเป็นเรื่องสิทธิ์ ที่นี่อาจารย์คนไหนก็แก้กิจกรรมนี้ได้อยู่แล้ว สิ่งที่ผิดคือตัว rubric ที่อ้างถึงเกณฑ์ซึ่งไม่ได้เป็นส่วนหนึ่งของมัน การตรวจความเป็นเจ้าของกลุ่มเรียนของ route นี้ยังเป็นงานที่ ADR-0002 ค้างไว้ |
| **frontend** | ไม่ต้องแก้ — หน้าจอส่งกลับเฉพาะ `id` ที่ได้จาก `GET /activity` ของกิจกรรมนั้นเอง และส่งรายการละครั้ง กรณีนี้จึงเกิดจากการยิง API ตรงเท่านั้น |

### รายการที่เคยเขียนไว้ว่า "แยกไปเป็น #25" และปิดแล้วในรอบนี้

- **`PUT /activity` ลบคะแนนที่ตรวจไปแล้วทิ้ง** (#13) — ปิดตามข้อ 1 ข้างบน

---

## #36 — ไฟล์ต้องมาพร้อมลายเซ็นที่ API ออกให้

กติกาเต็มอยู่ใน [ADR-0006](docs/adr/0006-file-access.md) — ไฟล์ของแฟ้มสะสมผลงาน
ไฟล์ของรายวิชา และไฟล์ที่นักศึกษาส่ง ใช้กฎเดียวกันทั้งหมด: **ใครเข้าถึงแถวที่ไฟล์
แขวนอยู่ได้ ก็โหลดไฟล์นั้นได้** และสิทธิ์นั้นตัดสินตอนที่ address ของไฟล์ออกจาก API
ไม่ใช่ตอนที่ไฟล์ถูกขอ

### 1. `file_path` และ `url` ของไฟล์แนบเป็น URL พร้อมใช้ ไม่ใช่ object key

| | |
| --- | --- |
| **ของเดิม** | ทุก response ที่มีไฟล์แนบส่ง object key ดิบออกมา เช่น `"portfolio-award/1-2-certificate.pdf"` แล้วให้ frontend ประกอบเป็น URL เอง |
| **ของใหม่** | ส่ง `"/files?path=portfolio-award%2F1-2-certificate.pdf&exp=…&sig=…"` — path ที่ `GET /files` รับได้ทันที เซ็นด้วย HMAC และมีอายุ 1 ชั่วโมง ทั้งช่อง `file_path` และช่อง `url` ของไฟล์ (ไฟล์แนบชนิดลิงก์ยังเป็น URL ภายนอกเหมือนเดิม ไม่แตะ) กระทบทุก endpoint ที่ผ่าน `getAttachments` — 20 จุดใน 12 service รวม `GET /portfolio/public/:token` |
| **เหตุผล** | `getAttachments` เป็นจุดเดียวที่ address ของไฟล์ออกจาก API และทุกจุดที่เรียกมันได้ตัดสินไปแล้วว่าผู้เรียกเห็นแถวนั้นได้ไหม เซ็นตรงนี้จุดเดียวจึงได้กติกาเดียวกันทุก endpoint พร้อมกัน โดยไม่ต้องเขียนกฎชุดที่สองจาก object key ซึ่งไม่ได้บอกว่าตัวเองแขวนอยู่กับแถวไหน |
| **ขอบเขต** | ค่าใน**ฐานข้อมูล**ไม่เปลี่ยน คอลัมน์ `attachments.file_path` ยังเก็บ object key ดิบเหมือนเดิม สิ่งที่เปลี่ยนคือค่าที่ส่งออกไปเท่านั้น |
| **frontend** | แก้ที่ `utils/get-file.ts` จุดเดียว — จาก `` `${BACKEND_URL}/files?path=${src}` `` เหลือ `` `${BACKEND_URL}${src}` `` จุดเรียกทั้ง 40 กว่าจุดไม่ต้องแก้ เพราะยังส่งค่าที่ได้จาก API ตัวเดิมเข้าไป และจุดที่แยกลิงก์ภายนอกด้วย `startsWith("http")` ก็ยังแยกได้เหมือนเดิม |

### 2. `GET /files` ปฏิเสธคำขอที่ไม่มีลายเซ็น

| | |
| --- | --- |
| **ของเดิม** | `?path=` อย่างเดียวพอ ไม่มี middleware ตรวจสิทธิ์เลยแม้แต่ `requireUser` ใครรู้ object key ก็โหลดใบประกาศ รูปโปรไฟล์ หรืองานที่นักศึกษาส่งของใครก็ได้ โดยไม่ต้องล็อกอิน |
| **ของใหม่** | ไม่มี `sig`/`exp` หรือลายเซ็นผิด → `403` `{ "message": "ลิงก์ไฟล์นี้ไม่ถูกต้อง" }` · ลายเซ็นถูกแต่เลยเวลา → `410` `{ "message": "ลิงก์ไฟล์นี้หมดอายุแล้ว กรุณาโหลดหน้านี้ใหม่" }` · ลายเซ็นถูกแต่ไม่มี object นั้น → `404` ข้อความเดิม · ไม่ส่ง `path` → `400` เหมือนเดิม |
| **เหตุผล** | รูที่ #36 ตั้งชื่อไว้ การเอา `requireUser` มาแปะแทนใช้ไม่ได้ด้วยสองเหตุผล — session เป็นคุกกี้ `sameSite: "lax"` ที่ไม่ตามไปกับ `<img src>` ข้าม origin (ซึ่งเป็นสภาพบน dev) และ `GET /portfolio/public/:token` ตั้งใจให้คนที่ไม่มีบัญชีเปิดได้ ถ้าไฟล์ต้องล็อกอิน ฟีเจอร์แชร์จะเหลือแต่ข้อความ |
| **ขอบเขต** | ไม่มีลายเซ็นได้ `403` ไม่ใช่ `400` ต่างจากกติกาปกติของ repo นี้ เพราะลายเซ็นคือตัวสิทธิ์ ไม่ใช่รูปร่างของคำขอ — เหตุผลเต็มอยู่ใน ADR-0006 และลายเซ็นผูกกับ**ไฟล์** ไม่ได้ผูกกับ**คน** ใครถือ URL ที่ยังไม่หมดอายุก็เปิดได้ ซึ่งเป็นสิ่งที่ทำให้ share link ทำงาน สิ่งที่ปิดคือการเดา key แล้วโหลดไฟล์ของคนที่ไม่เคยแชร์ให้ |
| **frontend** | ไม่ต้องแก้เพิ่มจากข้อ 1 — URL ที่ frontend ใช้มาจาก API ทั้งหมดอยู่แล้ว |

### 3. `GET /uploads/:filename` และ `/uploads/*` หายไป

| | |
| --- | --- |
| **ของเดิม** | สอง route เสิร์ฟไฟล์จากโฟลเดอร์ `uploads/` บนดิสก์ ไม่ตรวจอะไรเลยเช่นกัน |
| **ของใหม่** | ไม่มีทั้งคู่ — `GET /uploads/อะไรก็ตาม` ตอบ `404` ของ router ปกติ |
| **เหตุผล** | เป็นโค้ดตายมาตั้งแต่รับมอบ `middlewares/upload.ts` (multer แบบเขียนลงดิสก์) ไม่มี route ไหน import เลย ทุก route อัปโหลดผ่าน `upload-minio` โฟลเดอร์นั้นจึงว่างตลอด การเอา middleware ไปเฝ้าทางที่ไม่มีอะไรเดินผ่าน คือการเพิ่มโค้ดที่ทดสอบจริงไม่ได้ ทางเข้าไฟล์ควรมีทางเดียวและมีลายเซ็น |
| **ขอบเขต** | ลบตามไปด้วย — `middlewares/upload.ts`, volume `api-uploads` ใน `docker-compose.yml` และ `RUN mkdir -p uploads` ใน `apps/api/Dockerfile` ไม่มีข้อมูลไหนหาย เพราะไม่เคยมีอะไรถูกเขียนลงไป |
| **frontend** | ไม่ต้องแก้ — ไม่มีที่ไหนใน `apps/web` เรียก `/uploads` |

การป้องกัน path traversal ที่เพิ่มไว้ใน **#20 ข้อ 8** ("`GET /uploads/:filename`
ปฏิเสธชื่อไฟล์ที่เป็นเส้นทาง") หายไปพร้อมกับ route ที่มันเฝ้า — `filename ต้องเป็นชื่อไฟล์ ไม่ใช่เส้นทาง` ไม่มีทาง
เกิดขึ้นได้อีกแล้วเพราะไม่มี route ที่รับ `filename` เหลืออยู่ ส่วน `path` ของ
`GET /files` เป็น object key ใน MinIO ซึ่งไม่มีโฟลเดอร์แม่ให้ออกไป และตอนนี้ต้องมี
ลายเซ็นกำกับ คนนอกจึงตั้ง key เองไม่ได้ตั้งแต่ต้น

### รายการที่เคยเขียนไว้ว่า "แยกไปเป็น #36" และปิดแล้วในรอบนี้

- **ตัวไฟล์ยังไม่อยู่หลังกติกาของ ADR-0001** (#31) — ปิดตามข้อ 1 และ 2 ข้างบน
- **`getFile` ต่อ path เข้ากับ query โดยไม่ escape** (#19) — ปิดไปด้วยโดยไม่ได้ตั้งใจ
  ตอนนี้ query ถูกประกอบด้วย `URLSearchParams` ฝั่ง API ชื่อไฟล์ที่มี `&` หรือ `#`
  จึงไม่ทำให้ URL ขาดกลางคันอีก และฝั่ง frontend ไม่ได้ประกอบ query เองแล้ว
- **`isProduction` ใน `get-file.ts` เป็นเท็จเสมอ** (#19) — ตัวแปรและสาขาที่ตายอยู่
  ถูกลบทิ้ง ไม่ได้แปลว่าตัดสินเรื่อง origin บน production แล้ว ทุก URL ยังประกอบจาก
  `VITE_BACKEND_URL` เหมือนเดิมทุกประการ เพียงแต่เลิกแกล้งทำเป็นว่ามีทางเลือกที่สอง
  อยู่ คำถามว่า production ควรยิงไป origin ไหน ยังเปิดอยู่และขึ้นกับการ deploy

---

## #37 — กลุ่มต้องเป็นของคนสร้าง และมีได้แต่คนในชั้นเรียน

กติกาเต็มอยู่ใน [ADR-0007](docs/adr/0007-group-membership.md) — ADR-0004 ตัดสินไว้ว่า
**กลุ่มที่มีอยู่แล้วเป็นของหัวหน้า** แต่เว้นไว้สองข้อ คือตอน "ยังไม่มีกลุ่ม" ใครเป็น
เจ้าของคำขอ และรายชื่อที่ส่งมาต้องเป็นใครได้บ้าง #37 ปิดทั้งสองข้อ และใช้กับทั้ง
`/student-activity-group` และ `/student-learning-activity-group` เหมือนกันทุกตัวอักษร

### 1. `POST` ปฏิเสธคำขอที่ผู้เรียกไม่ได้เป็นหัวหน้าของรายชื่อที่ส่งมา

| | |
| --- | --- |
| **ของเดิม** | `POST` ทั้งสองตัวรับรายชื่อไปทั้งดุ้นโดยไม่ดูว่าผู้เรียกอยู่ตรงไหน นักศึกษาจึงตั้งคนอื่นเป็น `LEADER` โดยไม่ใส่ตัวเองลงในรายชื่อเลยก็ได้ — สร้างกลุ่มในนามคนอื่น แล้วคนที่ถูกตั้งเป็นหัวหน้าก็ได้กลุ่มที่ตัวเองไม่ได้ขอ |
| **ของใหม่** | `student_id` ของสมาชิกที่ `role === "LEADER"` ต้องเป็นคนที่ล็อกอินอยู่ ไม่ใช่ → `403` `{ "success": false, "message": "สร้างกลุ่มได้เฉพาะกลุ่มที่ตัวเองเป็นหัวหน้าเท่านั้น" }` ตรวจที่ `requireSelfLeader` ใน `owner.middleware.ts` ต่อจาก `validate` |
| **เหตุผล** | ตอนสร้างยังไม่มีแถวกลุ่มให้ `requireGroupLeader` ถาม สิ่งเดียวที่บอกความเป็นเจ้าของได้คือรายชื่อในคำขอเอง และ **"อยู่ในรายชื่อ" ไม่พอ** เพราะ ADR-0004 ให้เฉพาะหัวหน้าแก้และลบกลุ่มได้ คนที่ไม่ใช่หัวหน้าจึงถอนสิ่งที่ตัวเองสร้างไม่ได้ ตัดกรณี "สร้างกลุ่มให้เพื่อนแล้วให้เพื่อนเป็นหัวหน้า" ทิ้งอย่างตั้งใจ |
| **ขอบเขต** | เฉพาะ `POST` — `PATCH` ยังยกตำแหน่งหัวหน้าให้คนอื่นได้เหมือนเดิมตาม #27 (หัวหน้าคนเดิมเป็นคนส่งคำขอนั้น จึงยังเป็นเจ้าของคำขออยู่) schema เดิมบังคับ `LEADER` ไว้หนึ่งคนพอดีอยู่แล้ว รายชื่อที่ไม่มีหัวหน้าหรือมีสองคนจึงยังเป็น `400` ของ `validate` เหมือนเดิม · คอลัมน์ `created_by` เปลี่ยนมาอ่านจาก session แทนที่จะค้นหาคนที่รายชื่อบอกว่าเป็น `LEADER` **ค่าที่เขียนลงฐานข้อมูลเท่าเดิมทุกกรณี** เพราะกติกาข้อนี้บังคับให้สองอย่างนั้นเป็นคนเดียวกันอยู่แล้ว แต่ "ใครสร้าง" เป็นข้อเท็จจริงของคำขอตามแนวของ ADR-0001 ไม่ใช่สิ่งที่คำขอบอก |
| **frontend** | ไม่ต้องแก้ — หน้าจอสร้างกลุ่มใส่ผู้ใช้ที่ล็อกอินเป็น `LEADER` ให้เองอยู่แล้ว |

### 2. `POST` และ `PATCH` ปฏิเสธสมาชิกที่ไม่ได้ลงทะเบียนกลุ่มเรียนของชิ้นงานนั้น

| | |
| --- | --- |
| **ของเดิม** | ไม่มีการตรวจ `student_course` เลย หัวหน้าจึงดึงนักศึกษาจากคนละกลุ่มเรียน หรือคนละวิชา เข้ากลุ่มได้ และระบบสร้างแถว `student_activity` / `student_learning_activity` ให้คนนั้นตามไปด้วย ถ้า `student_id` ไม่ใช่นักศึกษาเลย FK จะพังกลางคันเป็น `500` |
| **ของใหม่** | ทุกคนในรายชื่อต้องมีแถว `student_course` ของ `section_id` ที่ชิ้นงานนั้นสังกัด ไม่ครบแม้แต่คนเดียว → `400` `{ "success": false, "message": "รายชื่อมีนักศึกษาที่ไม่ได้ลงทะเบียนกลุ่มเรียนนี้" }` ใช้กับ `POST` และ `PATCH` ของทั้งสอง router ตรวจที่ `assertMembersEnrolled` ใน `group.service.ts` |
| **เหตุผล** | รูที่ #37 ตั้งชื่อไว้ ตอบ `400` ไม่ใช่ `403` เพราะสิ่งที่ถูกปฏิเสธคือ**ค่าในรายชื่อที่ผู้เรียกส่งมา** ไม่ใช่สิทธิ์ของผู้เรียกที่จะทำสิ่งนี้ — ตัวผู้เรียกผ่านด่านสิทธิ์มาแล้วทั้งข้อ 1 และ `requireGroupLeader` แนวเดียวกับ `assertOwnRubric` ใน `activity.service.ts` |
| **ขอบเขต** | `student_id` ที่ไม่ใช่นักศึกษาในระบบเลย ตกในคำตอบเดียวกันนี้ **จาก `500` เดิมกลายเป็น `400`** เพราะไม่มีแถวลงทะเบียนอยู่แล้ว และเป็นการไม่บอกคนนอกว่ารหัสไหนมีอยู่จริงตามแนวของ ADR-0002 · `activity_id` / `learning_activity_id` ที่ไม่มีอยู่จริง ก็ตอบ `400` นี้แทน FK error `500` เดิมด้วยเหตุผลเดียวกัน · ชิ้นงานที่ `section_id` เป็น `null` (คอลัมน์ยอมให้ว่างได้ แต่ API ไม่มีทางเขียนค่านั้น) ถือว่าไม่มีรายชื่อชั้นเรียน จึงปฏิเสธทุกคน — pin ไว้ด้วย test แล้ว · การตรวจอยู่ **ใน transaction ก่อนการเขียนแถวแรก** และใน `PATCH` อยู่ **ก่อน `deleteMany` ของสมาชิกเดิม** คำขอที่ถูกปฏิเสธจึงไม่ทิ้งกลุ่มเปล่า ไม่ทิ้งแถวส่งงาน และไม่ทำให้กลุ่มเดิมเปลี่ยนไปแม้แต่แถวเดียว |
| **frontend** | ไม่ต้องแก้ — ตัวเลือกสมาชิกดึงมาจาก `GET /without-group` ซึ่งคืนเฉพาะคนในกลุ่มเรียนนั้นอยู่แล้ว ข้อความใหม่แสดงผ่านทางเดิมที่แสดง `message` ของ `400` |

### รายการที่เคยเขียนไว้ว่า "อยู่ที่ #37" และปิดแล้วในรอบนี้

- **`POST` ยังไม่ตรวจว่าผู้เรียกอยู่ในรายชื่อที่ตัวเองส่ง** (#27) — ปิดตามข้อ 1
  ข้างบน คำถามที่ค้างไว้ว่า "ผู้สร้างต้องเป็นหัวหน้าเสมอหรือแค่ต้องอยู่ในรายชื่อ"
  ตัดสินแล้วว่า **ต้องเป็นหัวหน้า**
- **ยังไม่ตรวจว่าสมาชิกที่ระบุลงทะเบียนกลุ่มเรียนเดียวกับชิ้นงาน** (#27) — ปิดตามข้อ 2
  ข้างบน ทั้ง `POST` และ `PATCH` และแถวส่งงานที่เคยตกค้างก็ไม่เกิดขึ้นอีก

### สิ่งที่พบแล้ว **pin ไว้ตามที่เป็น** (ไม่ได้แก้ใน ticket นี้)

- **ชิ้นงานที่ `section_id` เป็น `null` ตอบเหมือนมีคนนอกอยู่ในรายชื่อ** —
  `activities.section_id` ยอมให้ว่างได้ตาม baseline แต่ไม่มี endpoint ไหนเขียน
  ค่านั้น (`POST /activity` บังคับ) แถวแบบนั้นจึงเกิดไม่ได้ผ่าน API วันนี้ ถ้าเกิดขึ้น
  ทางอื่น ก็ไม่มีรายชื่อชั้นเรียนให้เทียบ และคำตอบคือ `400` ตัวเดียวกัน ไม่ได้บอกว่า
  ผิดที่รายชื่อหรือผิดที่ชิ้นงาน pin ไว้ที่ case "refuses work that belongs to no
  section" ฝั่ง `learning_activities.section_id` เป็น NOT NULL จึงไม่มีกรณีนี้
- **`activity_id` / `learning_activity_id` ที่ไม่มีอยู่จริงตอบ `400` ตัวเดียวกัน** —
  ของเดิมเป็น `500` จาก foreign key คำตอบใหม่ไม่ได้บอกว่าชิ้นงานไม่มีอยู่ ซึ่งเป็น
  ผลข้างเคียงที่ยอมรับได้ด้วยเหตุผลของ ADR-0002 (ไม่บอกคนนอกว่ารหัสไหนมีอยู่จริง)
  pin ไว้ที่ case "refuses work that does not exist" ทั้งสองไฟล์

### สิ่งที่ **ไม่ได้** ทำใน ticket นี้

- **ยังไม่ดูสถานะการส่งงานของกลุ่ม** — หัวหน้ายังแก้รายชื่อหรือยุบกลุ่มที่ส่งงานหรือ
  ถูกให้คะแนนไปแล้วได้ เป็นช่องเดิมที่ยกไว้ตั้งแต่ #27 และเป็นคำถามของตัวเอง
- **นักศึกษาที่ถอนรายวิชาหลังตั้งกลุ่มแล้ว** — ยังอยู่ในกลุ่มต่อไป การตรวจนี้เกิดตอน
  เขียนเท่านั้น ไม่ได้ไล่ตรวจย้อนหลัง
- **`POST /student/submit/activity` ยังไม่ตรวจว่าผู้ส่งอยู่ในกลุ่มที่อ้าง** — เป็นคนละ
  endpoint และคนละเกณฑ์ยอมรับ ไม่รวมใน #37

---

## #33 — คอลัมน์ `Decimal` ที่เหลือออกจาก API เป็นตัวเลข

`Prisma.Decimal` ที่ส่งเข้า `res.json` ตรงๆ ออกไปถึงผู้เรียกเป็น**สตริง** #15 ข้อ 3
ปิดไปแล้วหนึ่งที่ (`/evaluation/list`) และ #16 ข้อ 4 ปิด `portfolio_education.gpa`
รอบนี้ไล่ `schema.prisma` ทั้งไฟล์แล้วเหลือคอลัมน์ `Decimal` อีกห้าคอลัมน์:

| คอลัมน์ | ออกไปทางไหน |
| --- | --- |
| `rubric_details.weight` | `GET /rubric/shared-rubric/detail` |
| `activity_clo_mapping.score` | `POST /mapping/activity` (แถวที่เพิ่งสร้างคือ response) |
| `student_activity.score` | `GET /activity/student/detail`, `GET /student/classwork/list`, `GET /student/activities/list`, `GET /student/activities/details/:student_activity_id` |
| `student_activity_rubric_score.calculated_score` | `GET /activity/student/detail` |
| `activity_scores.score` | **ไม่ออกเลย** — เขียนอย่างเดียว ไม่มี endpoint ไหนอ่านออกไป จึงไม่มีอะไรต้องแก้ |

ทุกที่แปลงด้วย `Number(...)` ที่ชั้น service แบบเดียวกับ #15 และ #16 ไม่ประดิษฐ์วิธีที่สาม
ส่วนทางที่แปลงไปแล้วก่อนหน้านี้ (`/evaluation/list`, `/gradebook/*`,
`/activity/submitted/list`) ไม่ได้แตะ

> รายชื่อคอลัมน์ในตัว issue เองล้าสมัย — อ้างถึง `subject_score_ratio.score` /
> `weight` ซึ่งเป็น `Int?` กับไม่มีอยู่ และ `student_learning_activity.score`
> ซึ่งไม่มีในตาราง ตารางข้างบนมาจาก `schema.prisma` จริง

### 1. `GET /rubric/shared-rubric/detail` คืน `weight` เป็นตัวเลข

| | |
| --- | --- |
| **ของเดิม** | `rubric_details.weight` เป็น `Decimal(5,2)` และแถวจาก Prisma ถูกส่งต่อทั้งก้อน เกณฑ์ที่ถ่วงน้ำหนัก 2.5 จึงมาเป็น `"2.5"` |
| **ของใหม่** | `number` หรือ `null` เมื่อยังไม่ได้ตั้งค่า (คอลัมน์เป็น nullable) |
| **เหตุผล** | เป็นทางออกทางเดียวของคอลัมน์นี้ และ `SharedRubricDetailResp` ฝั่งเว็บประกาศ `weight: number \| null` ไว้อยู่แล้ว — ความไม่ตรงกันแบบนี้ยังไม่พังตอนแสดงผล แต่จะพังทันทีที่มีใครเอาไปบวกหรือเทียบ เหมือนที่ #15 ข้อ 3 เขียนไว้ |
| **frontend** | ไม่ต้องแก้ — ผู้ใช้ค่านี้มีที่เดียวคือ `shared-rubric-detail-table.tsx` ซึ่งตอนคัดลอกเกณฑ์เข้าฟอร์มตั้ง `weight: 0` ทับอยู่แล้ว ไม่ได้อ่านค่าจาก API เลย (`rubric-card.tsx` ที่พิมพ์ `{weight}%` ใช้ `RubricDetail` จาก `types/activity-type.type.ts` ซึ่งเป็น `rubric_activity_mapping.weight` คนละคอลัมน์และเป็น `Int`) การแก้ครั้งนี้จึงทำให้ของจริงตรงกับที่ประกาศไว้ โดยไม่มีอะไรบนหน้าจอเปลี่ยน |

### 2. `POST /mapping/activity` คืน `score` เป็นตัวเลข

| | |
| --- | --- |
| **ของเดิม** | endpoint นี้ตอบด้วยแถว `activity_clo_mapping` ที่เพิ่งสร้าง ซึ่งมี `score` เป็น `Decimal(5,2)` ที่ service คิดเองจาก `score_number × weight ÷ 100` งาน 10 คะแนนที่ผูก CLO ด้วยน้ำหนัก 25% จึงตอบ `"2.5"` |
| **ของใหม่** | `2.5` |
| **เหตุผล** | ค่านี้ service คิดเองแล้วเขียนลงคอลัมน์ `Decimal` การส่งแถวที่เพิ่งสร้างกลับไปทั้งก้อนจึงพา `Decimal` ออกไปด้วยโดยไม่ตั้งใจ ไม่ใช่การตัดสินใจว่าจะให้เป็นสตริง |
| **frontend** | ไม่ต้องแก้ — `teacher-mapping-action.ts` ประกาศ response ของ thunk นี้ว่า `{ id: number }` อ่านแค่ `id` แล้วโหลดรายการใหม่ ไม่มีใครอ่าน `score` จาก response นี้ |

### 3. `GET /activity/student/detail` คืนคะแนนและคะแนนรายเกณฑ์เป็นตัวเลข

| | |
| --- | --- |
| **ของเดิม** | `score`, `student_score` และ `calculated_score` ของทุกเกณฑ์ในอาร์เรย์ `student_activity_rubric_score` ออกไปเป็นสตริงทั้งหมด ทั้งที่ `GetStudentActivityDetail` ฝั่ง API ประกาศ `student_score: Prisma.Decimal` ส่วน type ฝั่งเว็บประกาศ `number` — สองฝั่งไม่ตรงกันเองอยู่แล้ว |
| **ของใหม่** | ทั้งสามช่องเป็น `number` (`score` และ `student_score` ยังเป็น `null` ได้เมื่อยังไม่ถูกตรวจ) และ `GetStudentActivityDetail` ฝั่ง API เปลี่ยนเป็น `number` ตาม — type บอกสิ่งที่อยู่บน wire ไม่ใช่สิ่งที่ Prisma คืนมา |
| **เหตุผล** | endpoint นี้เป็นหน้าที่อาจารย์ใช้ตรวจงานและนักศึกษาใช้ดูผล คะแนนรายเกณฑ์ทั้งชุดออกไปเป็นสตริงพร้อมกันทีเดียว และเป็นที่เดียวที่ type สองฝั่งขัดกันเอง — ฝั่ง API เขียน `Prisma.Decimal` ฝั่งเว็บเขียน `number` แปลว่าไม่มีใครเคยยึดสิ่งที่อยู่บน wire เป็นหลัก |
| **ขอบเขต** | คำขอที่ไม่มีการส่งงานอยู่จริงยังตอบเหมือนเดิมทุกอย่าง คือมีแต่ `submitted_files` ไม่มีคีย์ `student_score` โผล่มาเป็น `null` |
| **frontend** | ไม่ต้องแก้ที่ type — `src/types/student-activity-type.type.ts` เขียน `student_score: number \| null` และ `calculated_score: number` ไว้ตรงแล้ว ส่วนจุดแสดงผลดูข้อ 5 |

### 4. คะแนนในรายการงานของนักศึกษาเป็นตัวเลข

| | |
| --- | --- |
| **ของเดิม** | `GET /student/classwork/list` ตอบ `received_point` เป็นสตริง และ `GET /student/activities/list` กับ `GET /student/activities/details/:student_activity_id` ตอบ `score` เป็นสตริง |
| **ของใหม่** | ทั้งสามที่เป็น `number` หรือ `null` เมื่อยังไม่มีคะแนน |
| **เหตุผล** | คะแนนก้อนเดียวกันนี้ออกไปหลายทางตามหน้าที่นักศึกษาเปิด ถ้าแปลงบางทางไม่แปลงบางทาง ผู้เรียกจะเจอชนิดข้อมูลต่างกันจากคอลัมน์เดียวกัน ซึ่งแย่กว่าผิดเหมือนกันทั้งหมด |
| **ขอบเขต** | `GET /all/classwork/list` และ `GET /student/calendar` ไม่ได้ `select` คะแนนมาตั้งแต่ต้น จึงไม่มีอะไรเปลี่ยน ส่วน `GET /portfolio/:id` ซึ่งเรียก service ตัวเดียวกันข้างใน อ่านแค่ชื่องานกับ feedback ไม่ได้ส่งคะแนนต่อออกไป |
| **frontend** | type ประกาศ `number` ไว้ตรงแล้วทั้งคู่ (`features/student/course/types/course-type.ts`) จุดแสดงผลดูข้อ 5 |

### 5. หน้าเว็บสามจุดเลิกกลืนคะแนน 0

| | |
| --- | --- |
| **ของเดิม** | สามจุดที่แสดง "ได้เท่าไหร่ / เต็มเท่าไหร่" ตรวจว่ามีคะแนนหรือยังด้วยความจริงเท็จของค่าเอง (`received_point && point`, `score && student_score`) ตอนที่ค่าเป็นสตริง `"0"` ซึ่งเป็นจริง จึงแสดง `0/10 คะแนน` ถูกต้องโดยบังเอิญ |
| **ของใหม่** | ทั้งสามจุดเทียบกับ `null` แทน (`!= null`) — `classwork-card.tsx`, `student-classwork-detail-page.tsx` และ `features/teacher/activity/components/grading-section.tsx` |
| **เหตุผล** | พอค่ากลายเป็นตัวเลข `0` ก็เป็นเท็จ นักศึกษาที่ถูกตรวจแล้วได้ 0 คะแนนจะเห็น `10 คะแนน` (คะแนนเต็ม) แทน `0/10 คะแนน` เหมือนยังไม่ถูกตรวจ นี่คือกรณีเดียวที่การเปลี่ยนชนิดนี้กระทบสิ่งที่ผู้ใช้เห็นจริง จึงแก้ไปพร้อมกันตาม D9 |
| **ผลข้างเคียง** | ตัวส่วน (`point` / `score` / `score_number`) ยังตรวจแบบเดิม เพราะเป็น `Int` มาตั้งแต่ต้น ไม่ได้ถูก ticket นี้แตะ |
| **เลขศูนย์ท้าย** | ไม่เปลี่ยน — `Decimal` ตัดศูนย์ท้ายทิ้งตั้งแต่ตอน serialize อยู่แล้ว คะแนน 18 เคยมาเป็น `"18"` ไม่ใช่ `"18.00"` (เห็นได้จาก test เดิมที่ assert ไว้แบบนั้น) เป็นตัวเลขแล้วก็ยังเป็น `18` เหมือนกัน |

### สิ่งที่ **ไม่ได้** ทำใน ticket นี้

- **ไม่ได้เขียน helper กลางสำหรับแปลง `Decimal`** — แต่ละที่แปลงตรงจุดที่ประกอบ
  response เหมือนที่ #15 และ #16 ทำไว้ เกณฑ์ยอมรับสั่งไว้ตรงๆ ว่า
  "ไม่ประดิษฐ์วิธีที่สาม"
- **`activity_scores.score`** — ไล่แล้วว่าไม่มีทางออก จึงปล่อยไว้ ถ้าวันหนึ่งมี
  endpoint อ่านมันออกไป ต้องแปลงตอนนั้น
- **ไม่ได้เปลี่ยนชนิดคอลัมน์ในฐานข้อมูล** — `Decimal(5,2)` ยังเป็นชนิดที่ถูกต้อง
  สำหรับคะแนน สิ่งที่แก้คือขาที่มันออกจาก API เท่านั้น

---

## #34 — ลบรายการแล้วไฟล์แนบหายไปด้วย

ทุก endpoint ที่ลบรายการซึ่งมีไฟล์แนบ เคยลบแต่ **แถวเชื่อม** ปล่อยแถวใน
`attachments` กับ object ใน MinIO ค้างไว้ตลอดกาล ไม่มีทางอ่านถึงอีกเพราะทุกทางอ่าน
เริ่มจากตารางเชื่อม ตอนนี้ไฟล์แนบที่ไม่มีใครชี้ถึงแล้วถูกลบทั้งแถวและ object
เหตุผลและกติกาการนับ reference อยู่ใน
[ADR-0008](docs/adr/0008-attachment-lifecycle.md)

**status และ body ของทุก endpoint ที่กระทบเหมือนเดิมทุกตัวอักษร** สิ่งที่เปลี่ยนคือ
สถานะของฐานข้อมูลและของถังหลังคำขอสำเร็จ จึงไม่มี frontend จุดไหนต้องตามแก้ — จดไว้
ที่นี่เพราะผู้เรียกที่เคยเก็บ `attachment_id` หรือ URL ของไฟล์ไว้ใช้ต่อหลังลบรายการ
จะเริ่มได้ `404` จาก `GET /files` แทนที่จะได้ไฟล์

### 1. ลบรายการแล้วไฟล์แนบของรายการนั้นถูกลบตาม

| | |
| --- | --- |
| **ของเดิม** | `DELETE /course-material`, `DELETE /portfolio-{certificate,training,internship,thesis,award,activity}/:id`, `DELETE /activity`, `DELETE /learning-activity` ลบแถวเจ้าของ (และแถวเชื่อมที่ cascade ตามไป) แต่แถวใน `attachments` กับไฟล์ในถังยังอยู่ครบ |
| **ของใหม่** | ไฟล์แนบที่ไม่มีตารางไหนชี้ถึงอีก ถูกลบทั้งแถวใน `attachments` และ object ใน MinIO |
| **เหตุผล** | ไฟล์จริงของคนจริงค้างในถังโดยไม่มีหน้าจอไหนลบได้ ขัดกับ TC-24, TC-56 และ TC-62 ที่ระบุว่าลบแล้วต้องหายทั้งชุด |
| **ขอบเขต** | `DELETE /activity` และ `DELETE /learning-activity` กวาด **สองฝั่ง** — ไฟล์โจทย์ที่อาจารย์แนบ และไฟล์ที่นักศึกษาทุกคนส่ง เพราะ `student_activity` cascade มาจากตัวกิจกรรม การลบกิจกรรมจึงทำให้งานที่ส่งไปแล้วขาดเจ้าของพร้อมกัน |
| **frontend** | ไม่ต้องแก้ |

### 2. `ids_to_delete` และ `remove_attachment_ids` ลบไฟล์จริง

| | |
| --- | --- |
| **ของเดิม** | `PUT /portfolio-…` ที่ส่ง `ids_to_delete` และ `PUT /activity` / `PUT /learning-activity` ที่ส่ง `remove_attachment_ids` ตัดแค่แถวเชื่อม ไฟล์ยังอยู่ในถังและยังเปิดได้ถ้ายังถือ URL อยู่ |
| **ของใหม่** | ไฟล์ที่ถูกเอาออกจากรายการสุดท้ายที่ถืออยู่ ถูกลบทั้งแถวและ object |
| **เหตุผล** | "เอาไฟล์นี้ออก" บนหน้าจอ คือคำสั่งลบในสายตาผู้ใช้ ไม่ใช่คำสั่งซ่อน |
| **frontend** | ไม่ต้องแก้ — แต่ผู้ใช้ที่กด undo ไม่ได้ ต้องอัปโหลดใหม่ ซึ่งเป็นสิ่งที่หน้าจอสื่ออยู่แล้ว |

### 3. ส่งงานซ้ำแล้วไฟล์รอบก่อนที่ไม่ได้เลือกเก็บ ถูกลบ

| | |
| --- | --- |
| **ของเดิม** | `POST /student/submit/{activity,group-activity,learning-activity,group-learning-activity}` ตัดแถวเชื่อมของรอบก่อนทิ้งแล้วผูกเฉพาะ `existing_files_ids` กลับเข้าไป ไฟล์ที่ไม่ได้ถูกเอ่ยถึงอีกจึงค้างอยู่ในถัง |
| **ของใหม่** | ไฟล์ที่การส่งใหม่ไม่ได้เอ่ยถึงอีก ถูกลบทั้งแถวและ object ส่วนไฟล์ที่อยู่ใน `existing_files_ids` ไม่ถูกแตะ |
| **เหตุผล** | นักศึกษาที่ส่งงานสิบรอบทิ้งไฟล์ไว้เก้าชุดที่ไม่มีใครเปิดได้ |
| **ขอบเขต** | การกวาดอยู่**หลัง**จังหวะที่ `existing_files_ids` ถูกผูกกลับเข้าไปในทรานแซกชันเดียวกัน ระหว่างสองจังหวะนั้นไฟล์ที่นักศึกษาเลือกเก็บยังไม่มีใครชี้ถึงชั่วคราว กวาดตรงนั้นคือลบงานที่เขาตั้งใจเก็บ |
| **frontend** | ไม่ต้องแก้ |

### 4. นอกเหนือจากที่ #34 ระบุ — สัปดาห์ของแผนการสอน และรูปโปรไฟล์

| | |
| --- | --- |
| **ของเดิม** | `DELETE /lesson-plan` cascade `course_material` ของสัปดาห์นั้นทิ้ง และ `PUT` / `upsert` / `DELETE /portfolio-personal` เปลี่ยนหรือทิ้ง `attachment_id` ของรูปโปรไฟล์ ทั้งสองทางทำให้ไฟล์ขาดเจ้าของด้วยกลไกเดียวกับข้างบน |
| **ของใหม่** | กวาดเหมือนกันทุกประการ — เอกสารทุกชิ้นของสัปดาห์ที่ถูกลบ และรูปโปรไฟล์ใบก่อนเมื่อถูกแทนที่หรือเมื่อลบข้อมูลส่วนตัว |
| **เหตุผล** | ตัว issue เขียนขอบเขตไว้ว่า "ครอบคลุม**อย่างน้อย**" รายการที่มันแจกแจง สองทางนี้เป็นรูหน้าตาเดียวกัน การแก้ทีละครึ่งจะทิ้งไว้ให้ค้นเจอใหม่ทีหลัง |
| **ขอบเขต** | รูปโปรไฟล์ถูกลบเมื่อ `attachment_id` **เปลี่ยนไปเป็นค่าอื่น** เท่านั้น ส่งค่าเดิมซ้ำไม่ลบอะไร |
| **frontend** | ไม่ต้องแก้ |

### 5. `remove_attachment_ids` ตัดได้เฉพาะไฟล์ของงานที่กำลังแก้

| | |
| --- | --- |
| **ของเดิม** | `PUT /activity` และ `PUT /learning-activity` ลบแถวเชื่อมด้วยเงื่อนไข `attachment_id` เพียงอย่างเดียว ไม่มี `activity_id` ส่งเลขของไฟล์ที่แขวนอยู่กับงานอื่นเข้าไป ไฟล์นั้นก็หลุดจากงานนั้นไปด้วยเงียบ ๆ |
| **ของใหม่** | เงื่อนไขผูกกับงานที่กำลังแก้ด้วย ไฟล์ของงานอื่นไม่ถูกแตะ |
| **เหตุผล** | เลขของไฟล์แนบไม่ได้บอกว่าใครเป็นเจ้าของ พอ #34 เอาการกวาดมาต่อท้ายการลบแถวเชื่อม บั๊กเดิมที่แค่ "หลุดการเชื่อม" กลายเป็น "ลบไฟล์ทิ้งถาวร" ทันที และ `PUT /activity` ตรวจแค่ว่าเป็นอาจารย์ ไม่ได้ตรวจว่าเป็นเจ้าของงาน — อาจารย์คนหนึ่งจึงลบไฟล์ของอีกคนได้ แก้ทันทีตาม D9 |
| **ขอบเขต** | `PUT /portfolio-…` ไม่มีปัญหานี้ — ทุกตัวผูกเงื่อนไขกับ id ของรายการอยู่แล้ว |
| **frontend** | ไม่ต้องแก้ — ไม่มีหน้าไหนส่งเลขของไฟล์ที่ไม่ใช่ของงานที่กำลังแก้อยู่แล้ว |

### สิ่งที่ **ไม่ได้** ทำใน ticket นี้

- **ไม่ได้กวาดขยะเก่าที่ค้างอยู่แล้ว** — ไม่มี migration และไม่มีสคริปต์กวาดถัง
  ระบบยังไม่เคยขึ้น production ของที่ค้างเป็นของ dev ทั้งหมด เหตุผลเต็มอยู่ใน
  ADR-0008 ข้อ 3
- **ไม่ได้ทำให้การลบ object เป็นเรื่องที่ล้มเหลวได้** — ถ้า MinIO ตอบไม่สำเร็จ
  ระบบ log แล้วจบ ผู้เรียกยังได้ `200` เพราะฐานข้อมูล commit ไปแล้ว
- **ไม่ได้เพิ่มถังขยะหรือ undo** — ลบแล้วลบเลย

---

## #38 — ส่งงานได้เฉพาะงานของตัวเอง

session บอกว่าใครเป็นคนกดส่ง แต่ `student_activity_id` กับ `group_id` มาจาก body
และไม่มีอะไรเทียบสองอย่างนี้เข้าหากันเลย ตอนนี้ทั้งสี่เส้นที่รับงานเข้าระบบตรวจก่อน
เขียนทุกครั้ง เหตุผลของสถานะที่เลือกและขอบเขตอยู่ใน
[ADR-0009](docs/adr/0009-submission-ownership.md)

รายการนี้เคยอยู่ในหัวข้อ "สิ่งที่ **ไม่ได้** เปลี่ยน" ตั้งแต่ #18 (ทางเดี่ยว) — ตอนนี้
ย้ายขึ้นมาเป็นการเปลี่ยนพฤติกรรมจริง

### 1. งานที่ระบุต้องเป็นของผู้ส่ง

| | |
| --- | --- |
| **ของเดิม** | `POST /student/submit/activity` และ `POST /student/submit/learning-activity` ส่ง `200` ให้ทุกคำขอที่ระบุ id ซึ่งมีอยู่จริง นักศึกษาที่รู้เลขของเพื่อนจึงส่งงานแทนเพื่อนได้ และเพราะทางนี้ตัดไฟล์แนบเดิมทิ้งทุกครั้งก่อนผูกของใหม่ ไฟล์ที่เพื่อนส่งไว้ก็ถูกทับ — หลัง #34 คือถูกลบถาวร |
| **ของใหม่** | `403` พร้อม `ส่งงานได้เฉพาะงานของตัวเองเท่านั้น` เมื่อแถวที่ระบุไม่ใช่ของ session |
| **เหตุผล** | เลขของแถวไม่ได้พิสูจน์อะไรเลยนอกจากว่ามีแถวนั้นอยู่ งานที่นักศึกษาส่งเป็นหลักฐานการเรียนของเขาเอง การเขียนทับมันโดยคนอื่นไม่ควรมีทางเกิดขึ้นจากคำขอที่ถูกต้องตามรูปแบบ |
| **ขอบเขต** | id ที่ไม่ตรงกับแถวไหนเลยยัง `404` `ไม่พบงานที่ต้องการส่ง` เหมือนเดิม — เหตุผลที่ไม่ยุบสองกรณีเข้าด้วยกันอยู่ใน ADR-0009 ข้อ 3 |
| **frontend** | ไม่ต้องแก้ — `features/student/course/components/classwork-detail/submit-classwork-section.tsx` ส่ง `classworkDetail.id` ซึ่งเป็นแถวของคนที่ล็อกอินอยู่เสมอ ทั้งโหมดเดี่ยวและโหมดกลุ่ม |

### 2. กลุ่มที่ระบุต้องเป็นกลุ่มที่ผู้ส่งตอบรับคำเชิญแล้ว

| | |
| --- | --- |
| **ของเดิม** | ทางกลุ่มดูแค่ว่ากลุ่มที่ระบุมีสมาชิกที่ `ACCEPT` อยู่ไหม ไม่ได้ดูว่าผู้เรียกเป็นหนึ่งในนั้นหรือเปล่า และเพราะคำขอเดียวเขียนถึงสมาชิกทุกคน คนนอกที่รู้ `group_id` จึงกดส่งแทนทั้งกลุ่มพร้อมทับไฟล์ของทุกคนได้ |
| **ของใหม่** | `403` พร้อม `ส่งงานกลุ่มได้เฉพาะกลุ่มที่ตัวเองเป็นสมาชิกเท่านั้น` |
| **เหตุผล** | ทางกลุ่มเป็น endpoint ที่คำขอเดียวเปลี่ยนงานของหลายคนพร้อมกัน ความเสียหายจากการไม่ตรวจจึงคูณด้วยจำนวนสมาชิก แยกข้อความออกจากข้อ 1 เพราะทางออกของนักศึกษาต่างกัน — อย่างหนึ่งคือกดผิดงาน อีกอย่างคือยังไม่ได้ตอบรับคำเชิญ |
| **frontend** | ไม่ต้องแก้ — `group_id` มาจาก `studentGroupWork` ที่โหลดมาจากกลุ่มของนักศึกษาคนนั้นเอง |

### 3. ทางกลุ่มคืนรายละเอียดของงานที่ผู้เรียกเป็นเจ้าของเท่านั้น

| | |
| --- | --- |
| **ของเดิม** | หลังเขียนเสร็จ ทางกลุ่มคืน `getStudentActivityDetail(student_activity_id)` ตามเลขที่ body ส่งมาโดยไม่ตรวจ สมาชิกกลุ่มจึงขออ่านรายละเอียดงานของเพื่อนร่วมกลุ่ม — หรือของคนนอกกลุ่ม — ผ่านคำขอส่งงานได้ |
| **ของใหม่** | เลขที่ระบุต้องเป็นแถวของ session เอง ไม่งั้น `403` ข้อความเดียวกับข้อ 1 |
| **เหตุผล** | การเป็นสมาชิกกลุ่มตอบคำถามว่า "เขียนได้ไหม" ไม่ได้ตอบว่า "อ่านของใครได้" คำตอบของ endpoint เป็นทางอ่านทางหนึ่ง จึงต้องผ่านการตรวจเหมือนกัน |
| **frontend** | ไม่ต้องแก้ — ด้วยเหตุผลเดียวกับข้อ 1 |

### สิ่งที่ **ไม่ได้** ทำใน ticket นี้

- **ไม่ได้แตะการให้คะแนนกลุ่ม** — `POST /student-activity/grade` ยังรวมสมาชิกที่ยัง
  ไม่ตอบรับคำเชิญ ตามที่บันทึกไว้ตั้งแต่ #14 เป็นการตัดสินเชิงนโยบายคนละเรื่อง
- **ไม่ได้ตรวจกำหนดส่ง** — ส่งหลังเดดไลน์ยังส่งได้และถูกนับว่าสายตาม #15

---

## #39 — ช่วงคะแนนมีตัวตนของตัวเอง

`PUT /activity` จับคู่ช่วงคะแนนด้วย `level_no` ซึ่งเป็นตำแหน่ง ไม่ใช่ตัวตน อาจารย์ลบ
ช่วงคะแนนหนึ่งช่วง ปุ่มในหน้าจอเรียงเลขที่เหลือใหม่ และช่วงที่เก็บไว้ก็กลับมาในเลขของ
ช่วงอื่น ตอนนี้ `rubric_levels.id` เดินทางไป-กลับกับ form แล้ว เหตุผลและขอบเขตอยู่ใน
[ADR-0010](docs/adr/0010-rubric-level-identity.md)

รายการนี้เคยอยู่ในหัวข้อ "สิ่งที่ **ไม่ได้** เปลี่ยน" ตั้งแต่ #25 — ตอนนี้ย้ายขึ้นมาเป็น
การเปลี่ยนพฤติกรรมจริง

### 1. `PUT /activity` รับ `id` ของช่วงคะแนน และใช้มันเป็นตัวตน

| | |
| --- | --- |
| **ของเดิม** | ช่วงคะแนนจับคู่ด้วย `level_no` อย่างเดียว ลบช่วงกลางของสามช่วงแล้วอาจารย์ได้ผลสองอย่างพร้อมกัน: ช่วงบนสุดที่ตั้งใจเก็บไว้ถูกลบทิ้งพร้อมคะแนนที่ให้ไว้ (เพราะไม่มีใครส่งเลข 3 มาอีก) และช่วงกลางที่ตั้งใจลบกลับอยู่ต่อโดยรับคำบรรยายของช่วงบนสุดมาแทน คะแนนที่ให้ไว้ที่ช่วงกลางจึงกลายเป็นคะแนนของช่วงบนสุดโดยไม่มีอะไรบอก |
| **ของใหม่** | ช่วงคะแนนใน `rubric` ส่ง `id` กลับมาได้ และ `id` คือสิ่งที่บอกว่าเป็นแถวไหน ช่วงที่เก็บไว้คงแถวเดิม คงคะแนนเดิม และคงคำบรรยายของตัวเอง ไม่ว่าจะย้ายไปอยู่ลำดับที่เท่าไร ส่วนช่วงที่อาจารย์ลบจริง ๆ ก็หายไปพร้อมคะแนนที่ให้ไว้ที่ช่วงนั้น ตามข้อ 1 ของ #25 |
| **เหตุผล** | คะแนนที่ตรวจไปแล้วคือคำพูดของอาจารย์ การแก้ตารางหนึ่งช่องไม่ควรทำให้คำพูดนั้นเปลี่ยนความหมาย และไม่ควรทำให้มันหายไปด้วย |
| **ขอบเขต** | `id` เป็น optional แบบเดียวกับ `id` ของเกณฑ์ — รูบริกที่ส่งกลับมาโดยไม่มี `id` เลยยังจับคู่ด้วย `level_no` เหมือนเดิมทุกประการ ส่วน `POST /activity` ไม่มีอะไรให้จับคู่ จึงไม่เปลี่ยน |
| **frontend** | **ต้องแก้ และแก้แล้วใน ticket นี้** — `GET /activity` คืน `rubric_levels.id` มาตั้งแต่แรก แต่ `handleInitEditForm` ใน `features/teacher/activity/components/rubric-form.tsx` ทิ้งมัน ตอนนี้เก็บไว้ และ `utils/rubric-payload.ts` ส่งมันออกไปพร้อมกับ `id` ของเกณฑ์ client อื่นที่ไม่ส่ง `id` ไม่พัง แต่ก็ไม่ได้อะไรเพิ่ม |

### 2. สาม `400` ใหม่ของช่วงคะแนน

| | |
| --- | --- |
| **ของเดิม** | ไม่มีการตรวจช่วงคะแนนเลย `level_no` ที่ซ้ำกันในเกณฑ์เดียวทำให้ตัวหลัง `upsert` ทับตัวหน้าเงียบ ๆ อาจารย์ได้ช่วงคะแนนน้อยกว่าที่หน้าจอแสดงหนึ่งช่วง |
| **ของใหม่** | `400` สามแบบ — `มีระดับคะแนนเดียวกันถูกส่งมาซ้ำ` (`id` ซ้ำ), `มีระดับคะแนนบางรายการที่ไม่ใช่ของเกณฑ์นี้` (`id` ของเกณฑ์อื่น), `มีระดับคะแนนซ้ำลำดับกันในเกณฑ์เดียวกัน` (`level_no` ซ้ำ) |
| **เหตุผล** | สองข้อแรกคือกฎของ `assertOwnRubric` จาก #25 ลงมาอีกชั้น: `id` ไม่ได้บอกว่าตัวเองเป็นของเกณฑ์ไหน เขียนโดยไม่ตรวจแปลว่าการบันทึกเกณฑ์หนึ่งไปเรียงเลขของอีกเกณฑ์หนึ่งใหม่ได้ ข้อสามเป็นของใหม่ — พอจับคู่ด้วย `id` แล้ว สองแถวที่ลำดับเดียวกันจะชนกันที่ unique constraint และได้ `500` แทนที่จะทับกันเงียบ ๆ |
| **ขอบเขต** | ตรวจก่อนการเขียนทุกอย่างในคำขอ เหมือน `assertOwnRubric` และด้วยเหตุผลเดียวกัน: `createAttachments` อัปโหลดขึ้น MinIO นอก transaction การปฏิเสธหลังจากนั้นจะทิ้งไฟล์ค้าง ข้อสามใช้กับ `POST /activity` ด้วย (ที่นั่นเคยตอบ `500` จาก unique constraint) ส่วนสองข้อแรกเป็นของ `PUT` อย่างเดียวเพราะ `POST` ไม่มี `id` ให้ส่งมา |
| **frontend** | ไม่ต้องแก้ — form ไม่เคยส่ง `id` ซ้ำ และตารางกว้างเท่ากันทุกแถว จึงไม่ส่ง `level_no` ซ้ำด้วย มีทางเดียวที่ยังเป็นไปได้: กิจกรรมที่เกณฑ์แต่ละข้อมีจำนวนช่วงไม่เท่ากัน (สร้างจากทางอื่นที่ไม่ใช่ฟอร์มนี้) `rubric-table.tsx` วาดคอลัมน์ตามเกณฑ์ที่กว้างที่สุด ช่องที่เกินมาจึงได้ `level_no` จาก `initialValue` ซ้ำกับช่องสุดท้ายของแถวนั้น เดิมทีคำขอแบบนี้ก็ถูกปฏิเสธอยู่แล้วเพราะช่องที่เกินมาไม่มี `description` — ที่เปลี่ยนคือถ้าอาจารย์พิมพ์อะไรลงไปในช่องนั้น เมื่อก่อนข้อความจะถูกกลืนหายเงียบ ๆ ตอนนี้ได้ `400` บอกตรง ๆ |

### สิ่งที่ **ไม่ได้** ทำใน ticket นี้

- **ไม่ได้คำนวณ `student_activity.score` ใหม่** — คะแนนรวมยังมาจากรูบริกชุดที่เปลี่ยนไป
  แล้ว ตามที่ #25 ตัดสินไว้ว่าเป็นเรื่องของอาจารย์ที่ต้องตรวจใหม่เอง
- **ไม่ได้แตะการจับคู่ของ `POST /activity`** — ตอนสร้างยังไม่มีช่วงคะแนนให้จับคู่ ทุกช่วง
  เป็นของใหม่หมด สิ่งเดียวที่ endpoint นั้นได้จาก ticket นี้คือ `400` ในข้อ 2

---

## #40 — สองเส้นสุดท้ายที่อ่านข้อมูลส่วนตัวได้โดยไม่ต้องล็อกอิน

`GET /user` และ `GET /student/enrolled/subjects` ไม่มี middleware แม้แต่ตัวเดียว และ
รับ id ของ "คนที่จะดู" มาจาก query ใครก็ตามที่เดารหัส 8 ตัวถูก ก็ได้ชื่อ อีเมล เบอร์โทร
และตารางเรียนของคนนั้นไปโดยไม่ต้องมี cookie #20 ปิดช่องที่ "ลืมส่ง id แล้วได้ของทุกคน"
ไปแล้ว แต่ #20 ทำเรื่องรูปร่างของ input ไม่ใช่เรื่องสิทธิ์

รายการนี้เคยอยู่ในหัวข้อ "สิ่งที่ **ไม่ได้** เปลี่ยน" ตั้งแต่ #18 โดยเขียนไว้ว่าเป็นเรื่อง
เดียวกับ #31 — ซึ่งจริง แต่ #31 ปิดเฉพาะกลุ่ม `portfolio` สองเส้นนี้อยู่นอกกลุ่มนั้น
จึงตกค้างมา เหตุผลที่สองเส้นนี้ปิดคนละแบบ และราคาที่การเลือกแบบนั้นต้องจ่าย อยู่ใน
[ADR-0011](docs/adr/0011-self-read-parameter.md)

### 1. `GET /user` ต้องล็อกอิน และดูได้เฉพาะแถวของตัวเอง

| | |
| --- | --- |
| **ของเดิม** | ไม่ต้องล็อกอิน `?id=` เป็นของใครก็ได้ และคำตอบคือแถว `users` ทั้งแถว — ชื่อ อีเมล เบอร์โทร รหัสภาควิชาและหลักสูตร ส่วน id ที่ไม่ตรงกับใครเลยได้ `200` พร้อม `data: null` |
| **ของใหม่** | ไม่มี session ตอบ `401` ตามข้อความมาตรฐาน `ไม่พบ Token หรือ Token หมดอายุ` ส่วน `id` ที่ไม่ใช่ของ session ตอบ `403` `คุณไม่มีสิทธิ์เข้าถึงข้อมูลของผู้ใช้อื่น` — **รวมถึง id ที่ไม่ตรงกับใครเลย** ซึ่งเมื่อก่อนได้ `200` |
| **เหตุผล** | ตาม [ADR-0001](docs/adr/0001-portfolio-access.md): ผู้ใช้ที่ล็อกอินแล้วเอื้อมไปหยิบข้อมูลของคนอื่นได้ `403` และ "ไม่มีแถวนี้" กับ "ไม่ใช่แถวของคุณ" ตอบเหมือนกัน เพราะการแยกสองอย่างนี้ออกจากกันคือการบอกว่ารหัส 8 ตัวไหนมีอยู่จริง ซึ่งเป็นคำถามที่ endpoint นี้เคยตอบให้ฟรี ๆ |
| **ขอบเขต** | ใช้ `requireUser` ไม่ใช่ `requireRole("STUDENT")` — แถวใน `users` เป็นของทุกคนที่ล็อกอินได้ และอาจารย์ก็ต้องอ่านของตัวเองได้ `id` ยังอยู่ใน query ตามเดิม ไม่ได้ย้ายไปอ่านจาก session เพื่อให้สัญญาของ endpoint เปลี่ยนน้อยที่สุด |
| **frontend** | ไม่ต้องแก้ — ผู้เรียกทั้งสองที่คือ `hooks/use-portfolio.ts` และ `pages/personal-details/student-personal-details-page.tsx` ส่ง `homeSlice.studentId` ซึ่งเป็นรหัสของคนที่ล็อกอินอยู่เสมอ (`services/user.service.ts` กับ `services/auth.service.ts` มี `getUser` เหมือนกันสองสำเนา แต่มีแค่สำเนาแรกที่ถูกเรียก) |

### 2. `GET /student/enrolled/subjects` อ่านนักศึกษาจาก session

| | |
| --- | --- |
| **ของเดิม** | `?student_id=` บอกว่าจะดูตารางเรียนของใคร ไม่ต้องล็อกอิน ตารางเรียนของเพื่อนจึงห่างไปหนึ่ง query parameter |
| **ของใหม่** | `requireRole("STUDENT")` และนักศึกษามาจาก session — ไม่มี session ตอบ `401` ล็อกอินเป็นอาจารย์ตอบ `403` `สิทธิ์การเข้าถึงเฉพาะนักศึกษาเท่านั้น` ส่วน `student_id` ที่ยังส่งมาถูกเมิน ไม่มีผลต่อคำตอบ |
| **เหตุผล** | รูปเดียวกับ `/student/course/list`, `/classwork/list` และ `/calendar` ที่อยู่ใน router เดียวกัน และเป็นรูปที่ #26 ตัดสินไว้แล้วสำหรับ GET ของกลุ่มทำงาน ([ADR-0003](docs/adr/0003-enrolment-access.md)) — พารามิเตอร์ที่บอกว่า "ฉันคือใคร" ไม่ควรมาจากผู้เรียก |
| **ขอบเขต** | `enrolledSubjectsQuery` ถูกลบทิ้งทั้งตัว `400` สองแบบที่เคยมี (ไม่ส่ง `student_id` / ส่งมาสองครั้ง) จึงหายไปด้วย ไม่มีอะไรให้ตรวจแล้ว |
| **frontend** | **ต้องแก้ และแก้แล้วใน ticket นี้** — `services/student.service.ts` `getEnrolledSubjects()` เลิกรับพารามิเตอร์ และผู้เรียกเดียวของมันคือ `features/student/portfolio/components/experience-skills/create-work-form.tsx` |

### สิ่งที่ **ไม่ได้** ทำใน ticket นี้

- **ไม่ได้ปิดอีกสามเส้นใน `/student` ที่ยังไม่มี middleware** — `GET /student/list`,
  `GET /student/activities/list` และ `GET /student/activities/details/:id` แยกเป็น
  [#41](https://github.com/khthana/Deep-Portfolio/issues/41) เพราะต้องตัดสินนโยบาย
  เพิ่มก่อน: `/list` เป็นหน้าจอของอาจารย์ที่ต้องผูกกับการเป็นเจ้าของ section ตาม
  ADR-0002 ส่วน `/activities/details/:id` ต้องตอบก่อนว่า portfolio สาธารณะ
  (`GET /portfolio/public/:token` ที่ #31 ตั้งใจเปิดไว้) ต้องอ่านรายละเอียดงานได้ไหม
  — **ปิดแล้ว** ดูหัวข้อ #41 ข้างล่าง คำถามเรื่อง portfolio สาธารณะตอบว่า "ไม่"
  เพราะข้อสมมติผิด: เส้นนั้นเรียกชั้น service ตรง ๆ ไม่ได้วิ่งผ่าน HTTP เส้นนี้
- **ไม่ได้รวมสำเนา `getUser` สองไฟล์ฝั่ง web** — `services/user.service.ts` กับ
  `services/auth.service.ts` มีฟังก์ชันเดียวกันตัวอักษรต่อตัวอักษร เป็นเรื่องของ
  ความสะอาด ไม่ใช่ของสิทธิ์

---

## #42 — แถวที่ไม่มีอยู่ ตอบ `404` ไม่ใช่ `500`

`update` และ `delete` ของ Prisma ไม่คืน `null` เมื่อหาแถวเป้าหมายไม่เจอ แต่ **throw**
`P2025` และ `errorHandler` ไม่รู้จักรหัสนั้น error ที่ไม่มีใครรู้จักจึงตกไปที่ `500`
ผู้เรียกที่ลบของที่ถูกลบไปแล้ว (เช่น เปิดค้างไว้สองแท็บ) จึงอ่านว่า "เซิร์ฟเวอร์พัง"
ทั้งที่ไม่มีอะไรพัง

รายการนี้ถูก pin ไว้ตั้งแต่ #16/#17 พร้อมเหตุผลว่าการแก้ที่ `errorHandler` เปลี่ยน
คำตอบของทุก endpoint พร้อมกัน จึงควรเป็น ticket ของตัวเอง — 47 จุดใน 19 ไฟล์ test ยืน
อยู่บน `500` เดิม เหตุผลของการแบ่งเป็นสองชั้น และเหตุผลที่ `P2002`/`P2003` ยังไม่ถูกแตะ
อยู่ใน [ADR-0012](docs/adr/0012-missing-row-status.md)

### 1. `PUT` และ `DELETE` ที่อ้างแถวที่ไม่มีอยู่ ตอบ `404`

| | |
| --- | --- |
| **ของเดิม** | `500` `เกิดข้อผิดพลาดภายในระบบ` — `P2025` ของ Prisma วิ่งถึง `errorHandler` โดยไม่มีใครรู้จัก |
| **ของใหม่** | `404` ทุกจุด สิบ controller ของกลุ่ม e-Portfolio ใช้ประโยคของตัวเองที่ `GET /:id` ใช้อยู่แล้ว รวมสิบเอ็ดประโยค (`portfolio-skill` มีทั้งของทักษะและของการเชื่อมโยงชิ้นงาน) (`ไม่พบรางวัลที่ต้องการ`, `ไม่พบการอบรมที่ต้องการ`, `ไม่พบทักษะที่ต้องการ` ฯลฯ) ส่วนเส้นที่ไม่มีประโยคของตัวเอง (`/activity`, `/learning-activity`, `/course/clo`, `/lesson-plan`, `/score-weight`, `/course-material`, `/student-activity/bookmark`, `/student-learning-activity/grade` และ `/bookmark`) ได้ `ไม่พบข้อมูลที่ต้องการ` จาก `errorHandler` |
| **เหตุผล** | `errorHandler` แปลง `P2025` เป็น `404` เป็นพื้น เพื่อให้ endpoint ที่ไม่มีใครนึกถึงก็ยังพูดถูก แล้ว `orNotFound()` ที่จุดเรียกส่งประโยคเฉพาะของ resource เข้าไปทับ เพื่อให้ `GET` กับ `DELETE` ที่ชี้แถวเดียวกันพูดเหมือนกัน ไม่ได้อ่าน `meta.cause` ของ Prisma มาเดาชื่อตาราง — เป็นภาษาอังกฤษ เป็นของภายใน และข้อ 3 ของ #20 ห้ามส่งอะไรของ Prisma ออกไปอยู่แล้ว |
| **ขอบเขต** | เฉพาะ `P2025` · `P2002` (unique) และ `P2003` (foreign key) ยังเป็น `500` เพราะเป็นคนละข่ายกันและต้องดูทีละจุดว่าคอลัมน์ไหนเป็นต้นเหตุถึงจะเขียนประโยคที่ใช้ได้จริง · `throw new Error(...)` ที่เขียนด้วยมือ (`Group not found`, `Invalid rubric data`, `Activity not found`) ก็ยังเป็น `500` สิบเอ็ดจุดที่ยังเป็น `500` มี comment ใน test บอกว่าเป็นรหัสอะไรหรือ throw ตัวไหน และทำไมยังอยู่ |
| **frontend** | ไม่ต้องแก้ — ไม่มีผู้เรียกไหนแยกสาขาตาม `500` และ `axios` ก็โยน error ของทั้ง `4xx` และ `5xx` เหมือนกันอยู่แล้ว สิ่งที่เปลี่ยนคือข้อความที่แสดง กลายเป็นประโยคไทยที่ตรงกับเรื่องที่เกิด |

### 2. `POST /activity` และ `PUT /activity` ตอบ `400` เมื่อหมวดคะแนนไม่มีอยู่จริง

| | |
| --- | --- |
| **ของเดิม** | `score_ratio_id` ที่ไม่ตรงกับแถวไหนใน `subject_score_ratio` ทำให้ `connect` โยน `P2025` แล้วได้ `500` |
| **ของใหม่** | `400` พร้อม `ไม่พบประเภทสัดส่วนคะแนนที่เลือก` และไม่มีแถวไหนถูกเขียน |
| **เหตุผล** | ตาม [ADR-0009](docs/adr/0009-submission-ownership.md): กิจกรรมคือแถวที่ผู้เรียก**อ้างถึง** ส่วนหมวดคะแนนคือ**ค่าที่ผู้เรียกส่งมา** ทั้งสองอย่างโยน `P2025` ตัวเดียวกันในคำสั่ง `update` เดียวกัน ถ้าไม่แยกออกจากกันก่อน กฎในข้อ 1 ก็จะมีข้อยกเว้นที่ต้องจำ service จึงถามว่าหมวดคะแนนมีอยู่จริงไหมก่อนเขียน — ลบข้อยกเว้นทิ้งที่ต้นทาง แทนที่จะเพิ่มเงื่อนไขให้ `errorHandler` |
| **ขอบเขต** | การถามอยู่**ใน transaction ก่อนคำสั่งเขียนแรก** คำขอที่ถูกปฏิเสธจึงไม่ทิ้งกิจกรรมเปล่าไว้ และไม่แตะกิจกรรมเดิมใน `PUT` · ไม่ได้แตะ `score_ratio_id ?? 0` ของ `POST /mapping/activity` ซึ่งเป็นคนละจุดและยัง pin ไว้ตามเดิม |
| **frontend** | ไม่ต้องแก้ — `activity-detail-form.tsx` เลือกหมวดคะแนนจากรายการที่ API ส่งมาให้ ค่าที่ไม่มีอยู่จริงจึงเกิดได้เฉพาะเมื่อมีคนลบหมวดนั้นทิ้งระหว่างที่ฟอร์มเปิดค้าง |

### รายการที่เคย pin ไว้ และปิดแล้วในรอบนี้

- **`PUT` / `DELETE` ที่อ้าง id ที่ไม่มีอยู่ ตอบ `500` ไม่ใช่ `404`** (#16, #17) — ตามข้อ 1

### สิ่งที่ **ไม่ได้** ทำใน ticket นี้

- **ไม่ได้ให้สถานะกับ `P2002` และ `P2003`** — `409` และ `400` เป็นคำตอบที่น่าจะถูก
  สำหรับทั้งคู่ แต่ประโยคที่ผู้ใช้อ่านต้องบอกว่า**คอลัมน์ไหน**ซ้ำหรือชี้ไปที่ไม่มีอะไร
  ถึงจะใช้ได้จริง ซึ่งต้องดูทีละจุด การเหมารวมเป็นสถานะเดียวคือการเปลี่ยน `500` ที่มี
  ความหมายให้เป็น `4xx` ที่ไม่มี
- **ไม่ได้แก้ `throw new Error("Activity not found")` ใน
  `activity-clo-mapping.service.ts`** — throw เดียวกันนี้ครอบทั้ง "กิจกรรมไม่มีอยู่"
  และ "กิจกรรมมีอยู่แต่ไม่มีคะแนน" `404` จึงถูกกับกรณีเดียวและผิดกับอีกกรณี การแยกสอง
  อย่างนี้ออกจากกันเป็นงานคนละชิ้น
- **ไม่ได้เพิ่มการตรวจความเป็นเจ้าของที่ไหน** — `404` ที่เพิ่มมาอยู่หลัง
  `requireOwnEntry` เสมอ และ middleware ตัวนั้นเขียนว่า
  `if (row && row.user_id !== session)` มาตั้งแต่ต้น แถวที่ไม่มีอยู่จึงผ่านมาถึง
  controller เหมือนเดิม สิ่งที่เปลี่ยนคือคำตอบที่ปลายทาง จาก `500` เป็น `404`
  ส่วน "ไม่ใช่แถวของคุณ" ยังเป็น `403` ที่ middleware เท่าเดิม

---

## #41 — สามเส้นสุดท้ายใน `/student` ที่ไม่มี middleware

หลัง #40 ยังเหลือสามเส้นใน `student.route.ts` ที่เปิดให้ผู้เรียกที่ **ไม่ได้ล็อกอิน**
เลย: รายชื่อนักศึกษาทั้งกลุ่มเรียน รายละเอียดงานที่ส่งพร้อมคะแนนและความเห็นอาจารย์
และสถานะการส่งงานทุกชิ้นของนักศึกษาคนไหนก็ได้ สิ่งเดียวที่กั้นอยู่คือตัวเลข
`section_id` ซึ่งเป็น autoincrement และรหัสนักศึกษาซึ่งพิมพ์อยู่บนบัตร

ทั้งสามเส้นได้ middleware ที่มีอยู่แล้ว ไม่ได้เขียนกฎใหม่ — แต่ละเส้นได้กฎของเพื่อนบ้าน
ตัวเอง เหตุผลของแต่ละข้ออยู่ใน [ADR-0013](docs/adr/0013-student-read-access.md)

### 1. `GET /student/list` เป็นของอาจารย์ผู้สอน section นั้น

| | |
| --- | --- |
| **ของเดิม** | ไม่มี middleware — ใครก็ได้ที่ส่ง `section_id` มา ได้ชื่อและรหัสของนักศึกษาทุกคนในกลุ่มเรียนนั้นไป โดยไม่ต้องมี cookie |
| **ของใหม่** | `requireRole("TEACHER")` แล้วต่อด้วย `requireOwnSection("query")` — ไม่มี session ตอบ `401` `ไม่พบ Token หรือ Token หมดอายุ` ล็อกอินเป็นนักศึกษาตอบ `403` `สิทธิ์การเข้าถึงเฉพาะอาจารย์เท่านั้น` เป็นอาจารย์ที่ไม่ได้สอน section นั้นตอบ `403` `คุณไม่มีสิทธิ์เข้าถึงข้อมูลของกลุ่มเรียนนี้` |
| **เหตุผล** | ผู้เรียกทั้งหมดฝั่ง web อยู่ใต้ `features/teacher/` ไม่มีหน้าจอของนักศึกษาไหนเรียกเลย และเมื่อเป็นของอาจารย์ [ADR-0002](docs/adr/0002-section-access.md) ก็ตอบไว้แล้วว่าเอื้อมถึง section ที่ตัวเองสอน |
| **ขอบเขต** | `section_id` ที่ไม่มีอยู่จริงตอบ `403` เท่ากับ "ไม่ได้สอน section นี้" ตามที่ `sectionRule` ตั้งใจไว้ — การแยกสองอย่างนี้ออกจากกันคือการบอกว่าเลขไหนมีอยู่จริง |
| **frontend** | ไม่ต้องแก้ — `student-table.tsx` และ gradebook ส่ง `homeSlice.selectedCourse.section_id` ซึ่งมาจากรายวิชาที่อาจารย์คนนั้นสอนอยู่แล้ว |

### 2. `GET /student/activities/details/:student_activity_id` เป็นของเจ้าของงาน

| | |
| --- | --- |
| **ของเดิม** | ไม่มี middleware — id ที่เป็นเลขรันจึงเปิดคะแนน ความเห็นอาจารย์ และไฟล์แนบของงานชิ้นไหนก็ได้ให้ใครก็ได้ |
| **ของใหม่** | `requireRole("STUDENT")` แล้วต่อด้วย `requireOwnEntry(entryOwner.studentActivity, "student_activity_id")` — งานของคนอื่นตอบ `403` `คุณไม่มีสิทธิ์เข้าถึงข้อมูลของผู้ใช้อื่น` |
| **เหตุผล** | ข้อสมมติใน issue ที่ว่า portfolio สาธารณะเรียกเส้นนี้ไม่จริง: `portfolio.service.ts` เรียก `getActivityDetailsByStudentActivityId()` ที่ชั้น service ตรง ๆ ส่วนฝั่งอาจารย์ใช้ `/student-activity` คนละเส้นกัน ผู้เรียกที่เหลือมีที่เดียวคือ e-Portfolio ของเจ้าของเอง กฎที่ถูกจึงเป็นกฎของ [ADR-0001](docs/adr/0001-portfolio-access.md) |
| **ขอบเขต** | งานที่ไม่มีอยู่ยังตอบ `404` `ไม่พบงานที่ต้องการ` ที่ controller เหมือนเดิม เพราะ `requireOwnEntry` เขียนว่า `if (row && …)` แถวที่ไม่มีจึงผ่าน middleware ไป · `requireOwnEntry` รับชื่อ path parameter เป็นอาร์กิวเมนต์ตัวที่สอง (default `"id"`) เส้นอื่นทั้ง 56 เส้นไม่ต้องแก้ |
| **frontend** | ไม่ต้องแก้ — `getActivityDetails()` ส่ง id มาทาง path อยู่แล้ว และผู้เรียกทั้งสี่ที่ (`create-work-form.tsx`, `work-card.tsx`, `use-portfolio.ts`, `student-work-detail-page.tsx`) เป็นหน้าจอ e-Portfolio ของเจ้าของเอง สองที่หลังอยู่บนเส้นทางที่เปิดสาธารณะได้ แต่ออกจากฟังก์ชันก่อนถึงบรรทัดนี้เมื่อมี `shareToken` |

### 3. `GET /student/activities/list` อ่านนักศึกษาจาก session

| | |
| --- | --- |
| **ของเดิม** | ไม่มี middleware และ `student_id` มาจาก query — สถานะการส่งงาน คะแนน และความเห็นอาจารย์ของเพื่อนทั้ง section ห่างไปหนึ่ง query parameter ยิ่งกว่านั้น `where: { student_id: undefined }` ของ Prisma ไม่ใช่ตัวกรองที่ไม่ตรงกับอะไรเลยแต่**ไม่มีตัวกรอง** ผู้เรียกที่ไม่ส่งพารามิเตอร์นี้จึงได้ของทุกคนใน section พร้อมกัน |
| **ของใหม่** | `requireRole("STUDENT")` นักศึกษามาจาก session และ `requireEnrolledSection("query")` ตรวจ section — ไม่มี session ตอบ `401` ล็อกอินเป็นอาจารย์ตอบ `403` `สิทธิ์การเข้าถึงเฉพาะนักศึกษาเท่านั้น` section ที่ไม่ได้ลงทะเบียนตอบ `403` `คุณไม่มีสิทธิ์เข้าถึงข้อมูลของกลุ่มเรียนนี้` ส่วน `student_id` ที่ยังส่งมาถูกเมิน ไม่มีผลต่อคำตอบ |
| **เหตุผล** | คำถามนี้มีสองครึ่ง — "ฉันคือใคร" กับ "section ไหน" — ครึ่งแรกมาจาก session ตามอีกห้าเส้น "เรื่องของฉัน" ใน router เดียวกัน ([ADR-0011](docs/adr/0011-self-read-parameter.md)) ครึ่งหลังมาจากคำขอแล้วให้การลงทะเบียนเรียนเป็นตัวตัดสิน ([ADR-0003](docs/adr/0003-enrolment-access.md)) |
| **ขอบเขต** | `sectionActivitiesQuery` เหลือ `section_id` อย่างเดียว `400` ที่ #20 ใส่ไว้สำหรับ `student_id` จึงหายไปด้วย เพราะไม่มีอะไรให้ตรวจแล้ว |
| **frontend** | **ต้องแก้ และแก้แล้วใน ticket นี้** — `services/student.service.ts` `getActivitiesBySectionId()` เลิกรับ `studentId` และผู้เรียกเดียวของมันคือ `features/student/portfolio/components/experience-skills/create-work-form.tsx` (ไฟล์เดียวกับที่ #40 แก้) |

### รายการที่เคย pin ไว้ และปิดแล้วในรอบนี้

- **`GET /student/activities/list` ที่ไม่ส่ง `student_id` คืนของทุกคนใน section**
  (#18) — ตามข้อ 3 ไม่มี `student_id` ให้ส่งอีกแล้ว
- **สามเส้นใน `/student` ที่ยังไม่มี middleware** (#40) — ตามข้อ 1–3 ทั้งสามเส้น

### สิ่งที่ **ไม่ได้** ทำใน ticket นี้

- **ไม่ได้เปิดให้อาจารย์อ่านงานผ่านข้อ 2** — อาจารย์มีเส้นของตัวเองอยู่แล้วที่
  `/student-activity` ซึ่ง #38 ปิดไปแล้ว การเพิ่มสาขาที่สองให้ `requireOwnEntry`
  ที่นี่คือการเขียนกฎให้ผู้เรียกที่ไม่มีอยู่จริง
- **ไม่ได้รวม `getActivityDetails` ฝั่ง web สองทางเข้าด้วยกัน** — `student.service.ts`
  กับ `student-activity-service.service.ts` อ่านรายละเอียดงานคนละเส้น เพราะสองเส้นนั้น
  มีกฎสิทธิ์คนละข้อกันจริง ๆ ไม่ใช่ของซ้ำ

---

## #49 — router ตัวสุดท้ายที่ไม่มีการตรวจสิทธิ์เลย

หลัง #30, #31, #36, #37, #38, #40 และ #41 ปิดที่เหลือไปหมดแล้ว `rubric.route.ts`
เป็น router ตัวสุดท้ายที่ไม่มี middleware ตรวจสิทธิ์แม้แต่ตัวเดียว**โดยไม่ได้ตั้งใจ**
มันไม่เคยอยู่ในรายการ pin เพราะ #13 ที่เขียน test ให้เส้นคู่นี้มองว่ามันเป็น
"แคตตาล็อก" และเขียนไว้ในหัวไฟล์ test ตรง ๆ ว่า "Nothing here checks a session"

เหตุผลของทั้งสองข้อข้างล่างอยู่ใน
[ADR-0014](docs/adr/0014-shared-rubric-access.md)

### 1. `GET /rubric/shared-rubric` เป็นของอาจารย์

| | |
| --- | --- |
| **ของเดิม** | ไม่มี middleware — ใครก็ได้ที่ส่ง `program_id` มา ได้เกณฑ์การประเมินทุกตัวของหลักสูตรนั้นไป โดยไม่ต้องมี cookie และ `program_id` ก็คือรหัสหลักสูตรที่ประกาศอยู่ในเอกสารหลักสูตร |
| **ของใหม่** | `requireRole("TEACHER")` — ไม่มี session ตอบ `401` `ไม่พบ Token หรือ Token หมดอายุ` ล็อกอินเป็นนักศึกษาตอบ `403` `สิทธิ์การเข้าถึงเฉพาะอาจารย์เท่านั้น` |
| **เหตุผล** | ผู้เรียกฝั่ง web ทั้งหมดอยู่ใต้ `features/teacher/activity/` — modal เลือกเกณฑ์สำเร็จรูปในหน้าจอสร้างกิจกรรม ไม่มีหน้าจอของนักศึกษาไหนเรียกเลย `requireUser` จึงกว้างเกินผู้เรียกที่มีอยู่จริง |
| **ขอบเขต** | ไม่ได้แคบลงไปถึง "อาจารย์ในหลักสูตรนั้น" กฎแบบนั้นต้องยืนบน `users.program_id` ซึ่งตัวนำเข้าไม่ได้บังคับให้กรอก ไม่มี UI ไหนเขียน และไม่มีโค้ดไหนอ่าน — กฎที่ยืนบนคอลัมน์ว่างจะปฏิเสธอาจารย์ที่มีสิทธิ์จริงทุกคน |

### 2. `GET /rubric/shared-rubric/detail` เป็นของอาจารย์

| | |
| --- | --- |
| **ของเดิม** | ไม่มี middleware — `rubric_id` เป็น autoincrement เลขน้อย ๆ เกณฑ์ย่อย คำอธิบายทั้งสี่ระดับ และน้ำหนักคะแนนของเกณฑ์ตัวไหนก็ได้จึงห่างไปหนึ่งการเดาเลข |
| **ของใหม่** | `requireRole("TEACHER")` เหมือนข้อ 1 ทั้งรหัสและข้อความ |
| **เหตุผล** | เส้นคู่กันกับข้อ 1 ผู้เรียกชุดเดียวกัน ไม่มีเหตุให้กฎต่างกัน |
| **ขอบเขต** | `rubric_id` ที่ไม่ตรงกับเกณฑ์ไหนเลยยังตอบ `200` พร้อมรายการว่างเหมือนเดิม ไม่ใช่ `404` — endpoint นี้เป็น `findMany` บนตารางเกณฑ์ย่อย ไม่เคยอ่านตัวเกณฑ์เอง [ADR-0012](docs/adr/0012-missing-row-status.md) จึงไม่บังคับ `404` ที่นี่ |

### frontend

**ไม่ต้องแก้** — `lib/axios.ts` ตั้ง `withCredentials: true` ไว้ทั้งตัว และผู้เรียก
ทั้งสองที่ (`shared-rubric-title-table.tsx` กับ `shared-rubric-detail-table.tsx`
ผ่าน `rubric-service.service.ts`) วิ่งผ่าน `axiosInstance` อยู่แล้ว cookie จึงติด
ไปกับคำขอตั้งแต่ก่อน ticket นี้ ทั้งคู่อยู่ใน `shared-rubric-modal.tsx` ซึ่งเปิด
จากหน้าจอสร้างกิจกรรมของอาจารย์

ข้อควรรู้: ทั้งสอง component จบ `catch` ด้วย `console.error` เฉย ๆ ถ้าวันหนึ่งเส้น
นี้ตอบ `403` ผู้ใช้จะเห็นตารางว่างโดยไม่มีข้อความบอก แต่ในทางปฏิบัติไม่มีเส้นทางไหน
พาผู้ใช้ที่ไม่ใช่อาจารย์มาถึง modal นี้ได้ จึงไม่ได้แก้ในรอบนี้

### สิ่งที่ **ไม่ได้** ทำใน ticket นี้

- **ไม่ได้ทำให้เกณฑ์สำเร็จรูปเขียนได้** — ไม่มี endpoint ไหนในระบบเขียนแถวใน
  `rubrics` เลย เป็น master data ที่เข้ามาทางตัวนำเข้า และอยู่ในรายชื่อ 28 ตาราง
  ที่ไม่มีเส้นทางเขียนตามที่ `CLAUDE.md` บอกไว้
- **ไม่ได้แตะ `rubrics.program_id` ที่เป็น nullable** — เกณฑ์ที่นำเข้ามาโดยไม่ระบุ
  หลักสูตรจะไม่ปรากฏใน `program_id` ไหนเลย เป็นเรื่องของข้อมูล ไม่ใช่ของสิทธิ์
- **ไม่ได้ใส่ guard ให้ `group.routes.ts`** — เกณฑ์ยอมรับของ #49 เขียนไว้ว่าหลัง
  ticket นี้จะเหลือ `files.route.ts` ตัวเดียวที่ไม่มี guard ซึ่งไม่จริง
  `group.routes.ts` ก็ไม่มีเหมือนกัน แต่นั่นตั้งใจมาแต่เดิม (ดูรายการ pin ข้างล่าง
  — invite token คือสิทธิ์ทั้งหมด เพราะลิงก์ในอีเมลเปิดโดยคนที่อาจยังไม่ได้ล็อกอิน)
  สิ่งที่ทำคือเติมหมายเหตุในหัวไฟล์ให้ ตามเจตนาของเกณฑ์ข้อนั้น

---

## #43 — กิจกรรมที่ยังผูกกับ CLO ไม่ได้ เลิกตอบว่าเซิร์ฟเวอร์พัง

`POST /mapping/activity` มีทางออกที่จบเป็น `500` อยู่สามทาง และทั้งสามทางไม่มีอะไร
พังเลย — คำขอถูกต้องครบถ้วน ฐานข้อมูลปกติ สิ่งที่ขาดคือความพร้อมของตัวกิจกรรมเอง

สองในสามใช้ `throw new Error("Activity not found")` ตัวเดียวกันทั้งที่เป็นคนละข่าว
ส่วนตัวที่สามไม่ได้ `throw` เลย มันเขียน `0` ลงคอลัมน์ที่มี foreign key แล้วปล่อยให้
Postgres ปฏิเสธ เหตุผลของทั้งสามข้ออยู่ใน
[ADR-0015](docs/adr/0015-unmappable-activity.md)

### 1. กิจกรรมที่ไม่มีอยู่ ตอบ `404`

| | |
| --- | --- |
| **ของเดิม** | `throw new Error("Activity not found")` → `500` `เกิดข้อผิดพลาดภายในระบบ` |
| **ของใหม่** | `404` `ไม่พบกิจกรรมที่ต้องการ` ประโยคเดียวกับที่ `portfolio-activity` ใช้อยู่แล้ว |
| **เหตุผล** | กิจกรรมคือสิ่งที่คำขอนี้พูดถึง mapping ถูกเพิ่มเข้าไปในตัวมัน ส่วน `clo_id` คือค่าที่ mapping ชี้ออกไป เส้นแบ่งของ [ADR-0012](docs/adr/0012-missing-row-status.md) เป็นเรื่องของบทบาท ไม่ใช่ของตำแหน่งในคำขอ ถึงแม้ `activity_id` จะมาจาก body ก็ตาม |
| **ขอบเขต** | `clo_id` ที่ไม่ตรงกับ CLO ไหนยังเขียนผ่านเหมือนเดิม ตารางนี้ไม่มี foreign key บน `clo_id` |

### 2. กิจกรรมที่ยังไม่มีหมวดคะแนน ตอบ `400`

| | |
| --- | --- |
| **ของเดิม** | `score_ratio_id: activity?.score_ratio_id ?? 0` — `0` ไม่มีอยู่ใน `subject_score_ratio` Postgres ปฏิเสธด้วย foreign key แล้วกลายเป็น `500` ที่ไม่บอกอะไรเลยเกี่ยวกับปัญหาจริง |
| **ของใหม่** | ถามก่อนเขียน ไม่มีหมวดคะแนนตอบ `400` `กิจกรรมนี้ยังไม่ได้เลือกประเภทสัดส่วนคะแนน` |
| **เหตุผล** | คำขอมีรูปแบบถูกต้องครบถ้วน สิ่งที่ขาดคือข้อเท็จจริงในฐานข้อมูล ไม่ใช่สิทธิ์ที่ถูกปฏิเสธ (`403`) และไม่ใช่แถวที่ไม่มีอยู่ (`404`) ระบบนี้ใช้แค่สี่รหัส `4xx` ไม่มี `409`/`422` ที่ไหนเลย `400` จึงเป็นที่ที่มันลง โดยให้ประโยคเป็นตัวแบกความหมาย — [ADR-0015](docs/adr/0015-unmappable-activity.md) เป็นเจ้าของการตัดสินนี้เอง ส่วนทางเลือกที่จะเขียน `null` แทนถูกตัดออก เพราะคอลัมน์เป็น `Int` ไม่ใช่ `Int?` และไม่มี endpoint ไหนตามมาเติมค่าให้ |
| **ขอบเขต** | `?? 0` อีกสามจุด (`activity.service.ts:518`, `student-activity.service.ts:295,380`) ไม่ได้แตะ ตรงนั้นอยู่ใน `where` ของ `findUnique` ซึ่ง `0` เป็นคีย์ที่ไม่ตรงกับแถวไหนโดยตั้งใจ จุดที่ #43 แก้คือจุดเดียวที่ `0` ถูก**เขียน**ลงฐานข้อมูล |

### 3. กิจกรรมที่ยังไม่มีคะแนน ตอบ `400`

| | |
| --- | --- |
| **ของเดิม** | `throw` ตัวเดียวกับข้อ 1 → `500` และประโยคยังบอกว่า "ไม่พบกิจกรรม" ทั้งที่กิจกรรมมีอยู่ |
| **ของใหม่** | `400` `กิจกรรมนี้ยังไม่มีคะแนนให้แบ่งตามผลการเรียนรู้` |
| **เหตุผล** | ไม่มีคะแนนให้แบ่งระหว่าง CLO การปฏิเสธถูกอยู่แล้วแต่เดิม เปลี่ยนแค่สถานะกับประโยค |
| **ขอบเขต** | คะแนน `0` ยังนับว่าไม่มีคะแนนเหมือนเดิม `score_number` เป็น `Int?` ที่ `@default(0)` กิจกรรมที่ยังไม่มีใครใส่คะแนนจึงมาถึงเป็น `0` ไม่ใช่ `null` และทั้งสองค่าไม่มีอะไรให้แบ่ง · ถ้ากิจกรรมขาดทั้งหมวดคะแนนและคะแนน ผู้เรียกได้ประโยคของข้อ 2 มี test ยืนบนลำดับนี้โดยตรง |

### frontend

**ต้องตาม แต่ไม่ได้ทำใน ticket นี้** — `mapping-section.tsx` จับ error แล้วแสดง
`ไม่สามารถเพิ่มกิจกรรมได้ กรุณาลองใหม่อีกครั้ง` ตายตัว ผู้ใช้จึงยังไม่เห็นประโยคใหม่
ทั้งสาม และประโยคของ API ก็หายไปตั้งแต่ชั้น thunk แล้ว — `createAsyncThunk` ที่ไม่ใช้
`rejectWithValue` ส่งต่อแค่ `SerializedError` ซึ่ง `message` คือ `"Request failed
with status code 400"` ของ axios ไม่ใช่ body ที่ API ตอบ

นี่เป็นรูปแบบเดียวกันทั้ง 10 กว่า slice ในโปรเจกต์ (`action.error.message ??
"Something went wrong"`) การแก้จึงเป็นงานข้ามระบบของตัวเอง แยกไปเป็น
[#51](https://github.com/khthana/Deep-Portfolio/issues/51)

ที่ยังคุ้มถึงแม้หน้าจอยังไม่ตาม: `500` ในล็อกคือสัญญาณเตือนเท็จที่ส่งคนไปตามหาความ
ผิดพลาดของเซิร์ฟเวอร์ที่ไม่มีอยู่ ส่วน `400` ไม่ใช่

### สิ่งที่ **ไม่ได้** ทำใน ticket นี้

- **ไม่ได้แปลง `P2003` ทั้งระบบ** — ADR-0012 ข้อ 3 ยังค้างตามเดิม สิ่งที่ทำคือถาม
  ก่อนเขียนจนไม่มี `P2003` ให้เกิดที่เส้นนี้ `POST /mapping/learning-activity` ที่
  อ้าง learning activity ซึ่งไม่มีอยู่ยังเป็น `500` เหมือนเดิม
- **ไม่ได้ตรวจว่า CLO อยู่ section เดียวกับกิจกรรม** — ไม่เคยมีการตรวจนี้มาแต่เดิม
  และไม่มี foreign key รองรับ

## #50 — คำขอที่ล้มเหลวไม่ทิ้งไฟล์แนบไว้อีกแล้ว

`createAttachments` รับ `tx` ได้มาตลอด แต่เจ็ดจุดที่เรียกมันจากใน `$transaction`
ไม่ได้ส่งเข้าไป แถว `attachments` จึงเขียนด้วย client นอก transaction และ commit
ทันที คำขอที่ล้มเหลวหลังจากนั้นย้อนกลับได้เฉพาะสิ่งที่อยู่ใน transaction จริง ๆ

**ผู้เรียกไม่เห็นความต่างเลย** ทุก endpoint ตอบ status และ body เหมือนเดิม รวมทั้ง
`500` ของ `POST /course-material` ที่อ้างสัปดาห์ซึ่งไม่มีอยู่ สิ่งที่เปลี่ยนคือ
สถานะของฐานข้อมูลและของถังหลังคำขอนั้น เหตุผลทั้งหมดอยู่ใน
[ADR-0016](docs/adr/0016-attachment-upload-rollback.md)

### 1. แถว `attachments` ย้อนกลับพร้อม transaction

| | |
| --- | --- |
| **ของเดิม** | คำขอที่ล้มเหลวทิ้งแถว `attachments` ไว้โดยไม่มีตารางเชื่อมไหนชี้ถึง อ่านไม่เจอผ่าน endpoint ไหน และลบไม่ได้ผ่านหน้าจอ |
| **ของใหม่** | ไม่มีแถวค้าง — `createAttachments` ได้ `tx` แล้ว |
| **เหตุผล** | ตรงกับสิ่งที่ #34 ทำไว้ฝั่งลบ (ADR-0008) แค่กลับด้าน แถวที่ไม่มีใครชี้ถึงคือขยะที่ query เห็นแต่ผู้ใช้ไม่เห็น |
| **ทางที่กระทบ** | `POST /course-material`, `POST`/`PUT /activity`, `POST`/`PUT /learning-activity`, `POST /announcement` |

### 2. ไฟล์ใน MinIO ถูกเก็บคืนเมื่อ transaction ไม่ commit

| | |
| --- | --- |
| **ของเดิม** | ไฟล์ที่อัปโหลดไปแล้วค้างในถังถาวร ที่เดียวที่จด key ของมันไว้คือแถว `attachments` ที่กำลังจะหายไป |
| **ของใหม่** | `transactionWithUploads` จำ key ของทุก object ที่อัปโหลดในคำขอนั้น แล้วเรียก `removeFiles` เก็บคืนเมื่อ transaction ล้ม |
| **เหตุผล** | MinIO ไม่ได้อยู่ใน transaction ของ Postgres และย้อนกลับไม่ได้ ตามเก็บเป็นวิธีเดียวที่เหลือ และ `removeFiles` ถูกออกแบบมาให้พังเงียบอยู่แล้ว จึงไม่ไปเปลี่ยนว่าผู้เรียกได้ error ตัวไหน |
| **ขอบเขต** | best-effort ตาม ADR-0008 ถ้าถังตอบไม่สำเร็จ object ก็ยังค้างเหมือนเดิม |

### frontend

**ไม่ต้องแก้** — ไม่มี endpoint ไหนเปลี่ยน status หรือ body

### สิ่งที่ **ไม่ได้** ทำใน ticket นี้

- **สี่ทางส่งงานใน `student.service.ts` ไม่ได้เก็บ object คืน** ทั้งสี่ส่ง `tx`
  อยู่แล้วตั้งแต่ก่อนหน้านี้ แถวจึงไม่ค้าง แต่ไฟล์ที่อัปโหลดก่อน transaction ล้ม
  ยังค้างในถัง อยู่นอกขอบเขตที่ #50 ตั้งไว้ ยกไปเป็น **#52** เหตุผลอยู่ใน
  ADR-0016 ข้อ 2
- **13 จุดของ e-Portfolio ที่เรียก `createAttachments` นอก transaction** ไม่ได้แตะ
  ตรงนั้นไม่มี transaction ให้ขัดกัน เป็นปัญหาคนละข้อ
- **`P2003` ยังเป็น `500`** ADR-0012 ข้อ 3 ยังค้างตามเดิม

---

## #45 — คะแนนของกลุ่มไปถึงเฉพาะคนที่ตอบรับคำเชิญ

คำถาม "ใครอยู่ในกลุ่มนี้" ถูกถามอยู่สี่ที่ในระบบ การส่งงานกับการอ่านงานกรอง
`status: "ACCEPT"` ไว้แล้ว การให้คะแนนไม่กรอง คนที่ถูกใส่ชื่อไว้แต่ไม่เคยกดรับคำ
เชิญจึงได้ทั้งคะแนนและสถานะ `GRADED` ทั้งที่แถวของเขาไม่เคยถูกตั้งเป็น `SUBMITTED`
เพราะการส่งงานข้ามเขาไป การตัดสินและเหตุผลอยู่ใน
[ADR-0017](docs/adr/0017-group-membership-means-accepted.md)

**ผู้เรียกไม่เห็นความต่างในรูปของ response เลย** ทั้งสองเส้นยังตอบ `200` และ body
ชุดเดิม สิ่งที่เปลี่ยนคือ**แถวไหนบ้างที่ถูกเขียน**

### 1. `POST /student-activity/grade` แบบกลุ่ม ไม่แตะคนที่ยังไม่ตอบรับ

| | |
| --- | --- |
| **ของเดิม** | สมาชิกทุกคนที่มีชื่อใน `group_id` ได้ `status: "GRADED"`, `graded_at`, คะแนนรายเกณฑ์ และ feedback ไม่ว่าจะ `PENDING`, `ACCEPT` หรือ `REJECTED` |
| **ของใหม่** | เฉพาะ `status: "ACCEPT"` แถวของคนที่ยังไม่ตอบรับคงเป็น `NOT_SUBMITTED` และ `graded_at` ยังเป็น `null` |
| **เหตุผล** | งานกลุ่มถูกส่งในนามของคนที่ตอบรับ (`submitGroupActivity` กรองแบบเดียวกัน) การให้คะแนนงานที่ระบบเองบอกว่าไม่เคยถูกส่ง ทำให้แถวเดียวขัดแย้งกับตัวเอง |
| **ใครเสียคะแนน** | นักศึกษาที่ถูกเชิญเข้ากลุ่มแล้วไม่เคยกดลิงก์ในคำเชิญ จากเดิมได้คะแนนเท่ากับทั้งกลุ่ม ตอนนี้ไม่ได้เลยจนกว่าจะกดรับแล้วให้อาจารย์ตรวจอีกครั้ง — และคำเชิญมีอายุเจ็ดวัน พ้นจากนั้นยังไม่มีทางขอใบใหม่ ดู ADR-0017 ข้อ 1 |

### 2. คะแนน CLO ของงานกลุ่มไม่ถูกสร้างให้คนที่ยังไม่ตอบรับ

| | |
| --- | --- |
| **ของเดิม** | `calculateCloScore` ได้รายชื่อชุดเดียวกัน จึงสร้างแถว `activity_scores` ให้คนที่ไม่เคยตอบรับด้วย |
| **ของใหม่** | มีเฉพาะแถวของคนที่ `ACCEPT` |
| **ยังไม่มีใครเห็นความต่างนี้** | ไม่มีโค้ดที่ไหนใน `apps/api/src` หรือ `apps/web/src` อ่าน `activity_scores` เลย — `/gradebook` คิดจาก `student_activity` ตรง ๆ ไม่มีหน้าจอไหนเปลี่ยนตัวเลข สิ่งที่เปลี่ยนคือสิ่งที่ตารางนี้เก็บไว้รอวันที่มีคนอ่านมัน |

### 3. `POST /student-learning-activity/grade` แบบกลุ่ม ตอบแบบเดียวกัน

| | |
| --- | --- |
| **ของเดิม** | สมาชิกทุกคนใน `group_id` ได้ `GRADED`, `graded_at`, `feedback` และ `remark` |
| **ของใหม่** | เฉพาะ `status: "ACCEPT"` |
| **ขอบเขต** | ฝั่งนี้ไม่มีการคิดคะแนน CLO และไม่มีคะแนนเป็นตัวเลข สิ่งที่เปลี่ยนคือสถานะกับ feedback |

### frontend

**ไม่ต้องแก้** — ไม่มี endpoint ไหนเปลี่ยน status หรือรูปของ body

### สิ่งที่ **ไม่ได้** ทำใน ticket นี้

- **อาจารย์ยังไม่เห็นว่ามีสมาชิกที่ยังไม่ตอบรับค้างอยู่** ทั้งสิบสองเส้นของ
  `/student-activity-group` และคู่ฝั่ง learning-activity เป็น
  `requireRole("STUDENT")` ทั้งหมด และ `getGroupSubmissions` ที่อาจารย์ใช้ก็กรอง
  `ACCEPT` อยู่แล้ว หลังการเปลี่ยนนี้ อาจารย์จึงตรวจงานโดยไม่รู้ว่ามีใครหายไป
  จากรายชื่อ การแก้ต้องเปลี่ยนรูปของ response ที่ `apps/web` ต้องตามแก้ ยกไปเป็น
  **#53** เหตุผลอยู่ใน ADR-0017 ข้อ 2
- **การติดดาวงานกลุ่มยังเขียนลงทุกแถวเหมือนเดิม**
  `addStudentActivityGroupToBookmark` และคู่ของมันไม่กรอง `status` ตั้งใจปล่อยไว้
  ดาวเป็นที่คั่นหน้าของอาจารย์ ไม่ใช่คะแนน — ADR-0017 ข้อ 3
- **แถวที่เคยถูกให้คะแนนไปแล้วแบบเดิม ไม่ได้ตามแก้** ไม่มี migration ตามล้าง
  กติกาบังคับตั้งแต่การให้คะแนนครั้งถัดไป (ฐานข้อมูลจริงยังไม่มีกลุ่มและยังไม่มี
  การให้คะแนนใด ๆ)
- **ยังไม่มีทางส่งคำเชิญซ้ำ** โทเคนถูกตั้งอายุเจ็ดวันตอนสร้างกลุ่ม
  (`student-activity-group.service.ts`) และ `PATCH` รายชื่อยกใบเดิมมาให้ต่อ
  คนที่ลืมกดรับเกินเจ็ดวันจึงกลับเข้ากลุ่มไม่ได้เลย นี่คือ
  ราคาที่ ADR-0017 ข้อ 1 รู้ตัวว่าจ่าย และการแก้คือ endpoint ใหม่ ไม่ใช่การเลิก
  กรอง `ACCEPT` ยังไม่มี issue รองรับ
- **ทั้งสองเส้นยังยอมรับ `student_activity_id` ของคนที่ยังไม่ตอบรับ** เส้นทางหา
  กลุ่มจาก id ที่ส่งมาโดยไม่ดู `status` แล้วให้คะแนนสมาชิกที่ `ACCEPT` ตามปกติ
  คำขอแบบนั้นจึงได้ `200` พร้อมคะแนน ทั้งที่แถวที่ถูกอ้างถึงไม่ได้ถูกแตะเลย
  ในทางปฏิบัติอาจารย์หยิบ id มาจาก `getGroupSubmissions` ซึ่งคืนเฉพาะคนที่
  `ACCEPT` อยู่แล้ว จึงไม่ได้แก้ใน ticket นี้ — การปฏิเสธคำขอเป็นสถานะใหม่ที่
  ผู้เรียกต้องรับมือ และไม่อยู่ในเกณฑ์ยอมรับของ #45

---

## #46 — รางวัลที่สร้างโดยไม่บอกว่าจะแสดงหรือไม่ ถูกแสดง

`createPortfolioAward` เขียน `is_show: is_show === true` ค่าที่ไม่ได้ส่งมาจึงกลาย
เป็น `false` และ `@default(true)` ของคอลัมน์ไม่มีโอกาสได้ทำงาน เป็นที่เดียวในระบบ
ที่ service ตอบแทนคอลัมน์แล้ว**ตอบไม่ตรงกับคอลัมน์** — อีกหกส่วนของ e-Portfolio
ที่มีธงเดียวกันปล่อยผ่านหมด การแปลงถูกลบทิ้ง ไม่ได้ย้ายไปไว้ที่อื่น
การตัดสินและเหตุผลอยู่ใน
[ADR-0018](docs/adr/0018-absent-flag-means-column-default.md)

### 1. `POST /portfolio-award` ที่ไม่ส่ง `is_show` มา

| | |
| --- | --- |
| **ของเดิม** | `201` พร้อม `is_show: false` และแถวในฐานข้อมูลเป็น `false` รางวัลไม่ปรากฏใน e-Portfolio โดยไม่มีอะไรบอก |
| **ของใหม่** | `201` พร้อม `is_show: true` ตาม `@default(true)` ของคอลัมน์ |
| **เหตุผล** | คอลัมน์ที่คุมการมองเห็นของ e-Portfolio ทั้ง 21 ตัวมีค่าตั้งต้นเป็น "แสดง" และ `GET /portfolio` ก็แปล `null` ของธงระดับหัวข้อเป็น `true` อยู่แล้ว มีแต่ฝั่งเขียนของ award ที่ตอบคนละอย่าง (รายการรายแถวบนหน้าเว็บกรอง falsy ทิ้ง `null` จึงหายไปเท่ากับ `false` — ค่าที่เขียนตอนสร้างแถวคือคำตอบสุดท้าย) |
| **ใครเจอ** | ผู้เรียก API ตรง ๆ เท่านั้น หน้าเว็บส่ง `is_show: values.is_show ?? true` มาทุกครั้ง |

`is_show` ที่ส่งมาจริงยังถูกใช้ตรงตามที่ส่ง — `false` ยังสร้างรางวัลแบบซ่อนได้
เหมือนเดิม

### frontend

**ไม่ต้องแก้** — `create-award-competition-form.tsx` ส่งช่องนี้มาทุกครั้งอยู่แล้ว
และ `portfolio-award.service.ts` ฝั่งเว็บก็ใส่ลง `FormData` เมื่อค่าไม่ใช่
`undefined` ผลลัพธ์ของเส้นทางที่หน้าเว็บใช้จึงเท่าเดิมทุกกรณี (สาขา create ใน
`award-edit-modal.tsx` spread `...values` โดยไม่มี `?? true` แต่ modal นั้นเปิด
ได้เฉพาะเมื่อมีแถวเดิมอยู่แล้ว สาขานั้นจึงยังไม่เคยทำงาน)

### สิ่งที่ **ไม่ได้** ทำใน ticket นี้

- **`PUT /portfolio-award` ยังแปลว่า "ไม่ส่งมา = ไม่แก้"** และไม่ถูกแตะเลย
  `POST` ถามว่าแถวใหม่เริ่มด้วยค่าอะไร ส่วน `PUT` ถามว่าผู้ใช้ตั้งใจแก้อะไร ถ้า
  `PUT` เอาค่าตั้งต้นมาใช้ด้วย คำขอที่แก้แค่ชื่อรางวัลจะเปิดการมองเห็นของแถวที่
  เจ้าของตั้งใจซ่อนไว้ — ADR-0018 ข้อ 3
- **แถวเก่าที่ถูกสร้างแบบซ่อนไว้แล้ว ไม่ได้ตามแก้** ไม่มี migration ย้อนหลัง
  เพราะแยกไม่ออกว่าแถวไหนเป็น `false` เพราะเจ้าของกดซ่อน และแถวไหนเพราะข้อบกพร่อง
  นี้ การเดาแทนเจ้าของแล้วเปิดของที่เขาตั้งใจซ่อน แย่กว่าปล่อยไว้
- **อีกหกส่วนไม่ถูกแตะ** ทั้งหมดปล่อยให้ค่าตั้งต้นทำงานอยู่แล้ว ไม่มีการแก้เผื่อ
- **`POST /portfolio-skill/assign-work` ก็ไม่ถูกแตะ** `assignWorkToSkills`
  destructure ค่าตั้งต้นของ `isShow*` ทั้งสี่ตัวไว้เองเป็น `false` ซึ่งตรงกับ
  `@default(false)` ของคอลัมน์พอดี ผู้เรียกจึงไม่เห็นความต่าง เป็นสำเนาที่สอง
  แบบเดียวกับที่ award เป็น แต่ไม่ใช่ของเสีย — ADR-0018 บันทึกไว้ว่าให้เก็บกวาด
  ตอนที่มีคนแตะไฟล์นั้นด้วยเหตุผลอื่น

---

## #44 — รายการ "กลุ่มที่เคยสร้าง" เรียงเหมือนเดิมทุกครั้ง

`getStudentActivityGroupInSec` (และฝาแฝดฝั่ง learning activity) ยุบกลุ่มที่มีสมาชิก
ชุดเดียวกันให้เหลือตัวเดียวมาตั้งแต่โค้ดที่รับมอบ #44 ตั้งต้นว่านี่คือข้อบกพร่อง แต่
ผู้เรียกที่มีอยู่ที่เดียว — รายการ "กลุ่มที่เคยสร้าง" ในโมดัลสร้างกลุ่ม — ถามหา**ชุด
สมาชิกที่เคยร่วมงานเพื่อหยิบมาใช้ซ้ำ** ไม่ได้ถามหากลุ่ม การยุบจึงอยู่ต่อ สิ่งที่เป็น
ข้อบกพร่องจริงคือ `findMany` ไม่มี `orderBy` เลยสักชั้น เหตุผลทั้งหมดอยู่ใน
[ADR-0019](docs/adr/0019-group-list-is-member-lists.md)

### 1. `GET /student-activity-group/all` เรียงกลุ่มตาม `id`

| | |
| --- | --- |
| **ของเดิม** | ลำดับของกลุ่มใน response คือลำดับที่ Postgres คืนแถวมา และตัวที่รอดจากการยุบก็คือแถวแรกที่มาถึง ซึ่งไม่มีอะไรผูกไว้ว่าเป็นตัวไหน |
| **ของใหม่** | `orderBy: { id: "asc" }` กลุ่มมาจากเก่าไปใหม่ และ `group_id` ของชุดสมาชิกที่ถูกยุบ คือกลุ่มแรกที่เคยใช้ชุดนั้น |
| **เหตุผล** | คำตอบของ endpoint ไม่ควรขึ้นกับลำดับแถวที่ query planner เลือกให้ ในทางปฏิบัติมันออกมาตามลำดับที่แทรก ซึ่งเป็นความบังเอิญ ไม่ใช่สัญญา |
| **ใครเจอ** | ไม่มีใครในทางปฏิบัติ — ลำดับใหม่ตรงกับลำดับที่เคยได้มาโดยบังเอิญ |

### 2. สมาชิกในแต่ละชุด เรียงหัวหน้าก่อน แล้วตามรหัสนักศึกษา

| | |
| --- | --- |
| **ของเดิม** | ลำดับสมาชิกเป็นลำดับที่ Postgres คืนมา ในทางปฏิบัติคือลำดับที่ถูกแทรกตอนสร้างกลุ่ม |
| **ของใหม่** | `orderBy: [{ role: "asc" }, { student_id: "asc" }]` — enum ประกาศ `LEADER` ก่อน `MEMBER` หัวหน้ากลุ่มจึงมาก่อนเสมอ ที่เหลือเรียงตามรหัส |
| **เหตุผล** | `group-item.tsx` เอารหัสมา `join(", ")` แสดงเป็นบรรทัดให้เลือก ลำดับจึงเป็นส่วนหนึ่งของคำตอบ และเมื่อคลิกแล้ว `handleOnSelectedGroup` ก็จัดหัวหน้าขึ้นก่อนแล้วเรียงตามรหัสอยู่ดี การเรียงแบบเดียวกันทำให้ก่อนคลิกกับหลังคลิกไม่สลับตำแหน่งกัน |
| **ใครเจอ** | กลุ่มที่แถวสมาชิกถูกแทรกไว้ไม่ตรงกับลำดับนี้ จะเห็นสตริงรหัสในบรรทัดเรียงใหม่ |

ฝั่ง learning activity (`GET /student-learning-activity-group/all`) เปลี่ยนเหมือนกัน
ทุกอย่าง เพราะเป็นโค้ดชุดเดียวกันและลงที่ state ตัวเดียวกันในหน้าจอเดียวกัน

### frontend

**ไม่ต้องแก้** — `group-item.tsx` แสดงตามลำดับที่ได้มา และ `handleOnSelectedGroup`
เรียงใหม่เองหลังคลิก ไม่มีที่ไหนอ่าน `group_id` ของเส้นนี้ ส่วน `group_id` ที่ใช้ตอน
ส่งงานมาจาก `GET /student-activity-group` คนละเส้น

### สิ่งที่ **ไม่ได้** ทำใน ticket นี้

- **การยุบไม่ถูกเอาออก และ `activity_id` ไม่ถูกเพิ่ม** เกณฑ์ตรวจรับสองข้อแรกของ #44
  ขอไว้ทั้งคู่ แต่เขียนก่อนอ่านผู้เรียก และถูกถอนที่ตัว issue แทนที่จะทำตาม — สอง
  บรรทัดที่มีรหัสนักศึกษาชุดเดียวกันคือสองบรรทัดที่ผู้ใช้แยกไม่ออกอยู่ดี เพราะ
  response ไม่มีชื่อกิจกรรม มีแต่ id
- **ชื่อ endpoint กับชื่อ method ไม่ถูกเปลี่ยน** ทั้งคู่สัญญาว่าเป็น "กลุ่มทั้งหมด"
  ซึ่งไม่ตรงกับสิ่งที่ตอบ แต่การเปลี่ยน path เป็นการแก้สัญญากับหน้าเว็บเพื่อชื่อที่
  ตรงกว่าอย่างเดียว เจตนาถูกเขียนไว้เป็น comment เหนือ method ทั้งสองฝั่งแทน
- **`GET /student-activity-group` ยังไม่เรียงสมาชิก** เส้นที่ถามทีละกิจกรรมไม่ถูกแตะ
  ทั้งที่ `show-groupwork-modal.tsx` เอารายชื่อไปแสดงตามลำดับที่ได้มาเหมือนกัน
  ลำดับของสองเส้นจึงยังไม่รับประกันว่าตรงกัน — #44 ถามถึง `/all` เส้นเดียว และการ
  เปลี่ยนอีกเส้นต้องมาพร้อม test ของมันเอง

---

## #47 — ชิ้นงานที่อ้างใน mapping ต้องมีอยู่จริงและต้องเป็นของผู้เรียก

`portfolio_skill_activity_mapping.student_activity_id` ไม่มี foreign key และไม่มี
โค้ดตรงไหนตรวจ ทั้งสามเส้นที่เขียน mapping จึงรับ id อะไรก็ได้ ทั้ง id ที่ไม่มีตัวตน
และ id ของชิ้นงานคนอื่น อย่างหลังหนักกว่า เพราะ `GET /portfolio-skill/works` หยิบ
`feedback` ของทุกชิ้นงานที่ mapping ชี้ไปมาแสดง — คำวิจารณ์ที่อาจารย์เขียนถึงเพื่อน
จึงมาโผล่ในหน้าของคนที่รู้ id เท่านั้น เหตุผลทั้งหมดอยู่ใน
[ADR-0020](docs/adr/0020-mapping-names-own-submission.md)

### 1. id ที่ไม่มีชิ้นงานรองรับ ตอบ `400`

| | |
| --- | --- |
| **ของเดิม** | `POST /portfolio-skill`, `PUT /portfolio-skill/:id` และ `POST /portfolio-skill/assign-work` เขียนแถวสำเร็จ ตอบ `201`/`200` แล้ว `/works` ก็หา `feedback` ไม่เจอ คืน `null` |
| **ของใหม่** | `400` `{ "success": false, "message": "ไม่พบชิ้นงานที่เลือก" }` และไม่มีอะไรถูกเขียน รวมถึงตัวทักษะที่ `POST` สร้างในคำขอเดียวกัน เพราะการตรวจอยู่ก่อนการเขียนแถวแรก |
| **เหตุผล** | ชิ้นงานเป็นค่าหนึ่งในแถวที่กำลังเขียน ไม่ใช่แถวที่คำขอกระทำต่อ จึงเป็น `400` ตาม ADR-0012 ข้อ 2 ไม่ใช่ `404` ตาม ADR-0009 ข้อ 3 |
| **ใครเจอ** | ผู้เรียก API ตรงเท่านั้น หน้าเว็บเลือกชิ้นงานจากรายการที่ API ส่งมาให้เสมอ |

### 2. ชิ้นงานของคนอื่น ตอบ `403`

| | |
| --- | --- |
| **ของเดิม** | เขียนแถวสำเร็จเหมือนกัน แล้ว `/works` ก็ดึง `feedback` ของชิ้นงานนั้นมาแสดงในหน้าของผู้เรียก |
| **ของใหม่** | `403` `{ "success": false, "message": "มีชิ้นงานบางรายการที่ไม่ใช่ของผู้ใช้รายนี้" }` และไม่มีอะไรถูกเขียน |
| **เหตุผล** | เป็นการปฏิเสธ**สิทธิ์** ไม่ใช่ค่าที่ผิดรูป เท่ากับที่ `assertOwnSkills` ตอบให้ `skill_id` ในไฟล์เดียวกัน และเป็นช่องเดียวกับที่ #38 ปิดไปแล้วทางส่งงาน |
| **ใครเจอ** | ผู้เรียก API ตรงเท่านั้น ด้วยเหตุผลเดียวกับข้อ 1 — และสมาชิกกลุ่มไม่เจอ เพราะแต่ละคนมีแถว `student_activity` ของตัวเอง |

### 3. ลบกิจกรรม แถว mapping หายตามไปด้วย

| | |
| --- | --- |
| **ของเดิม** | `DELETE /activity` ลบ `student_activity` ทิ้ง แต่ mapping ยังอยู่ ชี้ไปยัง id ที่ว่างแล้ว |
| **ของใหม่** | foreign key ใหม่ (`fk_skill_activity_submission`, `ON DELETE CASCADE`) พา mapping ไปด้วย ตัว `portfolio_skill` เองไม่ถูกแตะ |
| **เหตุผล** | ตรงกับ `skill_id` ของตารางเดียวกัน และกับลูกอีกสามตัวของ `student_activity` ที่ CASCADE ทั้งหมด ราคาที่ยอมจ่ายคือข้อความสะท้อนที่นักศึกษาเขียนเองหายไปด้วย — ดู ADR-0020 ข้อ 1 |
| **ใครเจอ** | นักศึกษาที่เอาชิ้นงานของกิจกรรมที่อาจารย์ลบไปใส่แฟ้มสะสมผลงาน ผลงานชิ้นนั้นหายจากหน้า โดยไม่มีอะไรแจ้ง |

migration `20260812000000_mapping_submission_fk` ลบแถวที่อ้าง id ที่ไม่มีอยู่ ก่อน
จะเพิ่ม constraint ทั้งนี้ฐานข้อมูลที่ใช้พัฒนาอยู่ไม่มีแถว mapping เลยสักแถวเมื่อ
12 สิงหาคม 2569 จึงไม่มีอะไรถูกลบจริง

### frontend

**ไม่ต้องแก้** ทุกเส้นทางบนหน้าจอส่ง id ของนักศึกษาที่ล็อกอินอยู่แล้ว —
`create-work-form.tsx` เลือกจากรายการที่ `/student/activities/list` ส่งมา ซึ่งอ่าน
นักศึกษาจาก session (#41) และ `work-edit-modal.tsx` ส่ง id ที่ได้จาก `/works` ของ
ตัวเองกลับไป ไม่มีที่ไหนเรียก `POST /portfolio-skill` พร้อม `mappings` และไม่มีที่ไหน
เรียก `PUT /portfolio-skill/:id` เลย

### สิ่งที่ **ไม่ได้** ทำใน ticket นี้

- **แถวเก่าที่อ้างชิ้นงานของคนอื่นไม่ถูกตรวจย้อนหลัง** กฎใหม่บังคับที่ทางเขียนเท่านั้น
  migration ลบเฉพาะแถวที่อ้าง id ที่ไม่มีตัวตน ซึ่งไม่มีอยู่จริงในฐานข้อมูลนี้
- **`P2003` ยังไม่ถูกแปลที่ `errorHandler`** ยังเป็น `500` ตามที่ ADR-0012 ข้อ 3
  ตั้งใจปล่อยไว้ การตรวจของ ticket นี้อยู่ก่อนหน้านั้น foreign key จึงไม่ควรถูก
  ผู้เรียกเห็นเลย
- **คอลัมน์ไม่ถูกทำให้ `NOT NULL`** เกณฑ์ตรวจรับข้อ 3 ของ #47 ขอไว้ แต่ baseline
  ประกาศ `NOT NULL` มาตั้งแต่ต้น (บรรทัด 514) จึงไม่มีอะไรให้ทำ — แก้ที่ตัว issue แทน

---

## หมายเหตุ: สิ่งที่ **ไม่ได้** เปลี่ยน

รายการนี้ถูกไล่ตรวจกับโค้ดจริงทีละข้อเมื่อ **11 สิงหาคม 2569** หลัง #41 ทุกข้อที่
เขียนว่า **ปิดแล้ว** ได้รับการยืนยันจากโค้ดในตอนนั้น ไม่ใช่จากการที่ ticket ถูกปิด
ข้อที่ยังเป็นข้อบกพร่องจริงมีเลข issue ของตัวเองแล้วทุกข้อ — #43 ถึง #50 — และ
เหตุผลที่หมดอายุ (ส่วนใหญ่คือ "รอ #20" กับ "ระบบยังไม่มีการตรวจความเป็นเจ้าของ
section ที่ไหนเลย") ถูกเขียนใหม่ให้ตรงกับสิ่งที่เกิดขึ้นจริง

ข้อที่ไม่มีทั้ง "ปิดแล้ว" และเลข issue คือข้อที่**ตั้งใจให้เป็นแบบนั้น** ไม่ใช่ข้อ
ที่ยังไม่ได้ทำ

- **`401` / `403` ของทุก route ที่ตรวจบทบาท** — `requireRole("TEACHER")` และ
  `requireRole("STUDENT")` ตอบสถานะและข้อความไทยชุดเดิมทุกตัวอักษร
  (`สิทธิ์การเข้าถึงเฉพาะอาจารย์เท่านั้น` / `สิทธิ์การเข้าถึงเฉพาะนักศึกษาเท่านั้น`)
  การรวม middleware เป็นตัวเดียวเป็นเรื่องภายใน ผู้เรียกไม่ควรรู้สึกอะไร
- **`res.clearCookie("token", ...)` ที่โดเมน `.deep-core.net` ใน logout** — ยังคง
  ไว้เหมือนเดิมใน #10 (บรรทัดนี้ไม่เคยลบอะไรได้จริง เพราะ server ลบ cookie ของ
  โดเมนอื่นไม่ได้) และถูกเอาออกแล้วใน #11 ตามที่บอกไว้
- **โครงสร้างสิทธิ์ (#11)** — บทบาทยังอ่านจาก `user_roles` ทุก request เหมือนเดิม
  ตัวตนจาก Google ไม่ได้บอกอะไรเกี่ยวกับสิทธิ์เลย และ access token ใบใหม่ไม่มี
  `role` อยู่ข้างในแล้วด้วย (ของเดิมมี แต่ไม่เคยมีใครอ่าน — `requireRole` อ่านจาก
  ฐานข้อมูลอยู่แล้ว) รูปแบบ session เท่าเดิม: httpOnly cookie คู่ access/refresh
- **`POST /auth/refresh` ยังไม่ตรวจว่าผู้ใช้ยังมีอยู่จริง (#11)** — ตรวจแค่ลายเซ็นของ
  refresh token แล้วออก access token ใบใหม่ ผู้ใช้ที่ถูกลบไปแล้วจึง refresh สำเร็จได้
  แต่ access token ที่ได้ก็ใช้ไม่ได้อยู่ดี เพราะ `requireUser` ตรวจการมีอยู่จริงทุก
  request (ข้อ 1 ของ #10) การไล่เก็บให้ครบเป็นเรื่องของการ revoke session ซึ่งยังไม่มี
  ในระบบเลยและไม่ใช่ขอบเขตของ #11
- **ลิงก์ "DEEP-QA" ในเมนูของอาจารย์ (#11)** — ยังชี้ไป `https://deep-core.net/`
  เหมือนเดิม นั่นเป็นลิงก์ไปอีกผลิตภัณฑ์หนึ่ง ไม่ใช่เส้นทาง login สิ่งที่ #11 ตัด
  คือการ handshake ของ SSO ไม่ใช่ข้อเท็จจริงที่ว่าอาจารย์ใช้สองระบบ
- **query parameter ที่หายไปแล้วกลายเป็น `NaN`** (#12, #13) — **ปิดแล้ว** ดูหัวข้อ
  #20 ข้างบน ทั้งเจ็ดเส้น (`/score-weight`, `/lesson-plan`, `/course-material`,
  `/activity/list`, `/course/clo`, `/announcement`, `/learning-activity/list`)
  มี `validate({ query })` และ schema ทุกตัวบังคับ `section_id` เป็นตัวเลข
  คำขอที่ไม่ส่งมาจึงเป็น `400` ไม่ใช่รายการว่างหรือ `500` อีกต่อไป
- **`created_by` ของ `POST /announcement` ยังมาจาก body ไม่ใช่ session** (#12) —
  **ปิดแล้ว** ดูหัวข้อ #30 ข้างบน ข้อ 3 `created_by` อ่านจาก session และเส้นนี้มี
  `requireRole("TEACHER")` กับ `requireOwnSection("body")` แล้ว เหตุผลที่เขียนไว้
  ตอน #12 ว่า "ระบบยังไม่มีการตรวจความเป็นเจ้าของ section ที่ไหนเลย" หมดอายุไป
  พร้อมกับ #30
- **ไฟล์ใน bucket ไม่ถูกลบตาม `DELETE /course-material`** (#12) — **ปิดแล้วทั้งสอง
  ครึ่ง** ครึ่งการลบปิดใน #34: `deleteCourseMaterial` เรียก `deleteUnreferenced`
  แล้ว `removeFiles` หลัง commit ดูหัวข้อ #34 ข้างบน · ครึ่งการ**สร้าง** ปิดใน #50:
  `createCourseMaterial` ส่ง `tx` เข้า `createAttachments` แล้ว และไฟล์ที่อัปโหลด
  ไปก่อน transaction ล้มถูกเก็บคืน ดูหัวข้อ #50 ข้างบน
- **`PUT /activity` ลบคะแนนที่ตรวจไปแล้วทิ้ง** (#13) — route นี้ลบ
  `rubric_activity_mapping` ของกิจกรรมทั้งชุดแล้วสร้างใหม่ทุกครั้งที่บันทึก และ
  `student_activity_rubric_score` มี `ON DELETE CASCADE` อยู่ข้างหลัง การแก้แค่
  กำหนดส่งของกิจกรรมที่ตรวจไปแล้วจึงลบคะแนนรายเกณฑ์ของนักศึกษาทุกคนทิ้งเงียบๆ
  เป็นข้อเสียที่หนักที่สุดในกลุ่มนี้ แต่การแก้ให้ถูกต้องแปลว่า frontend ต้องส่ง
  `id` ของเกณฑ์เดิมกลับมาแทนที่จะส่งรายการใหม่ทั้งชุด ซึ่งเกินขอบเขตของ #13
  test บันทึกพฤติกรรมปัจจุบันไว้ตามที่เป็น และแยกไปเป็น #25 — **ปิดแล้ว** ดูหัวข้อ
  #25 ข้างบน
- **`POST /mapping/activity` ตอบ `500` เมื่อกิจกรรมยังไม่มีหมวดคะแนน** (#13) —
  service ใส่ `score_ratio_id: activity?.score_ratio_id ?? 0` ลงไปตรงๆ และ `0`
  ไม่ใช่ค่าที่มีอยู่จริงใน `subject_score_ratio` จึงติด foreign key แล้วกลาย
  เป็น `500` ทั้งที่ควรเป็น `400` ไม่ใช่งานของ #20 อย่างที่เขียนไว้ตอนนั้น เพราะ
  คำขอ**มีรูปแบบถูกต้องครบถ้วน** สิ่งที่ขาดคือข้อเท็จจริงในฐานข้อมูล และไม่ใช่งาน
  ของ #42 ด้วย เพราะเป็น `P2003` (foreign key) ไม่ใช่ `P2025` ที่ #42 แปลง — ดู
  ตารางรหัสใน [ADR-0012](docs/adr/0012-missing-row-status.md) ข้อ 3 ส่วน
  `score_ratio_id` ที่มาจาก **body** ของ `POST`/`PUT /activity` เป็นคนละจุดและปิด
  แล้วใน #42 ข้อ 2 · แยกไปเป็น
  [#43](https://github.com/khthana/Deep-Portfolio/issues/43) **ปิดแล้ว** ดูหัวข้อ
  #43 ข้างบน ตอนนี้ถามก่อนเขียน จึงไม่มี `P2003` ให้เกิด
- **`GET /rubric/shared-rubric` ที่ไม่ส่ง `program_id` คืน rubric ของทุกหลักสูตร**
  (#13) — **ปิดแล้ว** ดูหัวข้อ #20 ข้างบน `sharedRubricQuery` บังคับ `program_id`
  คำขอที่ไม่ส่งมาจึงเป็น `400` ก่อนถึง `where` · แต่เส้นนี้ยังไม่มี middleware ตรวจ
  สิทธิ์เลยแม้แต่ตัวเดียว ซึ่งเป็นคนละเรื่องกับ `400` และไม่เคยถูกบันทึกไว้ที่ไหน
  — แยกไปเป็น [#49](https://github.com/khthana/Deep-Portfolio/issues/49)
  **ปิดแล้ว** ดูหัวข้อ #49 ข้างบน ทั้งสองเส้นได้ `requireRole("TEACHER")`
- **`POST /group/validate-invite` และ `POST /group/accept-invite` ไม่ตรวจ session**
  (#14) — สองตัวนี้ถูกเรียกจากลิงก์ในอีเมล โดยคนที่อาจยังไม่ได้ล็อกอิน ตัว invite
  token (สุ่ม 32 ไบต์ อายุ 7 วัน) จึงเป็นสิทธิ์ทั้งหมด ใครถือ token ก็ตอบรับแทน
  นักศึกษาคนนั้นได้ นี่เป็นการออกแบบเดิมโดยตั้งใจ ไม่ใช่การตกหล่น จึงบันทึกไว้
  ตามที่เป็น ส่วน `action` ที่ส่งมาถูกเขียนลงคอลัมน์สถานะโดยไม่ตรวจค่า —
  **ปิดแล้วใน #20** ตอนนี้เป็น `z.enum(["ACCEPT", "REJECTED"])` ค่าอื่นได้ `400`
- **สาม `GET` ของกลุ่มรับ `student_id` จาก query โดยไม่ตรวจความเป็นเจ้าของ** (#14) —
  `/student-activity-group`, `/all` และ `/without-group` (รวมทั้งฝั่ง
  learning-activity) ตอบเรื่องนักศึกษาคนไหนก็ได้ให้ผู้เรียกคนไหนก็ได้ เหตุผล
  เดียวกับ `created_by` ของ #12 คือระบบยังไม่มีการตรวจความเป็นเจ้าของที่ไหนเลย
  การแก้เฉพาะจุดนี้ไม่ได้ปิดอะไรจริง หนักกว่านั้นคือ `/all` ที่ **ไม่ส่ง**
  `student_id` มาเลย จะคืนกลุ่มของทุกคนใน section เพราะ `undefined` ใน `some`
  แปลว่า "ไม่ต้องกรอง" test บันทึกไว้ตามที่เป็น และแยกไปเป็น #26 — **ปิดแล้ว** ดู
  หัวข้อ #26 ข้างบน `student_id` มาจาก session และ `/without-group` ตรวจการลงทะเบียน
- **`PATCH` กลุ่มที่ส่ง `members: []` ทำให้กลุ่มว่างเปล่าแบบกู้คืนไม่ได้** (#14) —
  service ลบสมาชิกทั้งหมดก่อนแล้วค่อยสร้างใหม่จากรายการที่ได้รับ รายการว่างจึง
  เหลือกลุ่มที่ไม่มีใครอยู่ และไม่มี endpoint ไหนพากลับมาได้ นอกจากนี้ยังไม่มีการ
  ตรวจว่าผู้เรียกเป็น LEADER ของกลุ่มนั้นจริง — สมาชิกคนไหนก็แก้รายชื่อได้ รวมกัน
  แล้วนักศึกษาคนใดก็ได้ที่รู้ `group_id` ลบกลุ่มของคนอื่นทิ้งได้ด้วย request เดียว
  การแก้ต้องตัดสินนโยบายเรื่องสิทธิ์ในกลุ่มก่อน ซึ่งเกินขอบเขตของ ticket ที่เขียน
  test ครอบพฤติกรรมเดิม test บันทึกไว้ตามที่เป็นทั้งสองฝั่ง และแยกไปเป็น #27 —
  **ปิดแล้ว** ดูหัวข้อ #27 ข้างบน รายชื่อว่างได้ `400` และการแก้เป็นสิทธิ์ของหัวหน้า
- **`GET /student-activity-group/all` ยุบกลุ่มที่มีสมาชิกชุดเดียวกันเหลือกลุ่มเดียว**
  (#14) — endpoint ทำ de-duplicate จาก "รายชื่อสมาชิกเรียงแล้ว" นักศึกษาคู่เดิม
  ที่ทำงานกลุ่มด้วยกันสองกิจกรรมจึงเห็นกลุ่มเดียว และ response ไม่มี `activity_id`
  ให้แยกแยะด้วย · #26 ปิดเรื่องความเป็นเจ้าของของเส้นนี้ไปแล้ว แต่ไม่ได้แตะการยุบ
  กลุ่ม ซึ่งยังเป็นอยู่ใน `getStudentActivityGroupInSec` — แยกไปเป็น
  [#44](https://github.com/khthana/Deep-Portfolio/issues/44) **ปิดแล้ว** —
  **ตัดสินแล้วว่าเป็นพฤติกรรมที่ตั้งใจ ไม่ใช่ของเสีย**
  ผู้เรียกมีที่เดียวคือรายการ "กลุ่มที่เคยสร้าง" ในโมดัลสร้างกลุ่ม
  ซึ่งหยิบ**รายชื่อ**ไปใช้ซ้ำ ไม่ได้อ่าน `group_id` เลย สองบรรทัดที่มีสมาชิกชุดเดียวกัน
  จึงเป็นบรรทัดที่ผู้ใช้แยกไม่ออกและคลิกแล้วได้ผลเท่ากัน การยุบจึงอยู่ต่อ และสิ่งที่แก้
  คือลำดับ — กลุ่มเรียงตาม `id` สมาชิกเรียงหัวหน้าก่อนแล้วตามรหัส ดูหัวข้อ #44 ข้างบน
  และ [ADR-0019](docs/adr/0019-group-list-is-member-lists.md)
- **การให้คะแนนกลุ่มรวมสมาชิกที่ยังไม่ตอบรับด้วย** (#14) — ทั้ง
  `POST /student-activity/grade` และฝั่ง learning-activity รวบรวมสมาชิกจาก
  `group_id` อย่างเดียว ไม่กรอง `status` คนที่ไม่เคยกดรับคำเชิญจึงได้คะแนนไปด้วย
  (ต่างจากการ**ส่งงาน** ที่กรอง `status: "ACCEPT"` ไว้แล้ว) ความไม่ตรงกันนี้
  บันทึกไว้ตามที่เป็น เพราะการเลือกข้างใดข้างหนึ่งเป็นการตัดสินใจเชิงนโยบาย
  ไม่ใช่การแก้ bug — แยกไปเป็น
  [#45](https://github.com/khthana/Deep-Portfolio/issues/45) **ปิดแล้ว** ดูหัวข้อ
  #45 ข้างบน การตัดสินใจเชิงนโยบายนั้นเกิดขึ้นแล้วใน
  [ADR-0017](docs/adr/0017-group-membership-means-accepted.md) และการให้คะแนน
  กรอง `ACCEPT` ตรงกับการส่งงานและการอ่านแล้ว
- **สถิติของกิจกรรมที่ยังไม่มีใครถูกตรวจเป็น `0` ไม่ใช่ `null`** (#15) —
  `GET /gradebook/per-activity` คำนวณ max/min/mean จากคะแนนของคนที่ตรวจแล้วเท่านั้น
  ไม่มีใครถูกตรวจเลยก็ตกไปที่ `0` ทั้งสามค่า กิจกรรมที่ทั้งห้องได้ 0 จริงๆ กับกิจกรรม
  ที่ยังไม่ได้เปิดตรวจจึงส่งค่าเดียวกันออกมา แยกได้ทางเดียวคือดู `graded_count` ประกอบ
  ซึ่งหน้าเว็บไม่ได้ดู ค่าชุดนี้ไหลต่อไปถึงนักศึกษาผ่าน `GET /evaluation/list` ด้วย
  การรายงาน `null` แทนเป็นการเปลี่ยน type ทั้งสองฝั่ง จึงแยกไปเป็น #28 —
  **ปิดแล้ว** ดูหัวข้อ #28 ข้างบน
- **งานที่ไม่เคยกรอกวันประกาศผล ไม่ถูกแสดงให้นักศึกษาเลย** (#15) —
  `GET /evaluation/list` ใช้ `checkIsOverAnnouncementDate` ซึ่งอ่าน `announcement_date`
  ที่เป็น `null` ว่า "ยังไม่ประกาศ" แต่ช่องนี้เป็น optional ตอนสร้างกิจกรรม อาจารย์ที่
  ไม่เคยกรอกและตรวจงานเสร็จแล้ว นักศึกษาก็ยังไม่เห็นผลตลอดไป โดยไม่มีอะไรบอกทั้งสองฝ่าย
  ว่าทำไม การเลือกความหมายของค่าว่าง — ประกาศทันที หรือบังคับให้กรอก — เป็นการตัดสินใจ
  เชิงนโยบาย จึงแยกไปเป็น #29 — **ปิดแล้ว** ดูหัวข้อ #29 ข้างบน
- **`/gradebook` ยังไม่ตรวจว่าอาจารย์คนนั้นสอน section นั้นจริง** (#15) — แยกไปเป็น
  #30 — **ปิดแล้ว** ดูหัวข้อ #30 ข้างบน ทุกเส้นของ `/gradebook` มี
  `requireOwnSection("query")` แล้ว
- **`GET /gradebook/per-activity` ที่ไม่ส่ง `section_id` ตอบ `200` ไม่ใช่ `500`** (#15) —
  **ปิดแล้ว** ดูหัวข้อ #20 ข้างบน `section_id` ถูกบังคับใน schema คำขอที่ไม่ส่งมาจึง
  เป็น `400` เท่ากับ `/per-student` แล้ว (เคสที่ล้มเหลวตาม T5 ยังใช้ `section_id`
  ที่เกินขนาดของ `Int` อยู่ เพราะเป็นคนละเรื่องกับการไม่ส่งมา)
- **กลุ่ม `portfolio` ทั้งกลุ่มไม่มีการตรวจสิทธิ์เลย** (#16) — 14 router ราว 60
  endpoint ไม่มี middleware แม้แต่ตัวเดียว และรับ `user_id` ของคนที่จะกระทำการแทน
  มาจาก query string หรือ body ต่างจาก `/gradebook` ที่ #15 ปิดไปได้ด้วย
  `requireRole("TEACHER")` บรรทัดเดียวเพราะกฎชัดว่า "ต้องเป็นอาจารย์" ที่นี่กฎที่ถูก
  คือ "ต้องเป็นเจ้าของแถว" ซึ่งระบบยังไม่มี และมีข้อยกเว้นที่ต้องเปิดสาธารณะจริงคือ
  `GET /portfolio/public/:token` จึงเป็นงานออกแบบ ไม่ใช่การซ่อมของเสีย test ทุกเคส
  ในกลุ่มนี้ยิงโดยไม่ส่ง cookie เลย และบันทึกไว้ตามที่เป็น แยกไปเป็น #31 พร้อมกับ
  กรณี `PUT` ที่เขียนทับคอลัมน์ `user_id` จาก body ได้ ซึ่ง pin ไว้ที่
  `portfolio-education.test.ts` แล้ว — **ปิดแล้ว** ดูหัวข้อ #31 ข้างบน
- **คอลัมน์ `Decimal` อื่นนอกกลุ่ม `portfolio`** (#16) — ค้างไว้ที่ #33 —
  **ปิดแล้ว** ดูหัวข้อ #33 ข้างบน
- **`PUT` ของ training และ certificate ล้างค่าวันที่/ปีให้ว่างไม่ได้** (#16) —
  **ปิดแล้ว** ดูหัวข้อ #20 ข้างบน `date` เป็น `clearableDate` และ `year` เป็น
  `clearableInteger` ซึ่งแยก "ไม่ได้ส่งมา" ออกจาก "ส่งค่าว่างมา" ที่ชั้น schema แล้ว
  service ก็เลิกแปลง falsy เป็น `undefined` และส่ง `updateData` ต่อไปตรง ๆ
  (award และ portfolio-activity ที่ #17 พบว่าเป็นแบบเดียวกัน ปิดไปพร้อมกัน — ทั้ง
  เจ็ดส่วนของ e-Portfolio ใช้ `clearable*` ชุดเดียวกัน)
- **`POST /portfolio-award` ที่ไม่ส่ง `is_show` มา สร้างรางวัลแบบ "ซ่อน"** (#17) —
  service เขียน `is_show: is_show === true` ค่าที่ไม่ได้ส่งมาจึงกลายเป็น `false` และ
  ค่าตั้งต้น `true` ของคอลัมน์ไม่มีโอกาสได้ทำงาน ต่างจากทุกส่วนที่เหลือในกลุ่มที่
  ปล่อยให้ค่าตั้งต้นทำงาน หน้าเว็บส่งช่องนี้มาทุกครั้งจึงยังไม่มีใครเจอ การเลือกว่า
  ค่าที่หายไปควรแปลว่าอะไรเป็นการตัดสินใจ ไม่ใช่การซ่อมของเสีย และ `PUT` ของ
  endpoint เดียวกันตอบคำถามนี้ไว้คนละแบบอยู่แล้ว (ไม่ส่งมา = ไม่แก้)
  ที่เขียนไว้ตอน #17 ว่า "รอ #20" นั้นไม่ตรง — #20 ทำเสร็จแล้วและตั้งใจไม่แตะจุดนี้
  เพราะ `optionalBool` ปล่อย `undefined` ผ่านออกไปถูกต้องอยู่แล้ว ตัวที่แปลงเป็น
  `false` คือ service ไม่ใช่ validation แยกไปเป็น #46 — **ปิดแล้ว** ดูหัวข้อ #46
  ข้างบน การแปลงถูกลบทิ้ง ค่าที่ไม่ได้ส่งมาจึงได้ `@default(true)` ของคอลัมน์
  เหมือนอีกหกส่วน ส่วน `PUT` ยังแปลว่า "ไม่ส่งมา = ไม่แก้" ตาม
  [ADR-0018](docs/adr/0018-absent-flag-means-column-default.md)
- **`portfolio_skill_activity_mapping.student_activity_id` ไม่มี foreign key** (#17) —
  `POST /portfolio-skill/assign-work` และ `POST /portfolio-skill` จึงรับ id ของงานส่ง
  ที่ไม่มีอยู่จริงได้ แถวถูกเขียนสำเร็จ และ `GET /portfolio-skill/works` ก็แค่หา
  `feedback` ไม่เจอแล้วคืน `null` การเพิ่ม constraint ย้อนหลังต้องตรวจข้อมูลเดิม
  ที่มีอยู่ก่อน และเป็นการแก้ schema ซึ่งอยู่นอกขอบเขตของ ticket ที่เขียน test
  test บันทึกไว้ตามที่เป็น — แยกไปเป็น
  [#47](https://github.com/khthana/Deep-Portfolio/issues/47) — **แก้แล้ว** ดูหัวข้อ
  #47 ข้างบน คอลัมน์มี foreign key แบบ `ON DELETE CASCADE` แล้ว และทั้งสามเส้นที่
  เขียน mapping (`PUT /portfolio-skill/:id` เป็นเส้นที่สาม ซึ่ง #17 ไม่ได้นับ) ตอบ
  `400` ให้ id ที่ไม่มีตัวตน และ `403` ให้ชิ้นงานของคนอื่น
- **`PUT` และ `DELETE` ที่อ้าง id ที่ไม่มีอยู่ ตอบ `500` ไม่ใช่ `404`** (#16, #17) —
  ทั้งหกส่วนของ e-Portfolio (training, certificate, internship, award, thesis,
  activity) มี controller ที่ตรวจ `404` ให้เฉพาะ `GET /:id` ส่วน `PUT` และ `DELETE`
  เรียก Prisma ตรง ๆ แล้วปล่อยให้ `P2025` ("record not found") วิ่งไปถึง
  `errorHandler` ซึ่งไม่รู้จัก error code ของ Prisma จึงตอบ `500` ผู้เรียกจึงอ่านว่า
  "เซิร์ฟเวอร์พัง" ทั้งที่จริงคือ "ไม่มีของชิ้นนี้" แยกไปเป็น #42 — **ปิดแล้ว** ดู
  หัวข้อ #42 ข้างบน `errorHandler` แปลง `P2025` เป็น `404` เป็นพื้นทั้งระบบ และ
  สิบ controller ที่มีประโยคของตัวเองก็ใช้ประโยคเดิมที่ `GET /:id` ใช้อยู่
- **`GET /user` ไม่มี middleware และคืนแถว `users` ทั้งแถว** (#18) — แยกไปเป็น #40 —
  **ปิดแล้ว** ดูหัวข้อ #40 ข้างบน ตอนนี้ต้องล็อกอิน และ `id` ที่ไม่ใช่ของ session
  ตอบ `403` ที่เขียนไว้ตอน #18 ว่าเป็นเรื่องเดียวกับ #31 นั้นถูกครึ่งเดียว — #31
  ปิดเฉพาะกลุ่ม `portfolio` เส้นนี้อยู่นอกกลุ่มจึงตกค้างมาจนถึง #40
- **`POST /student/submit/activity` ไม่ตรวจว่างานที่ส่งเป็นของผู้ส่ง** (#18) — session
  บอกว่าใครเป็นคนยิง แต่ `student_activity_id` มาจาก body และไม่มีอะไรตรวจว่าสองอย่าง
  นี้ตรงกัน นักศึกษาที่รู้ id ของเพื่อนจึงส่งงานแทนเพื่อนได้ และไฟล์แนบเดิมของเพื่อน
  ก็ถูกแทนที่ด้วยของที่มากับคำขอนั้น (endpoint นี้ลบของเดิมทิ้งทุกครั้งก่อนผูกของใหม่)
  แยกไปเป็น #38 — **ปิดแล้ว** ดูหัวข้อ #38 ข้างบน ทั้งสี่เส้นตรวจแล้ว และทางกลุ่มก็
  ตรวจการเป็นสมาชิกด้วย
- **section ที่ไม่มีอาจารย์ผู้สอน หายไปจากรายวิชาและปฏิทินของนักศึกษาทั้งอัน** (#18) —
  `getStudentCourseList` สร้างแต่ละรายการผ่าน `getCourseDetail` ซึ่งคืน `null` เมื่อ
  ไม่มีแถวใน `course_teachers` แล้วค่า `null` ถูกกรองทิ้ง นักศึกษาที่ลงทะเบียนไว้จริง
  จึงไม่เห็นทั้งวิชาและงานทุกชิ้นในวิชานั้น โดยไม่มีอะไรบอกว่าหายไปเพราะอะไร การเลือก
  ว่าควรตอบอย่างไร — ซ่อนต่อ หรือแสดงโดยเว้นชื่ออาจารย์ไว้ — เป็นการตัดสินใจเชิงนโยบาย
  ไม่ใช่การซ่อมของเสีย test มีเคสที่ pin พฤติกรรมนี้ไว้ตรง ๆ — แยกไปเป็น
  [#48](https://github.com/khthana/Deep-Portfolio/issues/48)
- **`GET /student/enrolled/subjects` ที่ไม่ส่ง `student_id` คืนการลงทะเบียนของทุกคน**
  (#18) — **ปิดแล้วสองรอบ** #20 ทำให้เป็น `400` และ #40 ตัด `student_id` ออกจาก
  query ทั้งตัว โดยอ่านนักศึกษาจาก session แทน ดูหัวข้อ #40 ข้างบน ครึ่งที่ #18
  บันทึกไว้ว่า "ไม่มี middleware ให้ตกด้วย" ก็ปิดพร้อมกันใน #40
- **`GET /user` ที่ไม่ส่ง `id` ตอบ `500` ส่วน `GET /student/activities/list` ที่ไม่ส่ง
  `section_id` ตอบ `200` พร้อม `[]`** (#18) — **ปิดแล้ว** #20 ทำให้ทั้งคู่เป็น `400`
  (ดู "รายการที่เคยเขียนไว้ว่า รอ #20 และปิดแล้วในรอบนี้" ในหัวข้อ #20) บรรทัดนี้
  ค้างอยู่เพราะไม่ได้ตามมาลบตอนนั้น เรื่องที่ยังเหลือคือ
  `GET /student/activities/list` ไม่มี middleware ซึ่งเป็น
  [#41](https://github.com/khthana/Deep-Portfolio/issues/41) ไม่ใช่เรื่องของ `400`
  — **ปิดครบแล้ว** ดูหัวข้อ #41 ข้างบน เส้นนั้นได้ `requireRole("STUDENT")` กับ
  `requireEnrolledSection` แล้ว และ `student_id` ที่เป็นต้นเหตุถูกตัดออกจาก query
- **การลบช่วงคะแนนกลาง ๆ ทำให้คะแนนที่ให้ไว้เพี้ยนความหมาย** (#25) — แยกไปเป็น #39 —
  **ปิดแล้ว** ดูหัวข้อ #39 ข้างบน `rubric_levels.id` เดินทางไป-กลับกับ form แล้ว และ
  กลายเป็นสิ่งที่บอกว่าแถวไหนคือแถวไหน ส่วนที่บันทึกไว้ตอน #25 ว่าต้องเปลี่ยนสัญญาของ
  `GET /activity` ด้วยนั้นไม่จริง — endpoint คืน `id` มาตั้งแต่แรก form เป็นฝ่ายทิ้งมัน
