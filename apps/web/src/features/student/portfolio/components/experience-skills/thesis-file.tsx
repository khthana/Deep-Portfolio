import type { MockThesisAttachmentType } from "../../types/experience-skill-type.type";

type Props = {
  name: string;
  type: MockThesisAttachmentType;
};

const ThesisFile = (props: Props) => {
  return <div>ThesisFile</div>;
};

export default ThesisFile;
