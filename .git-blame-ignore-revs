# Commits that moved lines without changing what they do. `git blame` should
# look straight through them to whoever wrote the line.
#
# GitHub reads this file on its own. Locally it has to be pointed at once:
#
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# Only add a revision here if it is purely mechanical — a formatter run, a
# rename applied by a tool. A commit that changes behaviour does not belong,
# however small the change looks.

# style: run prettier over the repo, once (#60)
14e7fc95f4c4bfca2855b4cfa936fa05a801c75d
